---
title: "六、两类函数相乘的积分"
sidebar_position: 6
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 六、两类函数相乘的积分

## 小题

<ExamQuestion>

1. **【1989-5-5 分】** 求不定积分

$$
 \int \frac{x+\ln(1-x)}{x^2}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int \frac{x+\ln(1-x)}{x^2}\,dx
=\ln(1-x)-\frac{\ln(1-x)}{x}+C.
$$
</ExamAnswer>

<ExamExplanation>
先将积分拆分为

$$
   I=\int\frac{dx}{x}+\int\frac{\ln(1-x)}{x^2}\,dx.
$$

对第二项使用分部积分，取

$$
   u=\ln(1-x),\qquad dv=\frac{dx}{x^2},
$$

则

$$
   du=-\frac{dx}{1-x},\qquad v=-\frac1x.
$$

因而

$$
  \begin{aligned}
   \int\frac{\ln(1-x)}{x^2}\,dx
  &=-\frac{\ln(1-x)}x-\int\frac{dx}{x(1-x)}\\
  &=-\frac{\ln(1-x)}x-\int\left(\frac1x+\frac1{1-x}\right)dx\\
  &=-\frac{\ln(1-x)}x-\ln|x|+\ln(1-x)+C.
  \end{aligned}
$$

再与第一项 $ \int\frac{dx}{x}=\ln|x|$ 相加，$\ln|x|$ 正好抵消，得到

$$
   I=\ln(1-x)-\frac{\ln(1-x)}x+C.
$$

由于原式含有 $\ln(1-x)$，其实数定义域要求 $x<1$，同时还需 $x\ne0$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1989-3-4 分】** 求

$$
 \int \frac{dx}{x\ln^2x}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int \frac{dx}{x\ln^2x}=-\frac1{\ln x}+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   u=\ln x,
$$

则

$$
   du=\frac{dx}{x}.
$$

因此

$$
  \begin{aligned}
   \int\frac{dx}{x\ln^2x}
  &=\int u^{-2}\,du\\
  &=-u^{-1}+C\\
  &=-\frac1{\ln x}+C.
  \end{aligned}
$$

本题使用的是复合函数积分公式，原被积函数的定义要求 $x>0$ 且 $x\ne1$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1990-3-5 分】** 计算

$$
 \int \frac{\ln x}{(1-x)^2}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int \frac{\ln x}{(1-x)^2}\,dx
=\frac{x\ln x}{1-x}+\ln|1-x|+C.
$$
</ExamAnswer>

<ExamExplanation>
取

$$
   u=\ln x,\qquad dv=\frac{dx}{(1-x)^2},
$$

则

$$
   du=\frac{dx}{x},\qquad v=\frac1{1-x}.
$$

由分部积分公式，

$$
  \begin{aligned}
   I
  &=\frac{\ln x}{1-x}-\int\frac{dx}{x(1-x)}\\
  &=\frac{\ln x}{1-x}-\int\left(\frac1x+\frac1{1-x}\right)dx\\
  &=\frac{\ln x}{1-x}-\ln x+\ln|1-x|+C.
  \end{aligned}
$$

合并前两项：

$$
   \frac{\ln x}{1-x}-\ln x
=\frac{x\ln x}{1-x},
$$

故

$$
   I=\frac{x\ln x}{1-x}+\ln|1-x|+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【1990-5-5 分】** 求不定积分

$$
 \int \frac{x\cos^4\frac{x}{2}}{\sin^3x}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int \frac{x\cos^4\frac{x}{2}}{\sin^3x}\,dx
=-\frac{x}{8}\cot^2\frac{x}{2}-\frac14\cot\frac{x}{2}-\frac{x}{8}+C.
$$
</ExamAnswer>

<ExamExplanation>
利用

$$
   \sin x=2\sin\frac{x}{2}\cos\frac{x}{2},
$$

有

$$
  \begin{aligned}
   \frac{\cos^4\frac{x}{2}}{\sin^3x}
  &=\frac{\cos^4\frac{x}{2}}{8\sin^3\frac{x}{2}\cos^3\frac{x}{2}}\\
  &=\frac18\cot\frac{x}{2}\csc^2\frac{x}{2}.
  \end{aligned}
$$

又因为

$$
   \frac{d}{dx}\left(\cot^2\frac{x}{2}\right)
=-\cot\frac{x}{2}\csc^2\frac{x}{2},
$$

所以

$$
   I=-\frac18\int x\,d\left(\cot^2\frac{x}{2}\right).
$$

分部积分得

$$
   I=-\frac{x}{8}\cot^2\frac{x}{2}
+\frac18\int\cot^2\frac{x}{2}\,dx.
$$

利用 $ \cot^2u=\csc^2u-1$，可得

$$
  \begin{aligned}
   \int\cot^2\frac{x}{2}\,dx
  &=\int\left(\csc^2\frac{x}{2}-1\right)dx\\
  &=-2\cot\frac{x}{2}-x+C.
  \end{aligned}
$$

代回即得

$$
   I=-\frac{x}{8}\cot^2\frac{x}{2}
-\frac14\cot\frac{x}{2}-\frac{x}{8}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【1991-5-5 分】** 求不定积分

$$
 I=\int \frac{x^2}{1+x^2}\arctan x\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 I=x\arctan x-\frac12\ln(1+x^2)
-\frac12(\arctan x)^2+C.
$$
</ExamAnswer>

<ExamExplanation>
先作恒等变形

$$
   \frac{x^2}{1+x^2}=1-\frac1{1+x^2}.
$$

因此

$$
   I=\int\arctan x\,dx
-\int\frac{\arctan x}{1+x^2}\,dx.
$$

第一项分部积分：

$$
  \begin{aligned}
   \int\arctan x\,dx
  &=x\arctan x-\int\frac{x}{1+x^2}\,dx\\
  &=x\arctan x-\frac12\ln(1+x^2).
  \end{aligned}
$$

第二项令 $ u=\arctan x$，则 $ du=\frac{dx}{1+x^2}$，故

$$
   \int\frac{\arctan x}{1+x^2}\,dx
=\frac12(\arctan x)^2.
$$

合并得到

$$
   I=x\arctan x-\frac12\ln(1+x^2)
-\frac12(\arctan x)^2+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【1991-3-5 分】** 求

$$
 \int x\sin^2x\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int x\sin^2x\,dx
=\frac{x^2}{4}-\frac{x\sin2x}{4}-\frac{\cos2x}{8}+C.
$$
</ExamAnswer>

<ExamExplanation>
由降幂公式

$$
   \sin^2x=\frac{1-\cos2x}{2},
$$

可得

$$
   I=\frac12\int x\,dx-\frac12\int x\cos2x\,dx.
$$

对第二项分部积分，取

$$
   u=x,\qquad dv=\cos2x\,dx,
$$

则

$$
   du=dx,\qquad v=\frac12\sin2x.
$$

因而

$$
  \begin{aligned}
   \int x\cos2x\,dx
  &=\frac{x\sin2x}{2}-\frac12\int\sin2x\,dx\\
  &=\frac{x\sin2x}{2}+\frac{\cos2x}{4}.
  \end{aligned}
$$

代回后得到

$$
   I=\frac{x^2}{4}-\frac{x\sin2x}{4}
-\frac{\cos2x}{8}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【1992-45-5 分】** 计算

$$
 I=\int \frac{\operatorname{arccot}e^x}{e^x}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 I=-e^{-x}\operatorname{arccot}e^x-x
+\frac12\ln(1+e^{2x})+C.
$$
</ExamAnswer>

<ExamExplanation>
将原式写成

$$
   I=\int e^{-x}\operatorname{arccot}e^x\,dx.
$$

取

$$
   u=\operatorname{arccot}e^x,
  \qquad dv=e^{-x}dx,
$$

则

$$
   du=-\frac{e^x}{1+e^{2x}}dx,
  \qquad v=-e^{-x}.
$$

由分部积分公式，

$$
  \begin{aligned}
   I
  &=-e^{-x}\operatorname{arccot}e^x
-\int\frac{dx}{1+e^{2x}}.
  \end{aligned}
$$

对剩余积分作变形：

$$
  \begin{aligned}
   \int\frac{dx}{1+e^{2x}}
  &=\int\left(1-\frac{e^{2x}}{1+e^{2x}}\right)dx\\
  &=x-\frac12\ln(1+e^{2x})+C.
  \end{aligned}
$$

因此

$$
   I=-e^{-x}\operatorname{arccot}e^x-x
+\frac12\ln(1+e^{2x})+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. **【1993-12-5 分】** 求

$$
 \int \frac{xe^x}{\sqrt{e^x-1}}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int \frac{xe^x}{\sqrt{e^x-1}}\,dx
=2x\sqrt{e^x-1}-4\sqrt{e^x-1}
+4\arctan\sqrt{e^x-1}+C.
$$
</ExamAnswer>

<ExamExplanation>
注意到

$$
   d\sqrt{e^x-1}=\frac{e^x}{2\sqrt{e^x-1}}dx,
$$

因此

$$
   \frac{e^x}{\sqrt{e^x-1}}dx
=2\,d\sqrt{e^x-1}.
$$

对原积分分部积分，得

$$
  \begin{aligned}
   I
  &=2\int x\,d\sqrt{e^x-1}\\
  &=2x\sqrt{e^x-1}-2\int\sqrt{e^x-1}\,dx.
  \end{aligned}
$$

记

$$
   J=\int\sqrt{e^x-1}\,dx,
$$

令

$$
   t=\sqrt{e^x-1},
$$

则 $ e^x=1+t^2$，且

$$
   dx=\frac{2t}{1+t^2}dt.
$$

从而

$$
  \begin{aligned}
   J
  &=2\int\frac{t^2}{1+t^2}dt\\
  &=2\int\left(1-\frac1{1+t^2}\right)dt\\
  &=2t-2\arctan t+C.
  \end{aligned}
$$

代回 $ t=\sqrt{e^x-1}$，并代入 $I$，得到

$$
   I=2x\sqrt{e^x-1}-4\sqrt{e^x-1}
+4\arctan\sqrt{e^x-1}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. **【1993-3-3 分】** 已知曲线 $ y=f(x)$ 过点 $ \left(0,-\frac12\right)$，且其上任一点 $ (x,y)$ 处的切线斜率为 $ x\ln(1+x^2)$，则 $ f(x)=\underline{\qquad}$。

<ExamSolution>

<ExamAnswer>
$$
 f(x)=\frac{1+x^2}{2}\ln(1+x^2)-\frac{1+x^2}{2}.
$$
</ExamAnswer>

<ExamExplanation>
由切线斜率条件可知

$$
   f'(x)=x\ln(1+x^2).
$$

两边积分，令

$$
   u=1+x^2,\qquad du=2x\,dx,
$$

则

$$
  \begin{aligned}
   f(x)
  &=\int x\ln(1+x^2)dx\\
  &=\frac12\int\ln u\,du\\
  &=\frac12(u\ln u-u)+C\\
  &=\frac{1+x^2}{2}\ln(1+x^2)-\frac{1+x^2}{2}+C.
  \end{aligned}
$$

利用曲线过点 $ \left(0,-\frac12\right)$，有

$$
   -\frac12=f(0)=-\frac12+C,
$$

故 $ C=0$，从而

$$
   f(x)=\frac{1+x^2}{2}\ln(1+x^2)-\frac{1+x^2}{2}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. **【1994-3-3 分】**

$$
 \int x^3e^{x^2}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int x^3e^{x^2}\,dx
=\frac12(x^2-1)e^{x^2}+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   u=x^2,
$$

则

$$
   du=2x\,dx,
  \qquad x^3dx=x^2(xdx)=\frac{u}{2}du.
$$

因而

$$
  \begin{aligned}
   \int x^3e^{x^2}\,dx
  &=\frac12\int ue^u\,du\\
  &=\frac12(ue^u-e^u)+C\\
  &=\frac12(u-1)e^u+C\\
  &=\frac12(x^2-1)e^{x^2}+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. **【1997-2-5 分】** 计算

$$
 \int e^{2x}(\tan x+1)^2\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int e^{2x}(\tan x+1)^2\,dx=e^{2x}\tan x+C.
$$
</ExamAnswer>

<ExamExplanation>
利用恒等式

$$
   (\tan x+1)^2=\tan^2x+2\tan x+1=\sec^2x+2\tan x,
$$

原积分化为

$$
   \int e^{2x}(\sec^2x+2\tan x)\,dx.
$$

又因为

$$
  \begin{aligned}
   \frac{d}{dx}\left(e^{2x}\tan x\right)
  &=2e^{2x}\tan x+e^{2x}\sec^2x\\
  &=e^{2x}(\sec^2x+2\tan x),
  \end{aligned}
$$

所以

$$
   \int e^{2x}(\tan x+1)^2\,dx=e^{2x}\tan x+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

12. **【1998-34-3 分】**

$$
 \int \frac{\ln x-1}{x^2}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 -\frac{\ln x}{x}+C.
$$
</ExamAnswer>

<ExamExplanation>
由商的求导法则，

$$
  \begin{aligned}
   \left(\frac{\ln x}{x}\right)'
  &=\frac{1-\ln x}{x^2},
  \end{aligned}
$$

因此

$$
   \frac{\ln x-1}{x^2}
=-\left(\frac{\ln x}{x}\right)'.
$$

故

$$
   \int \frac{\ln x-1}{x^2}\,dx
=-\frac{\ln x}{x}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

13. **【1998-2-3 分】**

$$
 \int \frac{\ln\sin x}{\sin^2x}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 -\cot x\ln|\sin x|-\cot x-x+C.
$$
</ExamAnswer>

<ExamExplanation>
在被积函数有定义的区间内，作分部积分。取

$$
   u=\ln|\sin x|,\qquad dv=\csc^2x\,dx,
$$

则

$$
   du=\cot x\,dx,\qquad v=-\cot x.
$$

因而

$$
  \begin{aligned}
   \int \ln|\sin x|\csc^2x\,dx
  &=-\cot x\ln|\sin x|+\int\cot^2x\,dx\\
  &=-\cot x\ln|\sin x|+\int(\csc^2x-1)\,dx\\
  &=-\cot x\ln|\sin x|-\cot x-x+C.
  \end{aligned}
$$

在 $\sin x>0$ 的区间内可写为 $\ln\sin x+C$；写成 $\ln|\sin x|+C$ 可覆盖所有被积函数有定义的区间。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

14. **【2000-2-5 分】** 设

$$
 f(\ln x)=\frac{\ln(1+x)}{x},
$$

计算

$$
 \int f(x)\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \int f(x)\,dx
=x-\left(1+e^{-x}\right)\ln(1+e^x)+C.
$$
</ExamAnswer>

<ExamExplanation>
令 $t=\ln x$，则 $x=e^t$，由已知条件得

$$
   f(t)=e^{-t}\ln(1+e^t).
$$

将自变量 $t$ 改写为 $x$，有

$$
   f(x)=e^{-x}\ln(1+e^x).
$$

于是

$$
   I=\int e^{-x}\ln(1+e^x)\,dx.
$$

分部积分，取

$$
   u=\ln(1+e^x),\qquad dv=e^{-x}\,dx,
$$

则

$$
   du=\frac{e^x}{1+e^x}\,dx,\qquad v=-e^{-x}.
$$

因此

$$
  \begin{aligned}
   I
  &=-e^{-x}\ln(1+e^x)+\int\frac{1}{1+e^x}\,dx\\
  &=-e^{-x}\ln(1+e^x)+x-\ln(1+e^x)+C\\
  &=x-\left(1+e^{-x}\right)\ln(1+e^x)+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

15. **【2000-4-3 分】**

$$
 \int \frac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   t=\sqrt{x},\qquad x=t^2,\qquad dx=2t\,dt,
$$

则

$$
   \int \frac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx
=2\int\arcsin t\,dt.
$$

对 $\int\arcsin t\,dt$ 分部积分：

$$
  \begin{aligned}
   \int\arcsin t\,dt
  &=t\arcsin t-\int\frac{t}{\sqrt{1-t^2}}\,dt\\
  &=t\arcsin t+\sqrt{1-t^2}+C.
  \end{aligned}
$$

因此

$$
  \begin{aligned}
   \int \frac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx
  &=2t\arcsin t+2\sqrt{1-t^2}+C\\
  &=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

16. **【2002-4-3 分】** 已知 $f(x)$ 的一个原函数为 $\ln^2x$，则

$$
 \int xf'(x)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 2\ln x-\ln^2x+C.
$$
</ExamAnswer>

<ExamExplanation>
因为 $\ln^2x$ 是 $f(x)$ 的一个原函数，所以

$$
   f(x)=\left(\ln^2x\right)'=\frac{2\ln x}{x}.
$$

对原积分分部积分，取 $u=x$，$dv=f'(x)dx$，则

$$
  \begin{aligned}
   \int xf'(x)\,dx
  &=xf(x)-\int f(x)\,dx\\
  &=x\cdot\frac{2\ln x}{x}-\ln^2x+C\\
  &=2\ln x-\ln^2x+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

17. **【2018-3-4 分】**

$$
 \int e^x\arcsin e^x\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 e^x\arcsin e^x+\sqrt{1-e^{2x}}+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
 t=e^x,\qquad dt=e^x\,dx,
$$

则

$$
 \int e^x\arcsin e^x\,dx=\int\arcsin t\,dt.
$$

对右端分部积分，取

$$
 u=\arcsin t,\qquad dv=dt,
$$

则

$$
 du=\frac{dt}{\sqrt{1-t^2}},\qquad v=t.
$$

因此

$$
\begin{aligned}
 \int\arcsin t\,dt
&=t\arcsin t-\int\frac{t}{\sqrt{1-t^2}}\,dt\\
&=t\arcsin t+\sqrt{1-t^2}+C.
\end{aligned}
$$

代回 $ t=e^x$，得

$$
 \int e^x\arcsin e^x\,dx
=e^x\arcsin e^x+\sqrt{1-e^{2x}}+C.
$$

在实数范围内，需有 $ e^x\le1$，即 $ x\le0$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 大题

<ExamQuestion>

1. **【1994-5-6 分】** 已知 $ \frac{\sin x}{x}$ 是函数 $f(x)$ 的一个原函数，求

$$
 \int x^3f'(x)\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 x^2\cos x-4x\sin x-6\cos x+C.
$$
</ExamAnswer>

<ExamExplanation>
因为 $ \frac{\sin x}{x}$ 是 $f(x)$ 的一个原函数，所以

$$
  \begin{aligned}
   f(x)
  &=\left(\frac{\sin x}{x}\right)'\\
  &=\frac{x\cos x-\sin x}{x^2}.
  \end{aligned}
$$

对所求积分分部积分：

$$
  \begin{aligned}
   \int x^3f'(x)\,dx
  &=x^3f(x)-\int3x^2f(x)\,dx\\
  &=x\bigl(x\cos x-\sin x\bigr)
-3\int(x\cos x-\sin x)\,dx.
  \end{aligned}
$$

又

$$
  \begin{aligned}
   \int(x\cos x-\sin x)\,dx
  &=\int x\cos x\,dx-\int\sin x\,dx\\
  &=x\sin x+\cos x+\cos x\\
  &=x\sin x+2\cos x.
  \end{aligned}
$$

因而

$$
  \begin{aligned}
   \int x^3f'(x)\,dx
  &=x^2\cos x-x\sin x-3x\sin x-6\cos x+C\\
  &=x^2\cos x-4x\sin x-6\cos x+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1995-5-7 分】** 求不定积分

$$
 \int(\arcsin x)^2\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 x(\arcsin x)^2+2\sqrt{1-x^2}\arcsin x-2x+C.
$$
</ExamAnswer>

<ExamExplanation>
设

$$
   I=\int(\arcsin x)^2\,dx.
$$

第一次分部积分，取

$$
   u=(\arcsin x)^2,\qquad dv=dx,
$$

则

$$
   du=\frac{2\arcsin x}{\sqrt{1-x^2}}\,dx,
  \qquad v=x.
$$

所以

$$
   I=x(\arcsin x)^2
-2\int\frac{x\arcsin x}{\sqrt{1-x^2}}\,dx.
$$

记

$$
   J=\int\frac{x\arcsin x}{\sqrt{1-x^2}}\,dx.
$$

注意到

$$
   \frac{x}{\sqrt{1-x^2}}\,dx=-d\sqrt{1-x^2},
$$

再次分部积分得

$$
  \begin{aligned}
   J
  &=-\int\arcsin x\,d\sqrt{1-x^2}\\
  &=-\sqrt{1-x^2}\arcsin x
+\int\sqrt{1-x^2}\cdot\frac{dx}{\sqrt{1-x^2}}\\
  &=-\sqrt{1-x^2}\arcsin x+x.
  \end{aligned}
$$

故

$$
  \begin{aligned}
   I
  &=x(\arcsin x)^2-2J\\
  &=x(\arcsin x)^2+2\sqrt{1-x^2}\arcsin x-2x+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1996-3-8 分】** 计算不定积分

$$
 \int \frac{\arctan x}{x^2(1+x^2)}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 -\frac{\arctan x}{x}+\ln|x|-\frac12\ln(1+x^2)
-\frac12(\arctan x)^2+C.
$$
</ExamAnswer>

<ExamExplanation>
先拆分有理因子：

$$
   \frac{1}{x^2(1+x^2)}
=\frac{1}{x^2}-\frac{1}{1+x^2}.
$$

因此

$$
   I
=\int\frac{\arctan x}{x^2}\,dx
-\int\frac{\arctan x}{1+x^2}\,dx.
$$

对第一项分部积分，取

$$
   u=\arctan x,
  \qquad dv=\frac{dx}{x^2},
$$

则

$$
   du=\frac{dx}{1+x^2},
  \qquad v=-\frac1x.
$$

所以

$$
  \begin{aligned}
   \int\frac{\arctan x}{x^2}\,dx
  &=-\frac{\arctan x}{x}
+\int\frac{dx}{x(1+x^2)}\\
  &=-\frac{\arctan x}{x}
+\int\left(\frac1x-\frac{x}{1+x^2}\right)dx\\
  &=-\frac{\arctan x}{x}
+\ln|x|-\frac12\ln(1+x^2).
  \end{aligned}
$$

第二项令 $ t=\arctan x$，则 $ dt=\frac{dx}{1+x^2}$，故

$$
   \int\frac{\arctan x}{1+x^2}\,dx
=\frac12(\arctan x)^2.
$$

综上，

$$
   I
=-\frac{\arctan x}{x}+\ln|x|-\frac12\ln(1+x^2)
-\frac12(\arctan x)^2+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【1999-4-6 分】** 设 $F(x)$ 为 $f(x)$ 的原函数，且当 $x\ge0$ 时，

$$
 f(x)F(x)=\frac{xe^x}{2(1+x)^2},
$$

已知 $ F(0)=1$，$ F(x)>0$，试求 $f(x)$。

<ExamSolution>

<ExamAnswer>
$$
 f(x)=\frac{xe^{x/2}}{2(1+x)^{3/2}},\qquad x\ge0.
$$
</ExamAnswer>

<ExamExplanation>
因为 $F(x)$ 是 $f(x)$ 的原函数，所以

$$
   F'(x)=f(x).
$$

由题设，

$$
   F(x)F'(x)=\frac{xe^x}{2(1+x)^2},
$$

从而

$$
   \bigl[F^2(x)\bigr]'
=\frac{xe^x}{(1+x)^2}.
$$

注意到

$$
   \left(\frac{e^x}{1+x}\right)'
=\frac{xe^x}{(1+x)^2},
$$

因此

$$
   F^2(x)=\frac{e^x}{1+x}+C.
$$

代入 $ F(0)=1$，得

$$
   1=1+C,
$$

故 $ C=0$。又因 $ F(x)>0$，所以

$$
   F(x)=\sqrt{\frac{e^x}{1+x}}
=\frac{e^{x/2}}{\sqrt{1+x}}.
$$

最后求导：

$$
  \begin{aligned}
   f(x)=F'(x)
  &=\frac{e^{x/2}}{2\sqrt{1+x}}
-\frac{e^{x/2}}{2(1+x)^{3/2}}\\
  &=\frac{xe^{x/2}}{2(1+x)^{3/2}}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【2001-1-6 分】** 求

$$
 \int \frac{\arctan e^x}{e^{2x}}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 -\frac{\arctan e^x}{2e^{2x}}-\frac{1}{2e^x}
-\frac12\arctan e^x+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   t=e^x,\qquad dx=\frac{dt}{t},
$$

则

$$
   \int \frac{\arctan e^x}{e^{2x}}\,dx
=\int \frac{\arctan t}{t^3}\,dt.
$$

对右端作分部积分，取

$$
   u=\arctan t,\qquad dv=\frac{dt}{t^3},
$$

则

$$
   du=\frac{dt}{1+t^2},\qquad v=-\frac{1}{2t^2}.
$$

因而

$$
  \begin{aligned}
   \int \frac{\arctan t}{t^3}\,dt
  &=-\frac{\arctan t}{2t^2}
+\frac12\int\frac{dt}{t^2(1+t^2)}.
  \end{aligned}
$$

又

$$
   \frac{1}{t^2(1+t^2)}
=\frac1{t^2}-\frac1{1+t^2},
$$

所以

$$
  \begin{aligned}
   \int \frac{\arctan t}{t^3}\,dt
  &=-\frac{\arctan t}{2t^2}
+\frac12\left(-\frac1t-\arctan t\right)+C.
  \end{aligned}
$$

代回 $ t=e^x$，得

$$
   \int \frac{\arctan e^x}{e^{2x}}\,dx
=-\frac{\arctan e^x}{2e^{2x}}-\frac{1}{2e^x}
-\frac12\arctan e^x+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【2002-34-6 分】** 设

$$
 f(\sin^2x)=\frac{x}{\sin x},
$$

求

$$
 \int \frac{\sqrt{x}}{\sqrt{1-x}}f(x)\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 2\sqrt{x}-2\sqrt{1-x}\arcsin\sqrt{x}+C,
\qquad 0<x<1.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   u=\sin^2t,\qquad 0<t<\frac\pi2,
$$

则

$$
   t=\arcsin\sqrt{u},\qquad \sin t=\sqrt{u}.
$$

由题设

$$
   f(u)=\frac{t}{\sin t}
=\frac{\arcsin\sqrt{u}}{\sqrt{u}}.
$$

因此

$$
   f(x)=\frac{\arcsin\sqrt{x}}{\sqrt{x}},
$$

原积分化为

$$
   I=\int\frac{\arcsin\sqrt{x}}{\sqrt{1-x}}\,dx.
$$

再令

$$
   \sqrt{x}=\sin t,
$$

则

$$
   x=\sin^2t,\qquad dx=2\sin t\cos t\,dt,
  \qquad \sqrt{1-x}=\cos t.
$$

故

$$
  \begin{aligned}
   I
  &=2\int t\sin t\,dt\\
  &=2\left(-t\cos t+\sin t\right)+C.
  \end{aligned}
$$

代回

$$
   t=\arcsin\sqrt{x},\qquad
  \sin t=\sqrt{x},\qquad \cos t=\sqrt{1-x},
$$

得

$$
   I=2\sqrt{x}-2\sqrt{1-x}\arcsin\sqrt{x}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【2003-2-9 分】** 计算不定积分

$$
 \int \frac{xe^{\arctan x}}{(1+x^2)^{3/2}}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac{(x-1)e^{\arctan x}}{2\sqrt{1+x^2}}+C.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
   t=\arctan x,
$$

则

$$
   x=\tan t,\qquad dx=\sec^2t\,dt,
  \qquad \sqrt{1+x^2}=\sec t.
$$

因而

$$
  \begin{aligned}
   I
  &=\int\frac{\tan t\,e^t}{\sec^3t}\sec^2t\,dt\\
  &=\int e^t\sin t\,dt.
  \end{aligned}
$$

利用公式

$$
   \int e^t\sin t\,dt
=\frac12e^t(\sin t-\cos t)+C,
$$

且

$$
   \sin t=\frac{x}{\sqrt{1+x^2}},
  \qquad \cos t=\frac1{\sqrt{1+x^2}},
$$

所以

$$
   I
=\frac{(x-1)e^{\arctan x}}{2\sqrt{1+x^2}}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. **【2006-2-10 分】** 求

$$
 \int \frac{\arcsin e^x}{e^x}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 -e^{-x}\arcsin e^x
+\ln\left(\frac{e^x}{1+\sqrt{1-e^{2x}}}\right)+C,
\qquad x\le0.
$$
</ExamAnswer>

<ExamExplanation>
在实数范围内，需有 $ 0<e^x\le1$，即 $ x\le0$。

令

$$
   t=e^x,
$$

则

$$
   dx=\frac{dt}{t},
$$

从而

$$
   I=\int\frac{\arcsin t}{t^2}\,dt.
$$

分部积分，取

$$
   u=\arcsin t,
  \qquad dv=\frac{dt}{t^2},
$$

则

$$
   du=\frac{dt}{\sqrt{1-t^2}},
  \qquad v=-\frac1t.
$$

所以

$$
   I=-\frac{\arcsin t}{t}
+\int\frac{dt}{t\sqrt{1-t^2}}.
$$

对后一积分令 $ t=\sin\theta$，则

$$
   \int\frac{dt}{t\sqrt{1-t^2}}
=\int\csc\theta\,d\theta
=\ln\tan\frac\theta2+C.
$$

又

$$
   \tan\frac\theta2
=\frac{\sin\theta}{1+\cos\theta}
=\frac{t}{1+\sqrt{1-t^2}},
$$

因此

$$
   I=-\frac{\arcsin t}{t}
+\ln\left(\frac{t}{1+\sqrt{1-t^2}}\right)+C.
$$

代回 $ t=e^x$，即得

$$
   I=-e^{-x}\arcsin e^x
+\ln\left(\frac{e^x}{1+\sqrt{1-e^{2x}}}\right)+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. **【2009-23-10 分】** 计算不定积分

$$
 \int \ln\left(1+\sqrt{\frac{1+x}{x}}\right)\,dx,
\qquad x>0.
$$

<ExamSolution>

<ExamAnswer>
$$
\begin{aligned}
 &x\ln\left(1+\sqrt{\frac{1+x}{x}}\right)
+\frac12\ln\left(\sqrt{x}+\sqrt{1+x}\right)\\
&\qquad -\frac12\sqrt{x(1+x)}+\frac{x}{2}+C.
\end{aligned}
$$
</ExamAnswer>

<ExamExplanation>
由于 $ x>0$，有

$$
  \begin{aligned}
   \ln\left(1+\sqrt{\frac{1+x}{x}}\right)
  &=\ln\left(\frac{\sqrt{x}+\sqrt{1+x}}{\sqrt{x}}\right)\\
  &=\ln\left(\sqrt{x}+\sqrt{1+x}\right)-\frac12\ln x.
  \end{aligned}
$$

令

$$
   u=\sqrt{x},\qquad x=u^2,\qquad dx=2u\,du,
$$

则

$$
  \begin{aligned}
   I
  &=2\int u\ln\left(u+\sqrt{1+u^2}\right)du
-2\int u\ln u\,du.
  \end{aligned}
$$

先计算第一项。令

$$
   A=2\int u\ln\left(u+\sqrt{1+u^2}\right)du.
$$

分部积分，取

$$
   U=\ln\left(u+\sqrt{1+u^2}\right),
  \qquad dV=2u\,du,
$$

则

$$
   dU=\frac{du}{\sqrt{1+u^2}},
  \qquad V=u^2.
$$

所以

$$
  \begin{aligned}
   A
  &=u^2\ln\left(u+\sqrt{1+u^2}\right)
-\int\frac{u^2}{\sqrt{1+u^2}}\,du\\
  &=u^2\ln\left(u+\sqrt{1+u^2}\right)
-\frac12u\sqrt{1+u^2}
+\frac12\ln\left(u+\sqrt{1+u^2}\right).
  \end{aligned}
$$

其中用到了

$$
   \int\frac{u^2}{\sqrt{1+u^2}}\,du
=\frac12u\sqrt{1+u^2}
-\frac12\ln\left(u+\sqrt{1+u^2}\right).
$$

另一方面，

$$
   2\int u\ln u\,du
=u^2\ln u-\frac{u^2}{2}.
$$

因此

$$
  \begin{aligned}
   I
  &=u^2\ln\left(\frac{u+\sqrt{1+u^2}}{u}\right)
+\frac12\ln\left(u+\sqrt{1+u^2}\right)\\
  &\qquad -\frac12u\sqrt{1+u^2}+\frac{u^2}{2}+C.
  \end{aligned}
$$

代回 $ u=\sqrt{x}$，得

$$
  \begin{aligned}
   I
  &=x\ln\left(1+\sqrt{\frac{1+x}{x}}\right)
+\frac12\ln\left(\sqrt{x}+\sqrt{1+x}\right)\\
  &\qquad -\frac12\sqrt{x(1+x)}+\frac{x}{2}+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. **【2011-3-10 分】** 求不定积分

$$
 \int \frac{\arcsin \sqrt{x}+\ln x}{\sqrt{x}}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
\begin{aligned}
 \int \frac{\arcsin \sqrt{x}+\ln x}{\sqrt{x}}\,dx
&=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}\\
&\quad+2\sqrt{x}\ln x-4\sqrt{x}+C,
\qquad 0<x\le1.
\end{aligned}
$$
</ExamAnswer>

<ExamExplanation>
在实数范围内，由 $ \ln x$ 和 $ \arcsin\sqrt{x}$ 的定义可知

$$
   0<x\le1.
$$

令

$$
   t=\sqrt{x},
$$

则

$$
   x=t^2,\qquad dx=2t\,dt,
  \qquad \ln x=2\ln t.
$$

原积分化为

$$
  \begin{aligned}
   I
  &=\int\frac{\arcsin t+2\ln t}{t}\cdot2t\,dt\\
  &=2\int\arcsin t\,dt+4\int\ln t\,dt.
  \end{aligned}
$$

对第一项分部积分：

$$
  \begin{aligned}
   \int\arcsin t\,dt
  &=t\arcsin t-\int\frac{t}{\sqrt{1-t^2}}\,dt\\
  &=t\arcsin t+\sqrt{1-t^2}.
  \end{aligned}
$$

又

$$
   \int\ln t\,dt=t\ln t-t.
$$

因此

$$
  \begin{aligned}
   I
  &=2t\arcsin t+2\sqrt{1-t^2}
+4t\ln t-4t+C.
  \end{aligned}
$$

代回 $ t=\sqrt{x}$，并利用 $ \ln\sqrt{x}=\frac12\ln x$，得到

$$
  \begin{aligned}
   I
  &=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}\\
  &\quad+2\sqrt{x}\ln x-4\sqrt{x}+C.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. **【2018-12-10 分】** 求不定积分

$$
 \int e^{2x}\arctan\sqrt{e^x-1}\,dx.
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac12e^{2x}\arctan\sqrt{e^x-1}
-\frac{e^x+2}{6}\sqrt{e^x-1}+C,
\qquad x\ge0.
$$
</ExamAnswer>

<ExamExplanation>
在实数范围内，需有 $ e^x-1\ge0$，即 $ x\ge0$。

令

$$
   t=\sqrt{e^x-1},
$$

则

$$
   e^x=1+t^2,
  \qquad dx=\frac{2t}{1+t^2}\,dt,
  \qquad e^{2x}=(1+t^2)^2.
$$

因此

$$
  \begin{aligned}
   I
  &=\int(1+t^2)^2\arctan t\cdot\frac{2t}{1+t^2}\,dt\\
  &=\int2t(1+t^2)\arctan t\,dt.
  \end{aligned}
$$

分部积分，取

$$
   u=\arctan t,
  \qquad dv=2t(1+t^2)\,dt,
$$

则

$$
   du=\frac{dt}{1+t^2},
  \qquad v=t^2+\frac{t^4}{2}.
$$

从而

$$
  \begin{aligned}
   I
  &=\left(t^2+\frac{t^4}{2}\right)\arctan t
-\int\frac{t^2+\frac12t^4}{1+t^2}\,dt.
  \end{aligned}
$$

注意到

$$
  \begin{aligned}
   \frac{t^2+\frac12t^4}{1+t^2}
  &=\frac12\cdot\frac{t^4+2t^2}{1+t^2}\\
  &=\frac12\left(t^2+1-\frac1{1+t^2}\right),
  \end{aligned}
$$

故

$$
  \begin{aligned}
   I
  &=\left(t^2+\frac{t^4}{2}\right)\arctan t
-\frac{t^3}{6}-\frac t2+\frac12\arctan t+C\\
  &=\frac{(1+t^2)^2}{2}\arctan t
-\frac{t^3}{6}-\frac t2+C.
  \end{aligned}
$$

代回 $ t=\sqrt{e^x-1}$，有

$$
   \frac{(1+t^2)^2}{2}=\frac12e^{2x},
$$

且

$$
  \begin{aligned}
   \frac{t^3}{6}+\frac t2
  &=\frac{t(t^2+3)}{6}\\
  &=\frac{e^x+2}{6}\sqrt{e^x-1}.
  \end{aligned}
$$

所以

$$
   I=\frac12e^{2x}\arctan\sqrt{e^x-1}
-\frac{e^x+2}{6}\sqrt{e^x-1}+C.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>
