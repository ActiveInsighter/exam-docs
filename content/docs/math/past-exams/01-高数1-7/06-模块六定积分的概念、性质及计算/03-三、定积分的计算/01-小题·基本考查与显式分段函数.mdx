---
title: "小题·基本考查与显式分段函数"
sidebar_label: "小题·基本考查与显式分段函数"
sidebar_position: 1
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 小题·基本考查与显式分段函数


>1.无穷区间上的积分

>2.有限区间上的积分

## 小题

### （一）基本考查


<ExamQuestion>

1. **【1987-5-4 分】** 计算定积分
$$
 \int_{\frac12}^{1}e^{\sqrt{2x-1}}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ 1$。</ExamAnswer>

<ExamExplanation>
令
$$
   u=\sqrt{2x-1},
$$
则
$$
   x=\frac{u^2+1}{2},
  \qquad dx=u\,du.
$$
当 $ x=\frac12$ 时，$ u=0$；当 $ x=1$ 时，$ u=1$。因此
$$
  \begin{aligned}
   \int_{\frac12}^{1}e^{\sqrt{2x-1}}\,dx
  &=\int_0^1ue^u\,du\\
  &=\left.(u-1)e^u\right|_0^1\\
  &=1.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1987-123-3 分】** 设 $ f(x)$ 为已知连续函数，
$$
 I=t\int_0^{\frac{s}{t}}f(tx)\,dx,
\qquad s>0,
\quad t>0,
$$
则 $ I$ 的值（ ）。

<ExamChoices>
<ExamOption>A. 依赖于 $ s$ 和 $ t$</ExamOption>
<ExamOption>B. 依赖于 $ s,t,x$</ExamOption>
<ExamOption>C. 依赖于 $ t$ 和 $ x$，不依赖于 $ s$</ExamOption>
<ExamOption>D. 依赖于 $ s$，不依赖于 $ t$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
作变量代换
$$
   u=tx,
  \qquad dx=\frac{du}{t}.
$$
当 $ x=0$ 时，$ u=0$；当 $ x=\frac{s}{t}$ 时，$ u=s$。于是
$$
  \begin{aligned}
   I
  &=t\int_0^{\frac{s}{t}}f(tx)\,dx\\
  &=t\int_0^s f(u)\frac{du}{t}\\
  &=\int_0^s f(u)\,du.
  \end{aligned}
$$
因此 $ I$ 只依赖于上限 $ s$，与 $ t$ 无关。积分变量 $ x$ 是哑变量，不会影响积分值，故选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1988-3-4 分】**
$$
 \int_0^4e^{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ 2e^2+2$。</ExamAnswer>

<ExamExplanation>
令
$$
   u=\sqrt{x},
  \qquad x=u^2,
  \qquad dx=2u\,du.
$$
当 $ x=0$ 时，$ u=0$；当 $ x=4$ 时，$ u=2$。因此
$$
  \begin{aligned}
   \int_0^4e^{\sqrt{x}}\,dx
  &=2\int_0^2ue^u\,du\\
  &=2\left.(u-1)e^u\right|_0^2\\
  &=2(e^2+1)\\
  &=2e^2+2.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【1988-45-2 分】** 判断正误：等式
$$
 \int_0^a f(x)\,dx=-\int_0^a f(a-x)\,dx
$$
对任意连续函数 $ f(x)$、常数 $ a$ 都成立。

<ExamSolution>

<ExamAnswer>错误。</ExamAnswer>

<ExamExplanation>
在右侧积分中令
$$
   u=a-x,
  \qquad dx=-du.
$$
当 $ x=0$ 时，$ u=a$；当 $ x=a$ 时，$ u=0$。因此
$$
  \begin{aligned}
   \int_0^a f(a-x)\,dx
  &=\int_a^0f(u)(-du)\\
  &=\int_0^a f(u)\,du.
  \end{aligned}
$$
正确关系应为
$$
   \int_0^a f(x)\,dx=\int_0^a f(a-x)\,dx,
$$
而不是右侧带负号，故原命题错误。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【1989-3-3 分】**
$$
 \int_0^\pi t\sin t\,dt=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \pi$。</ExamAnswer>

<ExamExplanation>
分部积分，取
$$
   u=t,
  \qquad dv=\sin t\,dt,
$$
则
$$
   du=dt,
  \qquad v=-\cos t.
$$
所以
$$
  \begin{aligned}
   \int_0^\pi t\sin t\,dt
  &=\left.-t\cos t\right|_0^\pi+\int_0^\pi\cos t\,dt\\
  &=\pi+\left.\sin t\right|_0^\pi\\
  &=\pi.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【1989-123-3 分】** 设 $ f(x)$ 是连续函数，且
$$
 f(x)=x+2\int_0^1f(t)\,dt,
$$
则 $ f(x)=\underline{\qquad}$。

<ExamSolution>

<ExamAnswer>$ x-1$。</ExamAnswer>

<ExamExplanation>
设
$$
   A=\int_0^1f(t)\,dt.
$$
则题设可写为
$$
   f(x)=x+2A.
$$
两边在 $ [0,1]$ 上积分，得
$$
  \begin{aligned}
   A
  &=\int_0^1(x+2A)\,dx\\
  &=\frac12+2A.
  \end{aligned}
$$
因而
$$
   A=-\frac12.
$$
代回原式，得到
$$
   f(x)=x-1.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【1990-3-3 分】**
$$
 \int_0^1x\sqrt{1-x}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{4}{15}$。</ExamAnswer>

<ExamExplanation>
令
$$
   u=1-x,
  \qquad x=1-u,
  \qquad dx=-du.
$$
则
$$
  \begin{aligned}
   \int_0^1x\sqrt{1-x}\,dx
  &=\int_0^1(1-u)u^{\frac12}\,du\\
  &=\int_0^1\left(u^{\frac12}-u^{\frac32}\right)du\\
  &=\frac23-\frac25\\
  &=\frac{4}{15}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. **【1990-12-5 分】** 求
$$
 \int_0^1\frac{\ln(1+x)}{(2-x)^2}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac13\ln2$。</ExamAnswer>

<ExamExplanation>
分部积分，取
$$
   u=\ln(1+x),
  \qquad dv=\frac{dx}{(2-x)^2},
$$
则
$$
   du=\frac{dx}{1+x},
  \qquad v=\frac{1}{2-x}.
$$
因而
$$
  \begin{aligned}
   \int_0^1\frac{\ln(1+x)}{(2-x)^2}\,dx
  &=\left.\frac{\ln(1+x)}{2-x}\right|_0^1
-\int_0^1\frac{dx}{(1+x)(2-x)}\\
  &=\ln2-\int_0^1\frac{dx}{(1+x)(2-x)}.
  \end{aligned}
$$
作部分分式分解：
$$
   \frac{1}{(1+x)(2-x)}
=\frac13\left(\frac{1}{1+x}+\frac{1}{2-x}\right).
$$
所以
$$
  \begin{aligned}
   \int_0^1\frac{dx}{(1+x)(2-x)}
  &=\frac13\left[\ln(1+x)-\ln(2-x)\right]_0^1\\
  &=\frac23\ln2.
  \end{aligned}
$$
因此
$$
   \int_0^1\frac{\ln(1+x)}{(2-x)^2}\,dx
=\ln2-\frac23\ln2
=\frac13\ln2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. **【1991-3-5 分】** 计算
$$
 \int_{1}^{4}\frac{dx}{x(1+\sqrt{x})}.
$$

<ExamSolution>

<ExamAnswer>$ 2\ln\frac{4}{3}$。</ExamAnswer>

<ExamExplanation>
令
$$
   u=\sqrt{x},\qquad x=u^2,\qquad dx=2u\,du.
$$
当 $ x=1$ 时，$ u=1$；当 $ x=4$ 时，$ u=2$。于是
$$
  \begin{aligned}
   \int_{1}^{4}\frac{dx}{x(1+\sqrt{x})}
  &=\int_1^2\frac{2u}{u^2(1+u)}\,du\\
  &=2\int_1^2\frac{1}{u(1+u)}\,du\\
  &=2\int_1^2\left(\frac1u-\frac{1}{1+u}\right)du\\
  &=2\left[\ln u-\ln(1+u)\right]_1^2\\
  &=2\ln\frac{4}{3}.
  \end{aligned}
$$
本题的关键是利用 $ u=\sqrt{x}$ 消去根式，再作部分分式分解。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. **【1992-3-5 分】** 求
$$
 \int_{0}^{\pi}\sqrt{1-\sin x}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ 4\sqrt2-4$。</ExamAnswer>

<ExamExplanation>
由
$$
   1-\sin x=\left(\sin\frac{x}{2}-\cos\frac{x}{2}\right)^2,
$$
得
$$
   \sqrt{1-\sin x}
=\left|\sin\frac{x}{2}-\cos\frac{x}{2}\right|.
$$
在 $ 0\le x\le\frac\pi2$ 上，$ \sin\frac{x}{2}\le\cos\frac{x}{2}$；在 $ \frac\pi2\le x\le\pi$ 上，符号相反。因此
$$
  \begin{aligned}
   \int_0^\pi\sqrt{1-\sin x}\,dx
  &=\int_0^{\frac\pi2}\left(\cos\frac{x}{2}-\sin\frac{x}{2}\right)dx
+\int_{\frac\pi2}^{\pi}\left(\sin\frac{x}{2}-\cos\frac{x}{2}\right)dx\\
  &=(2\sqrt2-2)+(2\sqrt2-2)\\
  &=4\sqrt2-4.
  \end{aligned}
$$
易错点是直接去掉平方根外的绝对值；必须在 $ x=\frac\pi2$ 处分段。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. **【1993-3-5 分】**
$$
 \int_{0}^{\frac{\pi}{4}}\frac{x}{1+\cos 2x}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac\pi8-\frac14\ln2$。</ExamAnswer>

<ExamExplanation>
利用恒等式
$$
   1+\cos2x=2\cos^2x,
$$
原积分化为
$$
   I=\frac12\int_0^{\frac\pi4}x\sec^2x\,dx.
$$
分部积分，取 $ u=x$，$ dv=\sec^2x\,dx$，则 $ du=dx$，$ v=\tan x$，所以
$$
  \begin{aligned}
   I
  &=\frac12\left[x\tan x\right]_0^{\frac\pi4}
-\frac12\int_0^{\frac\pi4}\tan x\,dx\\
  &=\frac\pi8-\frac12\left[-\ln(\cos x)\right]_0^{\frac\pi4}\\
  &=\frac\pi8-\frac14\ln2.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

12. **【1994-3-5 分】** 计算
$$
 \int_{0}^{1}x(1-x^4)^{\frac32}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac{3\pi}{32}$。</ExamAnswer>

<ExamExplanation>
令 $ u=x^2$，则 $ du=2x\,dx$，于是
$$
   \int_0^1x(1-x^4)^{\frac32}dx
=\frac12\int_0^1(1-u^2)^{\frac32}du.
$$
再令 $ u=\sin\theta$，$ 0\le\theta\le\frac\pi2$，则
$$
  \begin{aligned}
   \frac12\int_0^1(1-u^2)^{\frac32}du
  &=\frac12\int_0^{\frac\pi2}\cos^4\theta\,d\theta\\
  &=\frac12\cdot\frac{3\pi}{16}\\
  &=\frac{3\pi}{32}.
  \end{aligned}
$$
其中使用了
$$
   \int_0^{\frac\pi2}\cos^4\theta\,d\theta=\frac{3\pi}{16}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

13. **【1996-3-5 分】** 计算
$$
 \int_{0}^{\ln2}\sqrt{1-e^{-2x}}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \ln(2+\sqrt3)-\frac{\sqrt3}{2}$。</ExamAnswer>

<ExamExplanation>
令 $ t=e^{-x}$，则 $ dx=-\frac{dt}{t}$。当 $ x=0$ 时 $ t=1$，当 $ x=\ln2$ 时 $ t=\frac12$，故
$$
   I=\int_{\frac12}^1\frac{\sqrt{1-t^2}}{t}\,dt.
$$
再令 $ t=\sin\theta$，则 $ \theta\in\left[\frac\pi6,\frac\pi2\right]$，从而
$$
  \begin{aligned}
   I
  &=\int_{\frac\pi6}^{\frac\pi2}\frac{\cos^2\theta}{\sin\theta}\,d\theta\\
  &=\int_{\frac\pi6}^{\frac\pi2}(\csc\theta-\sin\theta)\,d\theta\\
  &=\left[\ln\tan\frac\theta2+\cos\theta\right]_{\frac\pi6}^{\frac\pi2}\\
  &=-\ln\tan\frac\pi{12}-\frac{\sqrt3}{2}.
  \end{aligned}
$$
因为 $ \tan\frac\pi{12}=2-\sqrt3$，且 $ \frac1{2-\sqrt3}=2+\sqrt3$，所以
$$
   I=\ln(2+\sqrt3)-\frac{\sqrt3}{2}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

14. **【1997-4-3 分】** 若
$$
 f(x)=\frac{1}{1+x^2}+x^3\int_0^1f(x)\,dx,
$$
则
$$
 \int_0^1f(x)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac\pi3$。</ExamAnswer>

<ExamExplanation>
记
$$
   A=\int_0^1f(x)\,dx.
$$
则题设为
$$
   f(x)=\frac{1}{1+x^2}+Ax^3.
$$
两边在 $ [0,1]$ 上积分，得
$$
  \begin{aligned}
   A
  &=\int_0^1\frac{dx}{1+x^2}+A\int_0^1x^3dx\\
  &=\frac\pi4+\frac A4.
  \end{aligned}
$$
因此
$$
   \frac34A=\frac\pi4,
  \qquad A=\frac\pi3.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

15. **【1997-3-3 分】** 若
$$
 f(x)=\frac{1}{1+x^2}+\sqrt{1-x^2}\int_0^1f(x)\,dx,
$$
则
$$
 \int_0^1f(x)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\pi}{4-\pi}$。</ExamAnswer>

<ExamExplanation>
记
$$
   A=\int_0^1f(x)\,dx.
$$
由题设
$$
   f(x)=\frac{1}{1+x^2}+A\sqrt{1-x^2}.
$$
两边在 $ [0,1]$ 上积分，得
$$
  \begin{aligned}
   A
  &=\int_0^1\frac{dx}{1+x^2}+A\int_0^1\sqrt{1-x^2}\,dx\\
  &=\frac\pi4+A\cdot\frac\pi4.
  \end{aligned}
$$
其中 $ \int_0^1\sqrt{1-x^2}\,dx=\frac\pi4$，它表示单位圆第一象限的面积。于是
$$
   A\left(1-\frac\pi4\right)=\frac\pi4,
$$
所以
$$
   A=\frac{\pi}{4-\pi}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

16. **【1999-2-3 分】** 函数
$$
 y=\frac{x^2}{\sqrt{1-x^2}}
$$
在区间 $ \left[\frac12,\frac{\sqrt3}{2}\right]$ 上的平均值为 $\underline{\qquad}$。

<ExamSolution>

<ExamAnswer>$ \frac{\pi(\sqrt3+1)}{12}$。</ExamAnswer>

<ExamExplanation>
函数 $ f(x)$ 在 $ [a,b]$ 上的平均值为
$$
   \overline f=\frac{1}{b-a}\int_a^bf(x)\,dx.
$$
令 $ x=\sin\theta$，则 $ dx=\cos\theta\,d\theta$。当 $ x=\frac12$ 时 $ \theta=\frac\pi6$；当 $ x=\frac{\sqrt3}{2}$ 时 $ \theta=\frac\pi3$。于是
$$
  \begin{aligned}
   \int_{\frac12}^{\frac{\sqrt3}{2}}\frac{x^2}{\sqrt{1-x^2}}\,dx
  &=\int_{\frac\pi6}^{\frac\pi3}\sin^2\theta\,d\theta\\
  &=\left[\frac\theta2-\frac{\sin2\theta}{4}\right]_{\frac\pi6}^{\frac\pi3}\\
  &=\frac\pi{12}.
  \end{aligned}
$$
区间长度为
$$
   \frac{\sqrt3}{2}-\frac12=\frac{\sqrt3-1}{2}.
$$
因此平均值为
$$
  \begin{aligned}
   \overline f
  &=\frac{\frac\pi{12}}{\frac{\sqrt3-1}{2}}\\
  &=\frac{\pi}{6(\sqrt3-1)}\\
  &=\frac{\pi(\sqrt3+1)}{12}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

17. **【2024-2-5 分】** 某物体以速度
$$
 v(t)=t+k\sin\pi t
$$
做直线运动，若它从 $ t=0$ 到 $ t=3$ 的时间段内平均速度是 $ \frac52$，则 $ k=\underline{\qquad}$。

<ExamSolution>

<ExamAnswer>$ \frac{3\pi}{2}$。</ExamAnswer>

<ExamExplanation>
在时间区间 $ [0,3]$ 上，平均速度为
$$
   \overline v=\frac13\int_0^3v(t)\,dt.
$$
因而
$$
  \begin{aligned}
   \frac52
  &=\frac13\int_0^3(t+k\sin\pi t)\,dt\\
  &=\frac13\left(\frac92+k\int_0^3\sin\pi t\,dt\right).
  \end{aligned}
$$
又
$$
   \int_0^3\sin\pi t\,dt
=\left[-\frac{\cos\pi t}{\pi}\right]_0^3
=\frac2\pi.
$$
所以
$$
   \frac52=\frac32+\frac{2k}{3\pi}.
$$
解得
$$
   k=\frac{3\pi}{2}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

18. **【2000-1-3 分】**
$$
 \int_0^1\sqrt{2x-x^2}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac\pi4$。</ExamAnswer>

<ExamExplanation>
配方得
$$
   2x-x^2=1-(x-1)^2.
$$
因而
$$
   \int_0^1\sqrt{2x-x^2}\,dx
=\int_0^1\sqrt{1-(x-1)^2}\,dx.
$$
曲线
$$
   y=\sqrt{1-(x-1)^2}
$$
是圆 $ (x-1)^2+y^2=1$ 的上半圆。在 $ 0\le x\le1$ 上所对应的图形为半径为 $ 1$ 的四分之一圆，因此
$$
   \int_0^1\sqrt{2x-x^2}\,dx=\frac14\pi\cdot1^2=\frac\pi4.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

19. **【2007-1-4 分】**
$$
 \int_{1}^{2}\frac{1}{x^{3}}e^{\frac1x}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\sqrt e}{2}$。</ExamAnswer>

<ExamExplanation>
令 $ u=\frac1x$，则
$$
   x=\frac1u,\qquad dx=-\frac1{u^2}\,du.
$$
因而
$$
   \frac1{x^3}\,dx=u^3\left(-\frac1{u^2}\,du\right)=-u\,du.
$$
当 $ x=1$ 时，$ u=1$；当 $ x=2$ 时，$ u=\frac12$。于是
$$
  \begin{aligned}
   \int_1^2\frac{1}{x^3}e^{\frac1x}\,dx
  &=\int_1^{\frac12}-ue^u\,du\\
  &=\int_{\frac12}^1ue^u\,du\\
  &=\left[(u-1)e^u\right]_{\frac12}^1\\
  &=\frac{\sqrt e}{2}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

20. **【2008-3-4 分】** 设
$$
 f\left(x+\frac1x\right)=\frac{x+x^3}{1+x^4},
$$
则
$$
 \int_2^{2\sqrt2}f(x)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac12\ln3$。</ExamAnswer>

<ExamExplanation>
令
$$
   u=x+\frac1x,\qquad x\ge1.
$$
则
$$
   du=\left(1-\frac1{x^2}\right)dx=\frac{x^2-1}{x^2}\,dx.
$$
当 $ u=2$ 时，$ x=1$；当 $ u=2\sqrt2$ 时，由
$$
   x+\frac1x=2\sqrt2
$$
且 $ x\ge1$，得 $ x=1+\sqrt2$。因此
$$
  \begin{aligned}
   \int_2^{2\sqrt2}f(u)\,du
  &=\int_1^{1+\sqrt2}\frac{x+x^3}{1+x^4}\cdot\frac{x^2-1}{x^2}\,dx\\
  &=\int_1^{1+\sqrt2}\frac{x^4-1}{x(x^4+1)}\,dx.
  \end{aligned}
$$
注意到
$$
   \frac{x^4-1}{x(x^4+1)}
=\frac12\left(\ln\frac{x^4+1}{x^2}\right)',
$$
故
$$
  \begin{aligned}
   \int_2^{2\sqrt2}f(u)\,du
  &=\frac12\left[\ln\frac{x^4+1}{x^2}\right]_1^{1+\sqrt2}\\
  &=\frac12\ln\frac{(1+\sqrt2)^2+(1+\sqrt2)^{-2}}{2}.
  \end{aligned}
$$
又因为
$$
   (1+\sqrt2)+\frac1{1+\sqrt2}=2\sqrt2,
$$
所以
$$
   (1+\sqrt2)^2+(1+\sqrt2)^{-2}=(2\sqrt2)^2-2=6.
$$
因此
$$
   \int_2^{2\sqrt2}f(x)\,dx=\frac12\ln\frac62=\frac12\ln3.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

21. **【2009-2-4 分】**
$$
 \lim_{n\to\infty}\int_0^1e^{-x}\sin nx\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ 0$。</ExamAnswer>

<ExamExplanation>
分部积分，取
$$
   u=e^{-x},\qquad dv=\sin nx\,dx,
$$
则
$$
   du=-e^{-x}\,dx,\qquad v=-\frac{\cos nx}{n}.
$$
因而
$$
  \begin{aligned}
   \int_0^1e^{-x}\sin nx\,dx
  &=-\left.\frac{e^{-x}\cos nx}{n}\right|_0^1
-\frac1n\int_0^1e^{-x}\cos nx\,dx.
  \end{aligned}
$$
于是
$$
  \begin{aligned}
   \left|\int_0^1e^{-x}\sin nx\,dx\right|
  &\le\frac{1+e^{-1}}n+\frac1n\int_0^1e^{-x}\,dx\\
  &\le\frac3n\to0.
  \end{aligned}
$$
由夹逼定理，所求极限为 $ 0$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

22. **【2010-1-4 分】**
$$
 \int_0^{\pi^2}\sqrt{x}\cos\sqrt{x}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ -4\pi$。</ExamAnswer>

<ExamExplanation>
令 $ t=\sqrt{x}$，则 $ x=t^2$，$ dx=2t\,dt$。积分上下限由 $ x=0,\pi^2$ 变为 $ t=0,\pi$，故
$$
   \int_0^{\pi^2}\sqrt{x}\cos\sqrt{x}\,dx
=2\int_0^\pi t^2\cos t\,dt.
$$
连续分部积分可得
$$
   \int t^2\cos t\,dt=t^2\sin t+2t\cos t-2\sin t+C.
$$
因此
$$
  \begin{aligned}
   2\int_0^\pi t^2\cos t\,dt
  &=2\left[t^2\sin t+2t\cos t-2\sin t\right]_0^\pi\\
  &=2(-2\pi)\\
  &=-4\pi.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

23. **【2012-1-4 分】**
$$
 \int_0^2x\sqrt{2x-x^2}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac\pi2$。</ExamAnswer>

<ExamExplanation>
配方得
$$
   2x-x^2=1-(x-1)^2.
$$
令 $ t=x-1$，则
$$
  \begin{aligned}
   \int_0^2x\sqrt{2x-x^2}\,dx
  &=\int_{-1}^1(t+1)\sqrt{1-t^2}\,dt\\
  &=\int_{-1}^1t\sqrt{1-t^2}\,dt+
  \int_{-1}^1\sqrt{1-t^2}\,dt.
  \end{aligned}
$$
第一项的被积函数为奇函数，在对称区间上的积分为 $ 0$；第二项表示单位圆上半圆的面积，等于 $ \frac\pi2$。故
$$
   \int_0^2x\sqrt{2x-x^2}\,dx=\frac\pi2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

24. **【2014-3-4 分】** 设
$$
 \int_0^axe^{2x}\,dx=\frac14,
$$
则 $ a=\underline{\qquad}$。

<ExamSolution>

<ExamAnswer>$ \frac12$。</ExamAnswer>

<ExamExplanation>
分部积分得
$$
   \int xe^{2x}\,dx=\frac{2x-1}{4}e^{2x}+C.
$$
因此
$$
  \begin{aligned}
   \int_0^axe^{2x}\,dx
  &=\left[\frac{2x-1}{4}e^{2x}\right]_0^a\\
  &=\frac{(2a-1)e^{2a}+1}{4}.
  \end{aligned}
$$
由题意
$$
   \frac{(2a-1)e^{2a}+1}{4}=\frac14,
$$
从而
$$
   (2a-1)e^{2a}=0.
$$
因为 $ e^{2a}>0$，所以
$$
   a=\frac12.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

25. **【2020-2-4 分】**
$$
 \int_0^1\frac{\arcsin\sqrt{x}}{\sqrt{x(1-x)}}\,dx=(\quad)
$$

<ExamChoices>
<ExamOption>A. $ \frac{\pi^2}{4}$</ExamOption>
<ExamOption>B. $ \frac{\pi^2}{8}$</ExamOption>
<ExamOption>C. $ \frac\pi4$</ExamOption>
<ExamOption>D. $ \frac\pi8$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>A。</ExamAnswer>

<ExamExplanation>
令
$$
   \sqrt{x}=\sin t,
$$
则
$$
   x=\sin^2t,
  \qquad dx=2\sin t\cos t\,dt.
$$
当 $ x$ 从 $ 0$ 变到 $ 1$ 时，$ t$ 从 $ 0$ 变到 $ \frac\pi2$。并且
$$
   \arcsin\sqrt{x}=t,
  \qquad \sqrt{x(1-x)}=\sin t\cos t.
$$
因此
$$
  \begin{aligned}
   \int_0^1\frac{\arcsin\sqrt{x}}{\sqrt{x(1-x)}}\,dx
  &=\int_0^{\frac\pi2}\frac{t}{\sin t\cos t}\cdot2\sin t\cos t\,dt\\
  &=2\int_0^{\frac\pi2}t\,dt\\
  &=\left[t^2\right]_0^{\frac\pi2}\\
  &=\frac{\pi^2}{4}.
  \end{aligned}
$$
故选 A。其余选项均与换元后的积分值不符。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

26. **【2022-1-5 分】**
$$
 \int_1^{e^2}\frac{\ln x}{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ 4$。</ExamAnswer>

<ExamExplanation>
令 $ x=t^2$，则 $ dx=2t\,dt$，$ \sqrt{x}=t$。积分上下限由 $ x=1,e^2$ 变为 $ t=1,e$。于是
$$
  \begin{aligned}
   \int_1^{e^2}\frac{\ln x}{\sqrt{x}}\,dx
  &=\int_1^e\frac{\ln t^2}{t}\cdot2t\,dt\\
  &=4\int_1^e\ln t\,dt\\
  &=4\left[t\ln t-t\right]_1^e\\
  &=4.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

27. **【2022-2-5 分】**
$$
 \int_0^1\frac{2x+3}{x^2-x+1}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{8\pi}{3\sqrt3}$。</ExamAnswer>

<ExamExplanation>
将分子拆成分母导数与常数项：
$$
   2x+3=(2x-1)+4.
$$
因此
$$
  \begin{aligned}
   \int_0^1\frac{2x+3}{x^2-x+1}\,dx
  &=\int_0^1\frac{2x-1}{x^2-x+1}\,dx
+4\int_0^1\frac{dx}{x^2-x+1}.
  \end{aligned}
$$
第一项为
$$
   \left[\ln(x^2-x+1)\right]_0^1=0.
$$
又
$$
   x^2-x+1=\left(x-\frac12\right)^2+\frac34,
$$
故
$$
  \begin{aligned}
   4\int_0^1\frac{dx}{x^2-x+1}
  &=\frac8{\sqrt3}
  \left[\arctan\frac{2x-1}{\sqrt3}\right]_0^1\\
  &=\frac8{\sqrt3}\left(\frac\pi6+\frac\pi6\right)\\
  &=\frac{8\pi}{3\sqrt3}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

28. **【2022-3-5 分】**
$$
 \int_0^2\frac{2x-4}{x^2+2x+4}\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \ln3-\frac\pi{\sqrt3}$。</ExamAnswer>

<ExamExplanation>
将分子改写为
$$
   2x-4=(2x+2)-6.
$$
因此
$$
  \begin{aligned}
   \int_0^2\frac{2x-4}{x^2+2x+4}\,dx
  &=\int_0^2\frac{2x+2}{x^2+2x+4}\,dx
-6\int_0^2\frac{dx}{x^2+2x+4}\\
  &=\left[\ln(x^2+2x+4)\right]_0^2
-6\int_0^2\frac{dx}{(x+1)^2+3}.
  \end{aligned}
$$
第一项为
$$
   \ln12-\ln4=\ln3.
$$
第二项为
$$
  \begin{aligned}
   6\int_0^2\frac{dx}{(x+1)^2+3}
  &=2\sqrt3\left[\arctan\frac{x+1}{\sqrt3}\right]_0^2\\
  &=2\sqrt3\left(\frac\pi3-\frac\pi6\right)\\
  &=\frac\pi{\sqrt3}.
  \end{aligned}
$$
所以
$$
   \int_0^2\frac{2x-4}{x^2+2x+4}\,dx
=\ln3-\frac\pi{\sqrt3}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

29. **【2023-12-5 分】** 设连续函数 $ f(x)$ 满足：$ f(x+2)-f(x)=x$，$ \int_0^2 f(x)\,dx=0$，则
$$
 \int_1^3 f(x)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ \frac12$。</ExamAnswer>

<ExamExplanation>
将积分在 $ x=2$ 处分开：
$$
   \int_1^3 f(x)\,dx
=\int_1^2 f(x)\,dx+\int_2^3 f(x)\,dx.
$$
对第二项令 $ x=t+2$，则
$$
  \begin{aligned}
   \int_2^3 f(x)\,dx
  &=\int_0^1 f(t+2)\,dt\\
  &=\int_0^1[f(t)+t] \,dt.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
   \int_1^3 f(x)\,dx
  &=\int_1^2 f(x)\,dx+\int_0^1 f(x)\,dx+\int_0^1x\,dx\\
  &=\int_0^2f(x)\,dx+\frac12\\
  &=\frac12.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

30. **【2025-123-5 分】** 计算
$$
 \int_0^1\frac{1}{(x+1)(x^2-2x+2)}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\pi}{10}+\frac{3}{10}\ln2$。</ExamAnswer>

<ExamExplanation>
作部分分式分解：
$$
  \begin{aligned}
   \frac{1}{(x+1)(x^2-2x+2)}
  &=\frac{A}{x+1}+\frac{Bx+C}{x^2-2x+2}.
  \end{aligned}
$$
比较系数得
$$
   A=\frac15,\qquad B=-\frac15,\qquad C=\frac35.
$$
故
$$
  \begin{aligned}
   \frac{1}{(x+1)(x^2-2x+2)}
  &=\frac1{5(x+1)}+\frac{-x+3}{5[(x-1)^2+1]}\\
  &=\frac1{5(x+1)}-\frac{2x-2}{10[(x-1)^2+1]}
+\frac{2}{5[(x-1)^2+1]}.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
   \int_0^1\frac{dx}{(x+1)(x^2-2x+2)}
  &=\frac15\left[\ln(x+1)-\frac12\ln((x-1)^2+1)
+2\arctan(x-1)\right]_0^1\\
  &=\frac15\left(\frac32\ln2+\frac\pi2\right)\\
  &=\frac{3}{10}\ln2+\frac\pi{10}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

### （二）显式分段函数


<ExamQuestion>

1. **【1991-3-3 分】** 设函数
$$

f(x)=
\begin{cases}
x^2,&0\le x\le1,\\
2-x,&1<x\le2,
\end{cases}
\qquad
F(x)=\int_0^x f(t)\,dt,\quad 0\le x\le2,
$$
则（ ）。
$$
\begin{aligned}
&A.\quad F(x)=
\begin{cases}
 \frac{x^3}{3},&0\le x\le1,\\
 \frac13+2x-\frac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&B.\quad F(x)=
\begin{cases}
 \frac{x^3}{3},&0\le x\le1,\\
 -\frac76+2x-\frac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&C.\quad F(x)=
\begin{cases}
 \frac{x^3}{3},&0\le x\le1,\\
 \frac{x^3}{3}+2x-\frac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&D.\quad F(x)=
\begin{cases}
 \frac{x^3}{3},&0\le x\le1,\\
 2x-\frac{x^2}{2},&1<x\le2.
\end{cases}
\end{aligned}
$$

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
当 $ 0\le x\le1$ 时，
$$
   F(x)=\int_0^x t^2\,dt=\frac{x^3}{3}.
$$
当 $ 1<x\le2$ 时，积分区间跨过分段点 $ t=1$，故
$$
  \begin{aligned}
   F(x)
  &=\int_0^1t^2\,dt+\int_1^x(2-t)\,dt\\
  &=\frac13+\left[2t-\frac{t^2}{2}\right]_1^x\\
  &=-\frac76+2x-\frac{x^2}{2}.
  \end{aligned}
$$
因此选 B。A 未扣除第二段积分在下限 $ t=1$ 处的值；C、D 均未正确处理跨分段点积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1992-13-5 分】** 设
$$

f(x)=
\begin{cases}
1+x^2,&x\le0,\\
e^{-x},&x>0,
\end{cases}
$$
求
$$
 \int_1^3f(x-2)\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac73-\frac1e$。</ExamAnswer>

<ExamExplanation>
令 $ t=x-2$，则 $ dx=dt$，上下限由 $ x=1,3$ 变为 $ t=-1,1$。由于被积函数在 $ t=0$ 处分段，故
$$
  \begin{aligned}
   \int_1^3f(x-2)\,dx
  &=\int_{-1}^1f(t)\,dt\\
  &=\int_{-1}^0(1+t^2)\,dt+\int_0^1e^{-t}\,dt\\
  &=\left[t+\frac{t^3}{3}\right]_{-1}^0+\left[-e^{-t}\right]_0^1\\
  &=\frac43+1-\frac1e\\
  &=\frac73-\frac1e.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1993-3-3 分】** 已知
$$

f(x)=
\begin{cases}
x^2,&0\le x<1,\\
1,&1\le x\le2,
\end{cases}
\qquad
F(x)=\int_1^x f(t)\,dt\quad(0\le x\le2),
$$
则 $ F(x)$ 为（ ）。
$$
\begin{aligned}
&A.\quad
\begin{cases}
 \frac{x^3}{3},&0\le x<1,\\
x,&1\le x\le2;
\end{cases}\\[2mm]
&B.\quad
\begin{cases}
 \frac{x^3}{3}-\frac13,&0\le x<1,\\
x,&1\le x\le2;
\end{cases}\\[2mm]
&C.\quad
\begin{cases}
 \frac{x^3}{3},&0\le x<1,\\
x-1,&1\le x\le2;
\end{cases}\\[2mm]
&D.\quad
\begin{cases}
 \frac{x^3}{3}-\frac13,&0\le x<1,\\
x-1,&1\le x\le2.
\end{cases}
\end{aligned}
$$

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
当 $ 0\le x<1$ 时，积分上限小于下限，故
$$
  \begin{aligned}
   F(x)
  &=\int_1^x t^2\,dt
=-\int_x^1t^2\,dt
=\frac{x^3}{3}-\frac13.
  \end{aligned}
$$
当 $ 1\le x\le2$ 时，
$$
   F(x)=\int_1^x1\,dt=x-1.
$$
因此选 D。A、C 忽略了第一段积分的下限为 $ 1$；A、B 又错误地把第二段写成 $ x$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【2004-34-4 分】** 设
$$

f(x)=
\begin{cases}
xe^{x^2},&-\frac12\le x<\frac12,\\
-1,&x\ge\frac12,
\end{cases}
$$
则
$$
 \int_{\frac12}^{2}f(x-1)\,dx=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>$ -\frac12$。</ExamAnswer>

<ExamExplanation>
令 $ t=x-1$，则积分区间变为 $ \left[-\frac12,1\right]$，并在 $ t=\frac12$ 处分段：
$$
  \begin{aligned}
   \int_{\frac12}^{2}f(x-1)\,dx
  &=\int_{-\frac12}^{\frac12}te^{t^2}\,dt+\int_{\frac12}^{1}(-1)\,dt.
  \end{aligned}
$$
第一项的被积函数 $ te^{t^2}$ 为奇函数，在对称区间上的积分为 $ 0$；第二项为
$$
   -\left(1-\frac12\right)=-\frac12.
$$
故原积分为 $ -\frac12$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>
