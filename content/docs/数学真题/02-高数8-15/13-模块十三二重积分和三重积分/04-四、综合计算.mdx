---
title: "四、综合计算"
sidebar_position: 4
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 四、综合计算

## 大题

<ExamQuestion>

1. **【2003-1-12 分】** 设

$$
 F(t)=\frac{\iiint_{\Omega(t)}f(x^2+y^2+z^2)\,dV}{\iint_{D(t)}f(x^2+y^2)\,d\sigma},
$$

$$
 G(t)=\frac{\iint_{D(t)}f(x^2+y^2)\,d\sigma}{\int_{-t}^{t}f(x^2)\,dx},
$$

其中

$$
 \Omega(t):x^2+y^2+z^2\le t^2,
\qquad
D(t):x^2+y^2\le t^2,
$$

$f$ 为正的连续函数。

（1）讨论 $F(t)$ 在 $(0,+\infty)$ 上的单调性；

（2）证明：当 $t>0$ 时，

$$
 F(t)>\frac2\pi G(t).
$$

<ExamSolution>

<ExamAnswer>
（1）$ F(t)$ 在 $(0,+\infty)$ 上严格单调增加；


（2）$ F(t)>\frac2\pi G(t)$。
</ExamAnswer>

<ExamExplanation>
令

$$
 A(t)=\int_0^tr^2f(r^2)\,dr,
\qquad
B(t)=\int_0^trf(r^2)\,dr,
\qquad
C(t)=\int_0^tf(r^2)\,dr.
$$

利用球坐标和极坐标，分别有

$$
 \iiint_{\Omega(t)}f(x^2+y^2+z^2)\,dV
=4\pi A(t),
$$

$$
 \iint_{D(t)}f(x^2+y^2)\,d\sigma
=2\pi B(t),
$$

以及

$$
 \int_{-t}^{t}f(x^2)\,dx=2C(t).
$$

因此

$$
 F(t)=\frac{2A(t)}{B(t)},
\qquad
G(t)=\frac{\pi B(t)}{C(t)}.
$$

（1）由微积分基本定理，

$$
 A'(t)=t^2f(t^2),
\qquad
B'(t)=tf(t^2).
$$

于是

$$
\begin{aligned}
 F'(t)
&=2\frac{A'(t)B(t)-A(t)B'(t)}{B^2(t)}\\
&=\frac{2tf(t^2)}{B^2(t)}\bigl[tB(t)-A(t)\bigr].
\end{aligned}
$$

而

$$
\begin{aligned}
 tB(t)-A(t)
&=\int_0^t(tr-r^2)f(r^2)\,dr\\
&=\int_0^tr(t-r)f(r^2)\,dr>0,
\end{aligned}
$$

其中 $t>0$ 且 $f>0$。因此

$$
 F'(t)>0,
$$

故 $F(t)$ 在 $(0,+\infty)$ 上严格单调增加。

（2）由柯西—施瓦茨不等式，

$$
\begin{aligned}
 B^2(t)
&=\left[\int_0^trf(r^2)\,dr\right]^2\\
&=\left[\int_0^t\sqrt{f(r^2)}\cdot r\sqrt{f(r^2)}\,dr\right]^2\\
&\le\left[\int_0^tf(r^2)\,dr\right]
\left[\int_0^tr^2f(r^2)\,dr\right]\\
&=A(t)C(t).
\end{aligned}
$$

由于 $r$ 在区间 $[0,t]$ 上不是常数，且 $f>0$，等号条件不能成立，故实际上

$$
 B^2(t)<A(t)C(t).
$$

于是

$$
 \frac{A(t)}{B(t)}>\frac{B(t)}{C(t)}.
$$

两边乘以 $2$，并代回 $F(t),G(t)$，得

$$
 F(t)=\frac{2A(t)}{B(t)}
 {}>\frac{2B(t)}{C(t)}
=\frac2\pi G(t).
$$

证毕。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【2011-12-11 分】** 设

$$
 D=\bigl\{(x,y)\mid0\le x\le1,\ 0\le y\le1\bigr\},
$$

$f_{xy}''(x,y)$ 连续，且

$$
 f(1,y)=0,
\qquad
f(x,1)=0,
$$

又已知

$$
 \iint_Df(x,y)\,dx\,dy=a.
$$

求

$$
 I=\iint_Dxyf_{xy}''(x,y)\,dx\,dy.
$$

<ExamSolution>

<ExamAnswer>$ I=a$。</ExamAnswer>

<ExamExplanation>
先对 $x$ 积分。对固定的 $y$，分部积分得

$$
\begin{aligned}
 \int_0^1x f_{xy}''(x,y)\,dx
&=\left.xf_y'(x,y)\right|_0^1-\int_0^1f_y'(x,y)\,dx\\
&=f_y'(1,y)-\int_0^1f_y'(x,y)\,dx.
\end{aligned}
$$

由 $f(1,y)=0$ 对一切 $y\in[0,1]$ 成立，故

$$
 f_y'(1,y)=0.
$$

于是

$$
 \int_0^1x f_{xy}''(x,y)\,dx
=-\frac d{dy}\int_0^1f(x,y)\,dx.
$$

令

$$
 H(y)=\int_0^1f(x,y)\,dx,
$$

则

$$
 I=-\int_0^1yH'(y)\,dy.
$$

再次分部积分，得

$$
\begin{aligned}
 I
&=-\left.yH(y)\right|_0^1+\int_0^1H(y)\,dy.
\end{aligned}
$$

由 $f(x,1)=0$，有

$$
 H(1)=\int_0^1f(x,1)\,dx=0,
$$

故边界项为零。因此

$$
\begin{aligned}
 I
&=\int_0^1\int_0^1f(x,y)\,dx\,dy\\
&=\iint_Df(x,y)\,dx\,dy\\
&=a.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【2018-2-10 分】** 设 $D$ 是摆线

$$

\begin{cases}
x=t-\sin t,\\
y=1-\cos t,
\end{cases}
\qquad 0\le t\le2\pi
$$

与 $x$ 轴围成的区域，求

$$
 \iint_D(x+2y)\,dx\,dy.
$$

<ExamSolution>

<ExamAnswer>$ 3\pi^2+5\pi$。</ExamAnswer>

<ExamExplanation>
记摆线弧对应的纵坐标为 $y=y(x)$。当参数 $t$ 从 $0$ 变到 $2\pi$ 时，

$$
 \frac{dx}{dt}=1-\cos t=y\ge0,
$$

故该摆线弧可看成 $x$ 的单值函数，且区域 $D$ 位于摆线弧与 $x$ 轴之间。

先计算区域面积：

$$
\begin{aligned}
 S_D
&=\int_0^{2\pi}y\,dx\\
&=\int_0^{2\pi}(1-\cos t)(1-\cos t)\,dt\\
&=\int_0^{2\pi}(1-\cos t)^2\,dt\\
&=\int_0^{2\pi}\bigl(1-2\cos t+\cos^2t\bigr)\,dt\\
&=2\pi+\pi=3\pi.
\end{aligned}
$$

由参数变换 $t\mapsto2\pi-t$，有

$$
 x(2\pi-t)=2\pi-x(t),
\qquad
y(2\pi-t)=y(t),
$$

所以区域 $D$ 关于直线 $x=\pi$ 对称。因此其形心横坐标为 $\pi$，从而

$$
 \iint_Dx\,dx\,dy
=\pi S_D=3\pi^2.
$$

再计算 $ \iint_Dy\,dx\,dy$。按先对 $y$ 积分，有

$$
\begin{aligned}
 \iint_Dy\,dx\,dy
&=\int_0^{2\pi}\left[\int_0^{y(t)}y\,dy\right]\frac{dx}{dt}\,dt\\
&=\frac12\int_0^{2\pi}(1-\cos t)^2(1-\cos t)\,dt\\
&=\frac12\int_0^{2\pi}(1-\cos t)^3\,dt.
\end{aligned}
$$

展开被积函数：

$$
 (1-\cos t)^3
=1-3\cos t+3\cos^2t-\cos^3t.
$$

利用

$$
 \int_0^{2\pi}\cos t\,dt=0,
\qquad
\int_0^{2\pi}\cos^2t\,dt=\pi,
\qquad
\int_0^{2\pi}\cos^3t\,dt=0,
$$

得

$$
 \iint_Dy\,dx\,dy
=\frac12(2\pi+3\pi)=\frac{5\pi}{2}.
$$

因此

$$
\begin{aligned}
 \iint_D(x+2y)\,dx\,dy
&=\iint_Dx\,dx\,dy+2\iint_Dy\,dx\,dy\\
&=3\pi^2+2\cdot\frac{5\pi}{2}\\
&=3\pi^2+5\pi.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【2020-3-10 分】** 设

$$
 D=\bigl\{(x,y)\mid x^2+y^2\le1,\ y\ge0\bigr\},
$$

且

$$
 f(x,y)=y\sqrt{1-x^2}+x\iint_Df(x,y)\,dx\,dy.
$$

求

$$
 \iint_Dx f(x,y)\,dx\,dy.
$$

<ExamSolution>

<ExamAnswer>$ \frac{3\pi^2}{128}$。</ExamAnswer>

<ExamExplanation>
记

$$
 A=\iint_Df(x,y)\,dx\,dy.
$$

则题设关系可写为

$$
 f(x,y)=y\sqrt{1-x^2}+xA.
$$

在区域 $D$ 上积分，得

$$
 A
=\iint_Dy\sqrt{1-x^2}\,dx\,dy
+A\iint_Dx\,dx\,dy.
$$

区域 $D$ 关于 $y$ 轴对称，而 $x$ 关于 $x$ 为奇函数，所以

$$
 \iint_Dx\,dx\,dy=0.
$$

因此

$$
\begin{aligned}
 A
&=\int_{-1}^{1}\int_0^{\sqrt{1-x^2}}y\sqrt{1-x^2}\,dy\,dx\\
&=\frac12\int_{-1}^{1}(1-x^2)^{\frac32}\,dx.
\end{aligned}
$$

令 $x=\sin t$，则 $dx=\cos t\,dt$，并利用偶性，得

$$
\begin{aligned}
 \int_{-1}^{1}(1-x^2)^{\frac32}\,dx
&=2\int_0^{\frac\pi2}\cos^4t\,dt\\
&=2\cdot\frac{3\pi}{16}\\
&=\frac{3\pi}{8}.
\end{aligned}
$$

故

$$
 A=\frac12\cdot\frac{3\pi}{8}
=\frac{3\pi}{16}.
$$

现在计算所求积分：

$$
\begin{aligned}
 I
&=\iint_Dx f(x,y)\,dx\,dy\\
&=\iint_Dxy\sqrt{1-x^2}\,dx\,dy
+A\iint_Dx^2\,dx\,dy.
\end{aligned}
$$

第一项的被积函数 $xy\sqrt{1-x^2}$ 关于 $x$ 为奇函数，区域 $D$ 关于 $y$ 轴对称，所以

$$
 \iint_Dxy\sqrt{1-x^2}\,dx\,dy=0.
$$

对第二项采用极坐标：

$$
 x=r\cos\theta,
\qquad
0\le r\le1,
\qquad
0\le\theta\le\pi.
$$

于是

$$
\begin{aligned}
 \iint_Dx^2\,dx\,dy
&=\int_0^\pi\int_0^1r^2\cos^2\theta\cdot r\,dr\,d\theta\\
&=\left(\int_0^1r^3\,dr\right)
\left(\int_0^\pi\cos^2\theta\,d\theta\right)\\
&=\frac14\cdot\frac\pi2\\
&=\frac\pi8.
\end{aligned}
$$

因此

$$
 I=A\cdot\frac\pi8
=\frac{3\pi}{16}\cdot\frac\pi8
=\frac{3\pi^2}{128}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>
