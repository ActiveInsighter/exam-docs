---
title: "四、广义积分"
sidebar_position: 4
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 四、广义积分

## 小题

### （一）广义积分的计算

<ExamQuestion>

1. **【1988-45-4 分】** 求定积分

$$
 \int_0^3\frac{\mathrm{d}x}{\sqrt{x}(1+x)}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{2\pi}{3}$。</ExamAnswer>

<ExamExplanation>
令 $ x=t^2$，则 $ \mathrm{d}x=2t\,\mathrm{d}t$，且当 $ x$ 从 $0$ 变到 $3$ 时，$ t$ 从 $0$ 变到 $\sqrt3$。于是

$$
  \begin{aligned}
   \int_0^3\frac{\mathrm{d}x}{\sqrt{x}(1+x)}
  &=2\int_0^{\sqrt3}\frac{\mathrm{d}t}{1+t^2}\\
  &=2\left.\arctan t\right|_0^{\sqrt3}\\
  &=\frac{2\pi}{3}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1991-3-3 分】**
$$
 \int_{1}^{+\infty}\frac{\ln x}{x^{2}}\,dx=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 1
$$
。
</ExamAnswer>

<ExamExplanation>
这是无穷区间上的反常积分。先取有限上限
$$
 R>1
$$
，作分部积分：
$$
   u=\ln x,\qquad dv=\frac{1}{x^2}\,dx,
$$
则
$$
   du=\frac{1}{x}\,dx,\qquad v=-\frac{1}{x}.
$$
因而
$$
  \begin{aligned}
   \int_1^R\frac{\ln x}{x^2}\,dx
  &=\left.-\frac{\ln x}{x}\right|_1^R
+\int_1^R\frac{1}{x^2}\,dx\\
  &=-\frac{\ln R}{R}+1-\frac{1}{R}.
  \end{aligned}
$$
由于
$$
   \lim_{R\to+\infty}\frac{\ln R}{R}=0,
$$
所以
$$
   \int_{1}^{+\infty}\frac{\ln x}{x^{2}}\,dx=1.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1992-3-3 分】**
$$
 \int_{1}^{+\infty}\frac{1}{x(x^{2}+1)}\,dx=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac12\ln2
$$
。
</ExamAnswer>

<ExamExplanation>
先作部分分式分解：
$$
 \frac{1}{x(x^2+1)}
=\frac1x-\frac{x}{x^2+1}.
$$
因此
$$
\begin{aligned}
 \int_1^R\frac{1}{x(x^2+1)}\,dx
&=\left[\ln x-\frac12\ln(x^2+1)\right]_1^R\\
&=\ln R-\frac12\ln(R^2+1)+\frac12\ln2.
\end{aligned}
$$
又
$$
\begin{aligned}
 \lim_{R\to+\infty}
\left[\ln R-\frac12\ln(R^2+1)\right]
&=\lim_{R\to+\infty}\frac12\ln\frac{R^2}{R^2+1}\\
&=0.
\end{aligned}
$$
故
$$
 \int_{1}^{+\infty}\frac{1}{x(x^{2}+1)}\,dx
=\frac12\ln2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【1993-3-5 分】** 求
$$
 \int_{0}^{+\infty}\frac{x}{(1+x)^{3}}\,dx
$$
。

<ExamSolution>

<ExamAnswer>
$$
 \frac12
$$
。
</ExamAnswer>

<ExamExplanation>
令
$$
   u=1+x,
$$
则
$$
 x=u-1
$$
，且积分区间由
$$
 [0,+\infty)
$$
 变为
$$
 [1,+\infty)
$$
。于是
$$
  \begin{aligned}
   \int_0^{+\infty}\frac{x}{(1+x)^3}\,dx
  &=\int_1^{+\infty}\frac{u-1}{u^3}\,du\\
  &=\int_1^{+\infty}\left(\frac1{u^2}-\frac1{u^3}\right)du\\
  &=\left[-\frac1u+\frac1{2u^2}\right]_1^{+\infty}\\
  &=\frac12.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【1995-5-3 分】** 设
$$
 \lim_{x\to\infty}\left(\frac{1+x}{x}\right)^{ax}
=\int_{-\infty}^{a}te^t\,dt
$$
，则常数
$$
 a=\_\_\_
$$
。

<ExamSolution>

<ExamAnswer>
$$
 2
$$
。
</ExamAnswer>

<ExamExplanation>
左端为
$$
  \begin{aligned}
   \lim_{x\to+\infty}
  \left(1+\frac1x\right)^{ax}
  &=\left[\lim_{x\to+\infty}
  \left(1+\frac1x\right)^x\right]^a\\
  &=e^a.
  \end{aligned}
$$

对右端作分部积分，或直接利用
$$
   \int te^t\,dt=(t-1)e^t+C,
$$
得
$$
  \begin{aligned}
   \int_{-\infty}^{a}te^t\,dt
  &=\left[(t-1)e^t\right]_{-\infty}^{a}\\
  &=(a-1)e^a.
  \end{aligned}
$$
由题意
$$
   e^a=(a-1)e^a.
$$
因为
$$
 e^a>0
$$
，所以
$$
   a-1=1,
$$
从而
$$
   a=2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【1997-2-3 分】**
$$
 \int_{0}^{+\infty}\frac{dx}{x^{2}+4x+8}=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac{\pi}{8}
$$
。
</ExamAnswer>

<ExamExplanation>
配方得
$$
 x^2+4x+8=(x+2)^2+4.
$$
因而
$$
\begin{aligned}
 \int_0^{+\infty}\frac{dx}{x^2+4x+8}
&=\int_0^{+\infty}\frac{dx}{(x+2)^2+2^2}\\
&=\left.\frac12\arctan\frac{x+2}{2}\right|_0^{+\infty}\\
&=\frac12\left(\frac\pi2-\frac\pi4\right)\\
&=\frac\pi8.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【2000-3-3 分】**
$$
 \int_{1}^{+\infty}\frac{dx}{e^{x}+e^{2-x}}=\_\_\_
$$
。

<ExamSolution>

<ExamAnswer>
$$
 \frac{\pi}{4e}
$$
。
</ExamAnswer>

<ExamExplanation>
分子、分母同乘
$$
 e^x
$$
，得
$$
   \frac{1}{e^x+e^{2-x}}
=\frac{e^x}{e^{2x}+e^2}.
$$
令
$$
   u=e^x,
$$
则
$$
 du=e^x\,dx
$$
。当
$$
 x=1
$$
 时，
$$
 u=e
$$
；当
$$
 x\to+\infty
$$
 时，
$$
 u\to+\infty
$$
。因此
$$
  \begin{aligned}
   \int_1^{+\infty}\frac{dx}{e^x+e^{2-x}}
  &=\int_e^{+\infty}\frac{du}{u^2+e^2}\\
  &=\left.\frac1e\arctan\frac{u}{e}\right|_e^{+\infty}\\
  &=\frac1e\left(\frac\pi2-\frac\pi4\right)\\
  &=\frac\pi{4e}.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. **【2000-2-3 分】**
$$
 \int_{2}^{+\infty}\frac{dx}{(x+7)\sqrt{x-2}}=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac{\pi}{3}
$$
。
</ExamAnswer>

<ExamExplanation>
令
$$
   t=\sqrt{x-2},
$$
则
$$
   x=t^2+2,\qquad dx=2t\,dt.
$$
当
$$
 x=2
$$
 时，
$$
 t=0
$$
；当
$$
 x\to+\infty
$$
 时，
$$
 t\to+\infty
$$
。于是
$$
  \begin{aligned}
   \int_2^{+\infty}\frac{dx}{(x+7)\sqrt{x-2}}
  &=\int_0^{+\infty}\frac{2t\,dt}{(t^2+9)t}\\
  &=2\int_0^{+\infty}\frac{dt}{t^2+3^2}\\
  &=\left.\frac23\arctan\frac{t}{3}\right|_0^{+\infty}\\
  &=\frac23\cdot\frac\pi2\\
  &=\frac\pi3.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. **【2002-1-3 分】**
$$
 \int_{e}^{+\infty}\frac{dx}{x\ln^{2}x}=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 1
$$
。
</ExamAnswer>

<ExamExplanation>
令
$$
   u=\ln x,
$$
则
$$
   du=\frac{dx}{x}.
$$
当
$$
 x=e
$$
 时，
$$
 u=1
$$
；当
$$
 x\to+\infty
$$
 时，
$$
 u\to+\infty
$$
。因此
$$
  \begin{aligned}
   \int_e^{+\infty}\frac{dx}{x\ln^2x}
  &=\int_1^{+\infty}\frac{du}{u^2}\\
  &=\left.-\frac1u\right|_1^{+\infty}\\
  &=1.
  \end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. **【2004-2-4 分】**
$$
 \int_{1}^{+\infty}\frac{dx}{x\sqrt{x^{2}-1}}=\_\_\_
$$

<ExamSolution>

<ExamAnswer>
$$
 \frac{\pi}{2}
$$
。
</ExamAnswer>

<ExamExplanation>
对
$$
 x\ge1
$$
，令
$$
   x=\sec t,
$$
其中
$$
 0\le t<\frac\pi2
$$
。则
$$
   dx=\sec t\tan t\,dt,
  \qquad
  \sqrt{x^2-1}=\tan t.
$$
因而
$$
  \begin{aligned}
   \int_1^{+\infty}\frac{dx}{x\sqrt{x^2-1}}
  &=\int_0^{\frac\pi2}\frac{\sec t\tan t}{\sec t\tan t}\,dt\\
  &=\int_0^{\frac\pi2}dt\\
  &=\frac\pi2.
  \end{aligned}
$$
也可直接利用原函数
$$
   \operatorname{arcsec}x
$$
 计算。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. **【2005-2-4 分】** $ \int_{0}^{1} \frac{x\,dx}{\left(2-x^{2}\right)\sqrt{1-x^{2}}}=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac{\pi}{4}$。</ExamAnswer>

<ExamExplanation>
令
$$
 u=\sqrt{1-x^2},
$$
则
$$
 u^2=1-x^2,
\qquad
du=-\frac{x}{\sqrt{1-x^2}}\,dx.
$$
同时
$$
 2-x^2=1+u^2.
$$
当 $ x=0$ 时，$ u=1$；当 $ x=1$ 时，$ u=0$。因此
$$
\begin{aligned}
 \int_0^1\frac{x\,dx}{(2-x^2)\sqrt{1-x^2}}
&=\int_1^0\frac{-du}{1+u^2}\\
&=\int_0^1\frac{du}{1+u^2}\\
&=\left.\arctan u\right|_0^1\\
&=\frac\pi4.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

12. **【2006-2-4 分】** 反常积分 $ \int_{0}^{+\infty}\frac{x\,dx}{(1+x^{2})^{2}}=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac12$。</ExamAnswer>

<ExamExplanation>
令
$$
 u=1+x^2,
\qquad du=2x\,dx.
$$
则
$$
\begin{aligned}
 \int_0^{+\infty}\frac{x\,dx}{(1+x^2)^2}
&=\frac12\int_1^{+\infty}u^{-2}\,du\\
&=\frac12\left[-\frac1u\right]_1^{+\infty}\\
&=\frac12.
\end{aligned}
$$
被积函数在 $ +\infty$ 附近与 $ x^{-3}$ 同阶，因此该反常积分收敛。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

13. **【2009-2-4 分】** 已知 $ \int_{-\infty}^{+\infty}e^{k|x|}\,dx=1$，则 $ k=\_\_\_$

<ExamSolution>

<ExamAnswer>$ -2$。</ExamAnswer>

<ExamExplanation>
因为 $ e^{k|x|}$ 是偶函数，故
$$
 \int_{-\infty}^{+\infty}e^{k|x|}\,dx
=2\int_0^{+\infty}e^{kx}\,dx.
$$
要使积分收敛，必须有
$$
 k<0.
$$
此时
$$
\begin{aligned}
 2\int_0^{+\infty}e^{kx}\,dx
&=2\left.\frac{e^{kx}}{k}\right|_0^{+\infty}\\
&=-\frac2k.
\end{aligned}
$$
由题意
$$
 -\frac2k=1,
$$
解得
$$
 k=-2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

14. **【2011-2-4 分】** 设函数
$$
f(x)=
\begin{cases}
\lambda e^{-\lambda x}, & x>0,\ \lambda>0,\\
0, & x\le0,
\end{cases}
$$
则 $ \int_{-\infty}^{+\infty}xf(x)\,dx=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac1\lambda$。</ExamAnswer>

<ExamExplanation>
因为 $ x\le0$ 时 $ f(x)=0$，所以
$$
 \int_{-\infty}^{+\infty}xf(x)\,dx
=\lambda\int_0^{+\infty}xe^{-\lambda x}\,dx.
$$
对右端作分部积分，取
$$
 u=x,
\qquad dv=e^{-\lambda x}\,dx,
$$
则
$$
 du=dx,
\qquad v=-\frac1\lambda e^{-\lambda x}.
$$
因此
$$
\begin{aligned}
 \int_0^{+\infty}xe^{-\lambda x}\,dx
&=\left.-\frac{x}{\lambda}e^{-\lambda x}\right|_0^{+\infty}
+\frac1\lambda\int_0^{+\infty}e^{-\lambda x}\,dx\\
&=\frac1{\lambda^2}.
\end{aligned}
$$
从而
$$
 \int_{-\infty}^{+\infty}xf(x)\,dx
=\lambda\cdot\frac1{\lambda^2}
=\frac1\lambda.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

15. **【2013-13-4 分】** $ \int_{1}^{+\infty}\frac{\ln x}{(1+x)^{2}}\,dx=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \ln2$。</ExamAnswer>

<ExamExplanation>
作分部积分，取
$$
 u=\ln x,
\qquad dv=\frac{dx}{(1+x)^2},
$$
则
$$
 du=\frac{dx}{x},
\qquad v=-\frac1{1+x}.
$$
因为
$$
 \lim_{x\to+\infty}\frac{\ln x}{1+x}=0,
$$
所以
$$
\begin{aligned}
 \int_1^{+\infty}\frac{\ln x}{(1+x)^2}\,dx
&=\left.-\frac{\ln x}{1+x}\right|_1^{+\infty}
+\int_1^{+\infty}\frac{dx}{x(1+x)}\\
&=\int_1^{+\infty}\left(\frac1x-\frac1{x+1}\right)dx\\
&=\left.\ln\frac{x}{x+1}\right|_1^{+\infty}\\
&=\ln2.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

16. **【2014-2-4 分】** $ \int_{-\infty}^{1}\frac{dx}{x^{2}+2x+5}=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac{3\pi}{8}$。</ExamAnswer>

<ExamExplanation>
配方得
$$
 x^2+2x+5=(x+1)^2+4.
$$
因此
$$
\begin{aligned}
 \int_{-\infty}^{1}\frac{dx}{x^2+2x+5}
&=\int_{-\infty}^{1}\frac{dx}{(x+1)^2+2^2}\\
&=\left.\frac12\arctan\frac{x+1}{2}\right|_{-\infty}^{1}\\
&=\frac12\left(\frac\pi4+\frac\pi2\right)\\
&=\frac{3\pi}{8}.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

17. **【2017-2-4 分】** $ \int_{0}^{+\infty}\frac{\ln(1+x)}{(1+x)^{2}}\,dx=\_\_\_$。

<ExamSolution>

<ExamAnswer>$ 1$。</ExamAnswer>

<ExamExplanation>
令
$$
 t=1+x,
$$
则当 $ x=0$ 时 $ t=1$，当 $ x\to+\infty$ 时 $ t\to+\infty$。因此
$$
 \int_0^{+\infty}\frac{\ln(1+x)}{(1+x)^2}\,dx
=\int_1^{+\infty}\frac{\ln t}{t^2}\,dt.
$$
作分部积分，取
$$
 u=\ln t,
\qquad dv=t^{-2}\,dt,
$$
则
$$
 du=\frac{dt}{t},
\qquad v=-\frac1t.
$$
从而
$$
\begin{aligned}
 \int_1^{+\infty}\frac{\ln t}{t^2}\,dt
&=\left.-\frac{\ln t}{t}\right|_1^{+\infty}
+\int_1^{+\infty}\frac{dt}{t^2}\\
&=1.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

18. **【2018-2-4 分】** $ \int_{5}^{+\infty}\frac{dx}{x^{2}-4x+3}=\_\_\_$。

<ExamSolution>

<ExamAnswer>$ \frac12\ln2$。</ExamAnswer>

<ExamExplanation>
分解因式：
$$
 x^2-4x+3=(x-1)(x-3).
$$
部分分式分解为
$$
 \frac1{(x-1)(x-3)}
=-\frac1{2(x-1)}+\frac1{2(x-3)}.
$$
因此
$$
\begin{aligned}
 \int_5^{+\infty}\frac{dx}{x^2-4x+3}
&=\left.\frac12\ln\frac{x-3}{x-1}\right|_5^{+\infty}\\
&=0-\frac12\ln\frac24\\
&=\frac12\ln2.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

19. **【2021-2-5 分】** $ \int_{-\infty}^{+\infty}|x|\cdot3^{-x^{2}}\,dx=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac1{\ln3}$。</ExamAnswer>

<ExamExplanation>
因为被积函数为偶函数，且
$$
 3^{-x^2}=e^{-x^2\ln3},
$$
所以
$$
\begin{aligned}
 \int_{-\infty}^{+\infty}|x|\cdot3^{-x^2}\,dx
&=2\int_0^{+\infty}x e^{-x^2\ln3}\,dx.
\end{aligned}
$$
令
$$
 u=x^2\ln3,
\qquad du=2x\ln3\,dx,
$$
则
$$
\begin{aligned}
 2\int_0^{+\infty}x e^{-x^2\ln3}\,dx
&=\frac1{\ln3}\int_0^{+\infty}e^{-u}\,du\\
&=\frac1{\ln3}.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

20. **【2021-1-5 分】** $ \int_{0}^{+\infty}\frac{dx}{x^{2}+2x+2}=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac\pi4$。</ExamAnswer>

<ExamExplanation>
配方得
$$
 x^2+2x+2=(x+1)^2+1.
$$
因此
$$
\begin{aligned}
 \int_0^{+\infty}\frac{dx}{x^2+2x+2}
&=\left.\arctan(x+1)\right|_0^{+\infty}\\
&=\frac\pi2-\frac\pi4\\
&=\frac\pi4.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

21. **【2023-2-5 分】** 若函数 $ f(\alpha)=\int_{2}^{+\infty}\frac{1}{x(\ln x)^{\alpha+1}}\,dx$ 在 $\alpha=\alpha_{0}$ 处取得最小值，则 $\alpha_{0}=(\ )$

<ExamChoices>
<ExamOption>A. $ \frac{-1}{\ln(\ln 2)}$</ExamOption>
<ExamOption>B. $-\ln2$</ExamOption>
<ExamOption>C. $ \frac{1}{-\ln 2}$</ExamOption>
<ExamOption>D. $\ln 2$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>A。</ExamAnswer>

<ExamExplanation>
令 $ t=\ln x$，则 $ dt=\frac{dx}{x}$，从而
$$
 f(\alpha)=\int_{\ln2}^{+\infty}t^{-\alpha-1}\,dt.
$$
该反常积分收敛的充要条件为 $ \alpha>0$。在此条件下，
$$
\begin{aligned}
 f(\alpha)
&=\left.-\frac1\alpha t^{-\alpha}\right|_{\ln2}^{+\infty}\\
&=\frac{(\ln2)^{-\alpha}}{\alpha}.
\end{aligned}
$$
取对数：
$$
 \ln f(\alpha)=-\alpha\ln(\ln2)-\ln\alpha.
$$
因为 $ 0<\ln2<1$，故 $ \ln(\ln2)<0$。求导得
$$
 \frac{d}{d\alpha}\ln f(\alpha)
=-\ln(\ln2)-\frac1\alpha.
$$
令其等于零，得到
$$
 \alpha_0=-\frac1{\ln(\ln2)}.
$$
又
$$
 \frac{d^2}{d\alpha^2}\ln f(\alpha)=\frac1{\alpha^2}>0,
$$
因而该点为唯一最小值点，故选 A。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

22. **【2024-3-5 分】** $ \int_{2}^{+\infty}\frac{5}{x^{4}+3x^{2}-4}\,dx=\_\_\_$

<ExamSolution>

<ExamAnswer>$ \frac12\ln3-\frac\pi8$。</ExamAnswer>

<ExamExplanation>
先因式分解：
$$
 x^4+3x^2-4=(x^2-1)(x^2+4).
$$
作部分分式分解：
$$
\begin{aligned}
 \frac5{(x^2-1)(x^2+4)}
&=\frac1{x^2-1}-\frac1{x^2+4}.
\end{aligned}
$$
因此
$$
\begin{aligned}
 \int_2^{+\infty}\frac5{x^4+3x^2-4}\,dx
&=\left[\frac12\ln\frac{x-1}{x+1}
-\frac12\arctan\frac x2\right]_2^{+\infty}\\
&=-\frac\pi4-\left(-\frac12\ln3-\frac\pi8\right)\\
&=\frac12\ln3-\frac\pi8.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

23. **【2025-23-5 分】** 设 $ \int_{1}^{+\infty}\frac{a}{x(2x+a)}\,dx=\ln2$，则 $a=\_\_\_$

<ExamSolution>

<ExamAnswer>$ 2$。</ExamAnswer>

<ExamExplanation>
当积分收敛且积分区间内无奇点时，作部分分式分解：
$$
 \frac{a}{x(2x+a)}=\frac1x-\frac2{2x+a}.
$$
因而
$$
\begin{aligned}
 \int_1^{+\infty}\frac{a}{x(2x+a)}\,dx
&=\left[\ln x-\ln|2x+a|\right]_1^{+\infty}\\
&=\ln\frac12+\ln|a+2|\\
&=\ln\frac{|a+2|}{2}.
\end{aligned}
$$
由题意
$$
 \ln\frac{|a+2|}{2}=\ln2,
$$
即 $ |a+2|=4$，形式上得到 $ a=2$ 或 $ a=-6$。

但当 $ a=-6$ 时，分母中的 $ 2x+a=2(x-3)$ 在积分区间 $ [1,+\infty)$ 内为零，原反常积分发散，故舍去。于是
$$
 a=2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

### （二）广义积分收敛性的判别

<ExamQuestion>

1. **【1987-45-2 分】** 下列广义积分收敛的是（  ）。

<ExamChoices>
<ExamOption>A. $ \int_{e}^{+\infty}\frac{\ln x}{x}\,dx$</ExamOption>
<ExamOption>B. $ \int_{e}^{+\infty}\frac{dx}{x\ln x}$</ExamOption>
<ExamOption>C. $ \int_{e}^{+\infty}\frac{dx}{x(\ln x)^2}$</ExamOption>
<ExamOption>D. $ \int_{e}^{+\infty}\frac{dx}{x\sqrt{\ln x}}$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>C。</ExamAnswer>

<ExamExplanation>
对各积分统一令 $ t=\ln x$，则 $ dt=\frac{dx}{x}$，且 $ x=e$ 对应 $ t=1$。

A 项化为
$$
 \int_1^{+\infty}t\,dt,
$$
发散。

B 项化为
$$
 \int_1^{+\infty}\frac{dt}{t},
$$
发散。

C 项化为
$$
 \int_1^{+\infty}\frac{dt}{t^2},
$$
收敛。

D 项化为
$$
 \int_1^{+\infty}\frac{dt}{\sqrt t},
$$
发散。因此选 C。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1995-45-3 分】** 下列广义积分发散的是（  ）。

<ExamChoices>
<ExamOption>A. $ \int_{-1}^{1}\frac{dx}{\sin x}$</ExamOption>
<ExamOption>B. $ \int_{-1}^{1}\frac{dx}{\sqrt{1-x^{2}}}$</ExamOption>
<ExamOption>C. $ \int_{0}^{+\infty}e^{-x^{2}}\,dx$</ExamOption>
<ExamOption>D. $ \int_{2}^{+\infty}\frac{dx}{x\ln^{2}x}$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>A。</ExamAnswer>

<ExamExplanation>
A 项在 $ x=0$ 处有瑕点，且
$$
 \frac1{\sin x}\sim\frac1x\qquad(x\to0).
$$
因此 $ \int_{-1}^{0}\frac{dx}{\sin x}$ 与 $ \int_{0}^{1}\frac{dx}{\sin x}$ 均不收敛。虽然被积函数为奇函数，其柯西主值为零，但按反常积分定义必须左右两部分分别收敛，故 A 项发散。

B 项为反正弦积分：
$$
 \int_{-1}^{1}\frac{dx}{\sqrt{1-x^2}}=\pi,
$$
收敛。

C 项在无穷远处指数衰减，收敛。

D 项令 $ t=\ln x$ 后化为 $ \int_{\ln2}^{+\infty}t^{-2}\,dt$，收敛。因此选 A。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【2005-4-3 分】** 下列结论中正确的是（  ）。

<ExamChoices>
<ExamOption>A. $ \int_{1}^{+\infty}\frac{dx}{x(x+1)}$ 与 $ \int_{0}^{1}\frac{dx}{x(x+1)}$ 都收敛</ExamOption>
<ExamOption>B. $ \int_{1}^{+\infty}\frac{dx}{x(x+1)}$ 与 $ \int_{0}^{1}\frac{dx}{x(x+1)}$ 都发散</ExamOption>
<ExamOption>C. $ \int_{1}^{+\infty}\frac{dx}{x(x+1)}$ 发散，$ \int_{0}^{1}\frac{dx}{x(x+1)}$ 收敛</ExamOption>
<ExamOption>D. $ \int_{1}^{+\infty}\frac{dx}{x(x+1)}$ 收敛，$ \int_{0}^{1}\frac{dx}{x(x+1)}$ 发散</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
当 $ x\to+\infty$ 时，
$$
 \frac1{x(x+1)}\sim\frac1{x^2},
$$
故 $ \int_1^{+\infty}\frac{dx}{x(x+1)}$ 收敛。

当 $ x\to0^+$ 时，
$$
 \frac1{x(x+1)}\sim\frac1x,
$$
故 $ \int_0^1\frac{dx}{x(x+1)}$ 发散。因此选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【2010-12-4 分】** 设 $m,n$ 是正整数，则反常积分
$$
 \int_{0}^{1}\frac{\sqrt[m]{\ln^{2}(1-x)}}{\sqrt[n]{x}}\,dx
$$
的收敛性（  ）。

<ExamChoices>
<ExamOption>A. 仅与 $m$ 的取值有关</ExamOption>
<ExamOption>B. 仅与 $n$ 的取值有关</ExamOption>
<ExamOption>C. 与 $m,n$ 的取值都有关</ExamOption>
<ExamOption>D. 与 $m,n$ 的取值都无关</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
被积函数在 $ x=0$ 和 $ x=1$ 两端都可能产生反常性，分别讨论。

当 $ x\to0^+$ 时，$ \ln(1-x)\sim-x$，故
$$
 \frac{\sqrt[m]{\ln^2(1-x)}}{\sqrt[n]x}
\sim x^{\frac2m-\frac1n}.
$$
对任意正整数 $m,n$，都有
$$
 \frac2m-\frac1n>-1,
$$
所以在 $ x=0$ 附近可积。

当 $ x\to1^-$ 时，令 $ t=1-x$，则 $ x^{-1/n}\to1$，只需考察
$$
 |\ln t|^{\frac2m}.
$$
对任意有限正指数 $ \frac2m$，积分 $ \int_0^\delta|\ln t|^{2/m}\,dt$ 均收敛。

因此原反常积分对任意正整数 $m,n$ 都收敛，敛散性与二者取值无关，故选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【2013-2-4 分】** 设函数
$$
 f(x)=
\begin{cases}
\frac1{(x-1)^{\alpha-1}},&1<x<e,\\[2mm]
\frac1{x\ln^{\alpha+1}x},&x\ge e,
\end{cases}
$$
若反常积分 $ \int_{1}^{+\infty}f(x)\,dx$ 收敛，则（  ）。

<ExamChoices>
<ExamOption>A. $\alpha<-2$</ExamOption>
<ExamOption>B. $\alpha>2$</ExamOption>
<ExamOption>C. $-2<\alpha<0$</ExamOption>
<ExamOption>D. $0<\alpha<2$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
将原积分拆成
$$
 \int_1^e\frac{dx}{(x-1)^{\alpha-1}}
+\int_e^{+\infty}\frac{dx}{x\ln^{\alpha+1}x}.
$$

对第一部分，令 $ t=x-1$，则在 $ t=0$ 附近被积函数为
$$
 t^{1-\alpha}.
$$
根据 $ \int_0^c t^p\,dt$ 收敛的条件 $ p>-1$，有
$$
 1-\alpha>-1,
$$
即
$$
 \alpha<2.
$$

对第二部分，令 $ u=\ln x$，则
$$
 \int_e^{+\infty}\frac{dx}{x\ln^{\alpha+1}x}
=\int_1^{+\infty}\frac{du}{u^{\alpha+1}}.
$$
该积分收敛的条件为
$$
 \alpha+1>1,
$$
即 $ \alpha>0$。

综合得
$$
 0<\alpha<2,
$$
故选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【2015-2-4 分】** 下列反常积分中收敛的是（  ）。

<ExamChoices>
<ExamOption>A. $ \int_{2}^{+\infty}\frac{dx}{\sqrt{x}}$</ExamOption>
<ExamOption>B. $ \int_{2}^{+\infty}\frac{\ln x}{x}\,dx$</ExamOption>
<ExamOption>C. $ \int_{2}^{+\infty}\frac{dx}{x\ln x}$</ExamOption>
<ExamOption>D. $ \int_{2}^{+\infty}\frac{x}{e^{x}}\,dx$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
A 项为 $ p$ 型积分 $ \int_2^{+\infty}x^{-1/2}\,dx$，因指数 $ \frac12\le1$，故发散。

B 项有
$$
 \int\frac{\ln x}{x}\,dx=\frac12(\ln x)^2+C,
$$
故发散。

C 项有
$$
 \int\frac{dx}{x\ln x}=\ln(\ln x)+C,
$$
故发散。

D 项中 $ xe^{-x}$ 指数衰减，且分部积分可得
$$
 \int_2^{+\infty}xe^{-x}\,dx
=\left.-(x+1)e^{-x}\right|_2^{+\infty}
=3e^{-2},
$$
因此收敛，故选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【2016-2-4 分】** 反常积分

① $ \int_{-\infty}^{0}\frac1{x^2}e^{\frac1x}\,dx$；

② $ \int_{0}^{+\infty}\frac1{x^2}e^{\frac1x}\,dx$

的敛散性为（  ）。

<ExamChoices>
<ExamOption>A. ①收敛，②收敛</ExamOption>
<ExamOption>B. ①收敛，②发散</ExamOption>
<ExamOption>C. ①发散，②收敛</ExamOption>
<ExamOption>D. ①发散，②发散</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
注意
$$
 \frac{d}{dx}e^{1/x}=-\frac1{x^2}e^{1/x}.
$$

对①，有
$$
\begin{aligned}
 \int_{-\infty}^{0}\frac1{x^2}e^{1/x}\,dx
&=-\left.e^{1/x}\right|_{-\infty}^{0^-}\\
&=-\left(0-1\right)=1,
\end{aligned}
$$
因此①收敛。

对②，在 $ x\to0^+$ 时 $ e^{1/x}\to+\infty$，并且
$$
\begin{aligned}
 \int_{\varepsilon}^{1}\frac1{x^2}e^{1/x}\,dx
&=-\left.e^{1/x}\right|_{\varepsilon}^{1}\\
&=e^{1/\varepsilon}-e\to+\infty
\qquad(\varepsilon\to0^+).
\end{aligned}
$$
所以②发散，故选 B。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. **【2016-1-4 分】** 若反常积分
$$
 \int_{0}^{+\infty}\frac{1}{x^{a}(1+x)^{b}}\,dx
$$
收敛，则（  ）。

<ExamChoices>
<ExamOption>A. $ a<1$ 且 $ b>1$</ExamOption>
<ExamOption>B. $ a>1$ 且 $ b>1$</ExamOption>
<ExamOption>C. $ a<1$ 且 $ a+b>1$</ExamOption>
<ExamOption>D. $ a>1$ 且 $ a+b>1$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>C。</ExamAnswer>

<ExamExplanation>
原积分在 $ x=0$ 和 $ x=+\infty$ 两处都可能反常，需要分别判断。

当 $ x\to0^+$ 时，$ (1+x)^b\to1$，故
$$
 \frac{1}{x^a(1+x)^b}\sim\frac1{x^a}.
$$
根据瑕积分判别准则，
$$
 \int_0^1\frac{dx}{x^a}
$$
收敛的充要条件是
$$
 a<1.
$$

当 $ x\to+\infty$ 时，$ 1+x\sim x$，故
$$
 \frac{1}{x^a(1+x)^b}\sim\frac1{x^{a+b}}.
$$
而
$$
 \int_1^{+\infty}\frac{dx}{x^{a+b}}
$$
收敛的充要条件是
$$
 a+b>1.
$$

因此原反常积分收敛的条件为
$$
 a<1,\qquad a+b>1,
$$
故选 C。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. **【2019-2-4 分】** 下列反常积分发散的是（  ）。

<ExamChoices>
<ExamOption>A. $ \int_{0}^{+\infty}xe^{-x}\,dx$</ExamOption>
<ExamOption>B. $ \int_{0}^{+\infty}xe^{-x^2}\,dx$</ExamOption>
<ExamOption>C. $ \int_{0}^{+\infty}\frac{\arctan x}{1+x^2}\,dx$</ExamOption>
<ExamOption>D. $ \int_{0}^{+\infty}\frac{x}{1+x^2}\,dx$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
A 项利用分部积分，
$$
\begin{aligned}
 \int_0^{+\infty}xe^{-x}\,dx
&=\left.-xe^{-x}\right|_0^{+\infty}+\int_0^{+\infty}e^{-x}\,dx\\
&=1,
\end{aligned}
$$
故收敛。

B 项令 $ u=x^2$，则 $ du=2x\,dx$，所以
$$
 \int_0^{+\infty}xe^{-x^2}\,dx
=\frac12\int_0^{+\infty}e^{-u}\,du
=\frac12,
$$
故收敛。

C 项令 $ u=\arctan x$，则 $ du=\frac{dx}{1+x^2}$，于是
$$
 \int_0^{+\infty}\frac{\arctan x}{1+x^2}\,dx
=\int_0^{\frac\pi2}u\,du
=\frac{\pi^2}{8},
$$
故收敛。

D 项有
$$
\begin{aligned}
 \int_0^R\frac{x}{1+x^2}\,dx
&=\frac12\ln(1+R^2).
\end{aligned}
$$
当 $ R\to+\infty$ 时，上式趋于 $ +\infty$，故该积分发散。因此选 D。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. **【2022-2-5 分】** 设 $ p$ 为常数，若反常积分
$$
 \int_{0}^{1}\frac{\ln x}{x^{p}(1-x)^{1-p}}\,dx
$$
收敛，则 $ p$ 的取值范围是（  ）。

<ExamChoices>
<ExamOption>A. $ (-1,1)$</ExamOption>
<ExamOption>B. $ (1,2)$</ExamOption>
<ExamOption>C. $ (-\infty,1)$</ExamOption>
<ExamOption>D. $ (-\infty,2)$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>A。</ExamAnswer>

<ExamExplanation>
被积函数在 $ x=0$ 和 $ x=1$ 两端均可能反常。

当 $ x\to0^+$ 时，$ (1-x)^{1-p}\to1$，故
$$
 \frac{\ln x}{x^p(1-x)^{1-p}}
\sim\frac{\ln x}{x^p}.
$$
已知
$$
 \int_0^\delta x^\alpha|\ln x|\,dx
$$
收敛的条件为 $ \alpha>-1$。这里 $ \alpha=-p$，故要求
$$
 -p>-1,
$$
即
$$
 p<1.
$$

当 $ x\to1^-$ 时，$ \ln x\sim-(1-x)$，且 $ x^p\to1$，故
$$
 \frac{\ln x}{x^p(1-x)^{1-p}}
\sim-(1-x)^p.
$$
瑕积分
$$
 \int_{1-\delta}^{1}(1-x)^p\,dx
$$
收敛的条件为
$$
 p>-1.
$$

综合两端条件，得
$$
 -1<p<1.
$$
因此选 A。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. **【2024-2-5 分】** 设非负函数 $ f(x)$ 在 $ [0,+\infty)$ 上连续，给出以下三个命题：

（1）若 $ \int_{0}^{+\infty}f^2(x)\,dx$ 收敛，则 $ \int_{0}^{+\infty}f(x)\,dx$ 收敛；

（2）若存在 $ p>1$，使得 $ \lim_{x\to+\infty}x^pf(x)$ 存在，则 $ \int_{0}^{+\infty}f(x)\,dx$ 收敛；

（3）若 $ \int_{0}^{+\infty}f(x)\,dx$ 收敛，则存在 $ p>1$，使得 $ \lim_{x\to+\infty}x^pf(x)$ 存在。

其中真命题的个数为（  ）。

<ExamChoices>
<ExamOption>A. $ 0$</ExamOption>
<ExamOption>B. $ 1$</ExamOption>
<ExamOption>C. $ 2$</ExamOption>
<ExamOption>D. $ 3$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
**命题（1）错误。** 取
$$
 f(x)=\frac1{(1+x)^{3/4}}.
$$
则 $ f(x)$ 非负且连续，并且
$$
 \int_0^{+\infty}f^2(x)\,dx
=\int_0^{+\infty}\frac{dx}{(1+x)^{3/2}}
$$
收敛；但
$$
 \int_0^{+\infty}f(x)\,dx
=\int_0^{+\infty}\frac{dx}{(1+x)^{3/4}}
$$
发散。因此平方可积不能推出可积。

**命题（2）正确。** 若存在 $ p>1$，且有限极限
$$
 \lim_{x\to+\infty}x^pf(x)=L
$$
存在，则 $ x^pf(x)$ 在充分大的区间上有界。因此存在常数 $ M>0$ 和 $ X>0$，使得当 $ x>X$ 时，
$$
 0\le f(x)\le\frac{M}{x^p}.
$$
因为 $ p>1$，反常积分 $ \int_X^{+\infty}x^{-p}\,dx$ 收敛，由比较判别法知 $ \int_X^{+\infty}f(x)\,dx$ 收敛；再结合 $ f$ 在 $ [0,X]$ 上连续，故原积分收敛。

**命题（3）错误。** 取
$$
 f(x)=\frac1{(x+2)[\ln(x+2)]^2}.
$$
则 $ f(x)$ 非负且连续，并且令 $ u=\ln(x+2)$ 可得
$$
 \int_0^{+\infty}f(x)\,dx
=\int_{\ln2}^{+\infty}\frac{du}{u^2}
$$
收敛。但对任意 $ p>1$，
$$
 x^pf(x)
=\frac{x^p}{(x+2)[\ln(x+2)]^2}
\sim\frac{x^{p-1}}{(\ln x)^2}\to+\infty,
$$
因而不存在有限极限。所以积分收敛不能保证函数具有某个高于一次幂的衰减速度。

因此仅命题（2）正确，真命题的个数为 $ 1$，故选 B。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 大题

### （一）广义积分的计算

<ExamQuestion>

1. **【1991-2-6 分】** 求

$$
 \int_{3}^{+\infty}\frac{dx}{(x-1)^{4}\sqrt{x^{2}-2x}}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{2}{3}-\frac{3\sqrt{3}}{8}=\frac{16-9\sqrt{3}}{24}$。</ExamAnswer>

<ExamExplanation>
令
$$
 t=x-1,
$$
则
$$
 x^{2}-2x=(x-1)^{2}-1=t^{2}-1,
$$
且当 $ x=3$ 时 $ t=2$，当 $ x\to+\infty$ 时 $ t\to+\infty$。因此
$$
 I=\int_{2}^{+\infty}\frac{dt}{t^{4}\sqrt{t^{2}-1}}.
$$

再令
$$
 t=\sec\theta,
$$
则
$$
 dt=\sec\theta\tan\theta\,d\theta,
\qquad
\sqrt{t^{2}-1}=\tan\theta.
$$
积分限相应变为
$$
 t=2\Longrightarrow\theta=\frac{\pi}{3},
\qquad
t\to+\infty\Longrightarrow\theta\to\frac{\pi}{2}.
$$
故
$$
\begin{aligned}
 I
&=\int_{\frac{\pi}{3}}^{\frac{\pi}{2}}
\frac{\sec\theta\tan\theta}{\sec^{4}\theta\tan\theta}\,d\theta\\
&=\int_{\frac{\pi}{3}}^{\frac{\pi}{2}}\cos^{3}\theta\,d\theta\\
&=\left(\sin\theta-\frac{\sin^{3}\theta}{3}\right)
\Bigg|_{\frac{\pi}{3}}^{\frac{\pi}{2}}\\
&=\frac{2}{3}-\frac{3\sqrt{3}}{8}
=\frac{16-9\sqrt{3}}{24}.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. **【1993-45-7 分】** 已知

$$
 \lim_{x\to\infty}\left(\frac{x-a}{x+a}\right)^{x}
=\int_{a}^{+\infty}4x^{2}e^{-2x}\,dx,
$$

求常数 $ a$ 的值。

<ExamSolution>

<ExamAnswer>$ a=0$ 或 $ a=-1$。</ExamAnswer>

<ExamExplanation>
先计算左端极限。取对数得
$$
\begin{aligned}
 \ln L
&=\lim_{x\to+\infty}x\ln\left(\frac{x-a}{x+a}\right)\\
&=\lim_{x\to+\infty}x\left[\ln\left(1-\frac{a}{x}\right)-\ln\left(1+\frac{a}{x}\right)\right]\\
&=-a-a=-2a.
\end{aligned}
$$
因而
$$
 L=e^{-2a}.
$$

再计算右端反常积分。分部积分或利用递推公式可得
$$
\begin{aligned}
 \int_a^{+\infty}4x^2e^{-2x}\,dx
&=e^{-2a}\left(2a^2+2a+1\right).
\end{aligned}
$$
由题设
$$
 e^{-2a}=e^{-2a}(2a^2+2a+1).
$$
因为 $ e^{-2a}>0$，约去后得
$$
 2a^2+2a=0,
$$
即
$$
 2a(a+1)=0.
$$
因此
$$
 a=0\quad\text{或}\quad a=-1.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. **【1995-45-6 分】** 计算

$$
 \int_{0}^{+\infty}\frac{xe^{-x}}{(1+e^{-x})^{2}}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \ln2$。</ExamAnswer>

<ExamExplanation>
注意到
$$
 \frac{e^{-x}}{(1+e^{-x})^{2}}
=\frac{e^x}{(1+e^x)^2}
=-\left(\frac{1}{1+e^x}\right)'.
$$
因此
$$
 I=-\int_0^{+\infty}x\,d\left(\frac{1}{1+e^x}\right).
$$
分部积分得
$$
\begin{aligned}
 I
&=-\left.\frac{x}{1+e^x}\right|_0^{+\infty}
+\int_0^{+\infty}\frac{dx}{1+e^x}.
\end{aligned}
$$
由于
$$
 \lim_{x\to+\infty}\frac{x}{1+e^x}=0,
$$
故边界项为 $ 0$。又令 $ u=e^{-x}$，则 $ dx=-\frac{du}{u}$，从而
$$
\begin{aligned}
 I
&=\int_0^{+\infty}\frac{dx}{1+e^x}
=\int_1^0\frac{u}{1+u}\left(-\frac{du}{u}\right)\\
&=\int_0^1\frac{du}{1+u}
=\ln2.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. **【1998-2-6 分】** 计算积分

$$
 \int_{\frac12}^{\frac32}\frac{dx}{\sqrt{|x-x^{2}|}}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\pi}{2}+2\ln\frac{\sqrt3+1}{\sqrt2}$。</ExamAnswer>

<ExamExplanation>
因为
$$
 x-x^2=x(1-x),
$$
在 $ \left[\frac12,1\right]$ 上非负，在 $ \left[1,\frac32\right]$ 上非正，所以应在 $ x=1$ 处分段：
$$
 I=\int_{\frac12}^{1}\frac{dx}{\sqrt{x(1-x)}}
+\int_{1}^{\frac32}\frac{dx}{\sqrt{x(x-1)}}.
$$

对第一部分，令 $ x=\sin^2\theta$，则
$$
 dx=2\sin\theta\cos\theta\,d\theta,
\qquad
\sqrt{x(1-x)}=\sin\theta\cos\theta.
$$
当 $ x=\frac12$ 时 $ \theta=\frac\pi4$，当 $ x=1$ 时 $ \theta=\frac\pi2$，故
$$
 \int_{\frac12}^{1}\frac{dx}{\sqrt{x(1-x)}}
=2\int_{\frac\pi4}^{\frac\pi2}d\theta
=\frac\pi2.
$$

对第二部分，令 $ x=\sec^2\theta$，则
$$
 dx=2\sec^2\theta\tan\theta\,d\theta,
\qquad
\sqrt{x(x-1)}=\sec\theta\tan\theta.
$$
因此
$$
\begin{aligned}
 \int_{1}^{\frac32}\frac{dx}{\sqrt{x(x-1)}}
&=2\int_0^{\theta_1}\sec\theta\,d\theta\\
&=2\ln(\sec\theta+\tan\theta)\Big|_0^{\theta_1},
\end{aligned}
$$
其中 $ \sec^2\theta_1=\frac32$，故
$$
 \sec\theta_1=\sqrt{\frac32},
\qquad
\tan\theta_1=\sqrt{\frac12}.
$$
从而
$$
 \int_{1}^{\frac32}\frac{dx}{\sqrt{x(x-1)}}
=2\ln\frac{\sqrt3+1}{\sqrt2}.
$$

综上
$$
 I=\frac\pi2+2\ln\frac{\sqrt3+1}{\sqrt2}.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. **【1999-2-6 分】** 计算

$$
 \int_{1}^{+\infty}\frac{\arctan x}{x^{2}}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac\pi4+\frac12\ln2$。</ExamAnswer>

<ExamExplanation>
令
$$
 u=\arctan x,
\qquad
dv=\frac{dx}{x^2},
$$
则
$$
 du=\frac{dx}{1+x^2},
\qquad
v=-\frac1x.
$$
分部积分得
$$
\begin{aligned}
 I
&=\left.-\frac{\arctan x}{x}\right|_1^{+\infty}
+\int_1^{+\infty}\frac{dx}{x(1+x^2)}\\
&=\frac\pi4+\int_1^{+\infty}\left(\frac1x-\frac{x}{1+x^2}\right)dx.
\end{aligned}
$$
后一积分为
$$
\begin{aligned}
 \int_1^{+\infty}\left(\frac1x-\frac{x}{1+x^2}\right)dx
&=\left[\ln x-\frac12\ln(1+x^2)\right]_1^{+\infty}\\
&=0-\left(-\frac12\ln2\right)\\
&=\frac12\ln2.
\end{aligned}
$$
因此
$$
 I=\frac\pi4+\frac12\ln2.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. **【2000-4-6 分】** 计算

$$
 I=\int_{1}^{+\infty}\frac{dx}{e^{1+x}+e^{3-x}}.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\pi}{4e^2}$。</ExamAnswer>

<ExamExplanation>
令
$$
 t=e^{x-1},
$$
则
$$
 dx=\frac{dt}{t}.
$$
当 $ x=1$ 时，$ t=1$；当 $ x\to+\infty$ 时，$ t\to+\infty$。并且
$$
\begin{aligned}
 e^{1+x}&=e^2t,\\
e^{3-x}&=\frac{e^2}{t}.
\end{aligned}
$$
因此
$$
\begin{aligned}
 I
&=\int_1^{+\infty}\frac{1}{e^2\left(t+\frac1t\right)}\cdot\frac{dt}{t}\\
&=\frac1{e^2}\int_1^{+\infty}\frac{dt}{1+t^2}\\
&=\frac1{e^2}\left[\arctan t\right]_1^{+\infty}\\
&=\frac1{e^2}\left(\frac\pi2-\frac\pi4\right)\\
&=\frac{\pi}{4e^2}.
\end{aligned}
$$

本题的关键是令 $ t=e^{x-1}$，使分母中的两个指数项分别化为 $ e^2t$ 与 $ \frac{e^2}{t}$，从而转化为反正切型积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. **【2008-2-9 分】** 求积分

$$
 \int_{0}^{1}\frac{x^{2}\arcsin x}{\sqrt{1-x^{2}}}\,dx.
$$

<ExamSolution>

<ExamAnswer>$ \frac{\pi^2+4}{16}$。</ExamAnswer>

<ExamExplanation>
令
$$
 x=\sin t,
$$
则
$$
 dx=\cos t\,dt,
\qquad
\sqrt{1-x^2}=\cos t,
\qquad
\arcsin x=t.
$$
当 $ x=0$ 时，$ t=0$；当 $ x=1$ 时，$ t=\frac\pi2$。故原积分化为
$$
 I=\int_0^{\frac\pi2}t\sin^2t\,dt.
$$
利用
$$
 \sin^2t=\frac{1-\cos2t}{2},
$$
得
$$
\begin{aligned}
 I
&=\frac12\int_0^{\frac\pi2}t\,dt
-\frac12\int_0^{\frac\pi2}t\cos2t\,dt.
\end{aligned}
$$
第一项为
$$
 \frac12\int_0^{\frac\pi2}t\,dt
=\frac12\cdot\frac12\left(\frac\pi2\right)^2
=\frac{\pi^2}{16}.
$$
对第二个积分分部积分：
$$
\begin{aligned}
 \int_0^{\frac\pi2}t\cos2t\,dt
&=\left.\frac{t\sin2t}{2}\right|_0^{\frac\pi2}
-\frac12\int_0^{\frac\pi2}\sin2t\,dt\\
&=0-\frac12
=-\frac12.
\end{aligned}
$$
因此
$$
\begin{aligned}
 I
&=\frac{\pi^2}{16}-\frac12\left(-\frac12\right)\\
&=\frac{\pi^2}{16}+\frac14\\
&=\frac{\pi^2+4}{16}.
\end{aligned}
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>
