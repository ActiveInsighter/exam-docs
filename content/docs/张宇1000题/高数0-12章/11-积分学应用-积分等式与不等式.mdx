---
title: "第11章 一元函数积分学的应用(二)——积分等式与积分不等式"
---

# 第11章 一元函数积分学的应用(二)——积分等式与积分不等式

## 基础部分

<ExamQuestion>

1. 设 $a>0$，则在 $[0,a]$ 上方程

$\displaystyle \int_{0}^{x}\sqrt{4a^{2}-t^{2}}\,dt+\int_{a}^{x}\dfrac{1}{\sqrt{4a^{2}-t^{2}}}\,dt=0$


的实根个数为（ ）

<ExamChoices>
<ExamOption>A. 0</ExamOption>
<ExamOption>B. 1</ExamOption>
<ExamOption>C. 2</ExamOption>
<ExamOption>D. 3</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
令

$\displaystyle F(x)=\int_{0}^{x}\sqrt{4a^{2}-t^{2}}\,dt+\int_{a}^{x}\dfrac{1}{\sqrt{4a^{2}-t^{2}}}\,dt$。

在 $[0,a]$ 上，$\displaystyle 4a^{2}-x^{2}\geq3a^{2}>0$，故

$\displaystyle F'(x)=\sqrt{4a^{2}-x^{2}}+\dfrac{1}{\sqrt{4a^{2}-x^{2}}}>0$。

所以 $F(x)$ 在 $[0,a]$ 上严格增加。又

$\displaystyle F(0)=-\int_{0}^{a}\dfrac{dt}{\sqrt{4a^{2}-t^{2}}}<0$，

$\displaystyle F(a)=\int_{0}^{a}\sqrt{4a^{2}-t^{2}}\,dt>0$。

由连续函数零点定理，方程在 $(0,a)$ 内至少有一个实根；由严格单调性知该实根唯一。因此实根个数为 $1$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

2. 设函数 $f(x)$ 具有二阶导数，$f'(x)>0$，$f''(x)<0$，记

$\displaystyle I_1=\int_{-\pi}^{\pi}f(x)\sin x\,dx$，

$\displaystyle I_2=\int_{-\pi}^{\pi}f(x)\cos x\,dx$，

则（ ）

<ExamChoices>
<ExamOption>A. $\displaystyle I_1>0,I_2<0$</ExamOption>
<ExamOption>B. $\displaystyle I_1<0,I_2>0$</ExamOption>
<ExamOption>C. $\displaystyle I_1<0,I_2<0$</ExamOption>
<ExamOption>D. $\displaystyle I_1>0,I_2>0$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>D。</ExamAnswer>

<ExamExplanation>
由于 $f'(x)>0$，函数 $f$ 严格增加。将 $I_1$ 的负半轴积分变换到正半轴，得

$\displaystyle I_1=\int_{0}^{\pi}[f(x)-f(-x)]\sin x\,dx$。

当 $x\in(0,\pi)$ 时，$f(x)>f(-x)$ 且 $\sin x>0$，所以 $I_1>0$。

对 $I_2$ 分部积分：

$\displaystyle I_2=\left.f(x)\sin x\right|_{-\pi}^{\pi}-\int_{-\pi}^{\pi}f'(x)\sin x\,dx$，

从而

$\displaystyle I_2=-\int_{0}^{\pi}[f'(x)-f'(-x)]\sin x\,dx$。

由于 $f''(x)<0$，故 $f'(x)$ 严格减少。对 $x>0$，有 $f'(x)<f'(-x)$，因此被积函数中方括号为负，进而 $I_2>0$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

3. 设 $f(x)$ 为连续函数，$T>0$，证明：$f(x)$ 以 $T$ 为周期的充分必要条件是任给常数 $a$，$\displaystyle \int_{a}^{a+T}f(x)\,dx$ 为常数。

<ExamSolution>

<ExamAnswer>该命题成立。</ExamAnswer>

<ExamExplanation>
**必要性：** 若 $f(x+T)=f(x)$，则令

$\displaystyle F(a)=\int_{a}^{a+T}f(x)\,dx$。

由变上、下限积分求导公式，

$\displaystyle F'(a)=f(a+T)-f(a)=0$，

故 $F(a)$ 为常数。

**充分性：** 若对任意 $a$，$\displaystyle F(a)=\int_{a}^{a+T}f(x)\,dx$ 均为同一常数，则 $F'(a)=0$。由连续性，

$\displaystyle 0=F'(a)=f(a+T)-f(a)$，

所以对任意 $a$ 均有 $f(a+T)=f(a)$，即 $f$ 以 $T$ 为周期。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

4. 设 $f(x)$ 连续，证明：

$\displaystyle \int_{0}^{x}\left[\int_{0}^{t}f(u)\,du\right]dt=\int_{0}^{x}f(t)(x-t)\,dt$。

<ExamSolution>

<ExamAnswer>该恒等式成立。</ExamAnswer>

<ExamExplanation>
令

$\displaystyle F(t)=\int_{0}^{t}f(u)\,du$，则 $F'(t)=f(t)$，且 $F(0)=0$。

对左端分部积分：

$\displaystyle \int_{0}^{x}F(t)\,dt=\left.tF(t)\right|_{0}^{x}-\int_{0}^{x}tF'(t)\,dt$，

即

$\displaystyle \int_{0}^{x}F(t)\,dt=x\int_{0}^{x}f(t)\,dt-\int_{0}^{x}tf(t)\,dt$。

因此

$\displaystyle \int_{0}^{x}F(t)\,dt=\int_{0}^{x}(x-t)f(t)\,dt$，

结论得证。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

5. 设 $f(x),g(x)$ 连续，$x\in[a,b]$，证明至少存在一点 $\xi\in(a,b)$，使

$\displaystyle f(\xi)\int_{a}^{\xi}g(t)\,dt=g(\xi)\int_{\xi}^{b}f(t)\,dt$。

<ExamSolution>

<ExamAnswer>至少存在一个满足条件的 $\xi\in(a,b)$。</ExamAnswer>

<ExamExplanation>
令

$\displaystyle F(x)=\int_{a}^{x}g(t)\,dt$，

$\displaystyle G(x)=\int_{x}^{b}f(t)\,dt$。

则 $F,G$ 在 $[a,b]$ 上连续、在 $(a,b)$ 内可导，并且

$\displaystyle F(a)G(a)=0$，$\displaystyle F(b)G(b)=0$。

对函数 $H(x)=F(x)G(x)$ 应用罗尔定理，存在 $\xi\in(a,b)$，使 $H'(\xi)=0$。而

$\displaystyle H'(x)=g(x)\int_{x}^{b}f(t)\,dt-f(x)\int_{a}^{x}g(t)\,dt$。

故 $H'(\xi)=0$ 等价于

$\displaystyle f(\xi)\int_{a}^{\xi}g(t)\,dt=g(\xi)\int_{\xi}^{b}f(t)\,dt$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

6. 证明：

$\displaystyle \int_{0}^{+\infty}\dfrac{dx}{1+x^4}=\int_{0}^{+\infty}\dfrac{x^2}{1+x^4}\,dx=\dfrac{\sqrt2}{4}\pi$。

<ExamSolution>

<ExamAnswer>两积分均为 $\displaystyle \dfrac{\sqrt2\pi}{4}$。</ExamAnswer>

<ExamExplanation>
记

$\displaystyle I_0=\int_{0}^{+\infty}\dfrac{dx}{1+x^4}$，

$\displaystyle I_2=\int_{0}^{+\infty}\dfrac{x^2}{1+x^4}\,dx$。

在 $I_0$ 中作代换 $x=\dfrac1t$，则

$\displaystyle I_0=\int_{0}^{+\infty}\dfrac{t^2}{1+t^4}\,dt=I_2$。

于是

$\displaystyle 2I_0=\int_{0}^{+\infty}\dfrac{1+x^2}{1+x^4}\,dx$。

因

$\displaystyle 1+x^4=(x^2+\sqrt2x+1)(x^2-\sqrt2x+1)$，

且

$\displaystyle \dfrac{1+x^2}{1+x^4}=\dfrac12\left[\dfrac1{x^2+\sqrt2x+1}+\dfrac1{x^2-\sqrt2x+1}\right]$，

故

$\displaystyle 2I_0=\dfrac12\int_{0}^{+\infty}\left[\dfrac1{(x+\frac1{\sqrt2})^2+\frac12}+\dfrac1{(x-\frac1{\sqrt2})^2+\frac12}\right]dx$。

分别积分得

$\displaystyle 2I_0=\dfrac{\pi}{\sqrt2}$，

从而

$\displaystyle I_0=I_2=\dfrac{\pi}{2\sqrt2}=\dfrac{\sqrt2\pi}{4}$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

7. 已知函数 $f(x),g(x)$ 可导，且 $f'(x)>0$，$g'(x)<0$，则（ ）

<ExamChoices>
<ExamOption>A. $\displaystyle \int_{-1}^{0}f(x)g(x)\,dx>\int_{0}^{1}f(x)g(x)\,dx$</ExamOption>
<ExamOption>B. $\displaystyle \int_{-1}^{0}|f(x)g(x)|\,dx>\int_{0}^{1}|f(x)g(x)|\,dx$</ExamOption>
<ExamOption>C. $\displaystyle \int_{-1}^{0}f[g(x)]\,dx>\int_{0}^{1}f[g(x)]\,dx$</ExamOption>
<ExamOption>D. $\displaystyle \int_{-1}^{0}f[f(x)]\,dx>\int_{0}^{1}g[g(x)]\,dx$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>C。</ExamAnswer>

<ExamExplanation>
令 $h(x)=f[g(x)]$。由复合函数求导法则，

$\displaystyle h'(x)=f'[g(x)]g'(x)<0$，

所以 $h(x)$ 严格减少。于是对任意 $t\in(0,1)$，有 $-t<t$，从而 $h(-t)>h(t)$。因此

$\displaystyle \int_{-1}^{0}h(x)\,dx-\int_{0}^{1}h(x)\,dx=\int_{0}^{1}[h(-t)-h(t)]\,dt>0$。

故选 C。其余各项不能由题设单调性必然推出。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

8. 设 $f(x),g(x)$ 在 $[0,1]$ 上连续，则使得

$\displaystyle \int_{0}^{1}f(x)\,dx\int_{0}^{1}g(x)\,dx\geq\int_{0}^{1}f(x)g(x)\,dx$

成立的条件是（ ）

<ExamChoices>
<ExamOption>A. $f(x),g(x)$ 均为增函数</ExamOption>
<ExamOption>B. $f(x),g(x)$ 均为减函数</ExamOption>
<ExamOption>C. $f(x)$ 为减函数，$g(x)$ 为偶函数</ExamOption>
<ExamOption>D. $f(x)$ 为奇函数，$g(x)$ 为增函数</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>按原题文字，四个选项均不能保证结论成立。若 C 项中的“偶函数”系“增函数”之误，则应选 C。</ExamAnswer>

<ExamExplanation>
在区间长度为 $1$ 时，有恒等式

$\displaystyle \int_{0}^{1}f(x)g(x)\,dx-\int_{0}^{1}f(x)\,dx\int_{0}^{1}g(x)\,dx$

$\displaystyle =\dfrac12\int_{0}^{1}\int_{0}^{1}[f(x)-f(y)][g(x)-g(y)]\,dx\,dy$。

若 $f,g$ 同向单调，则右端非负，因此 A、B 一般给出的是

$\displaystyle \int_{0}^{1}f(x)g(x)\,dx\geq\int_{0}^{1}f(x)\,dx\int_{0}^{1}g(x)\,dx$，

与题设方向相反。

C 按原文仅要求 $g$ 为偶函数，不能保证其在 $[0,1]$ 上与 $f$ 反向单调。例如取

$\displaystyle f(x)=1-x$，$\displaystyle g(x)=-x^2$，

则 $f$ 为减函数，$g$ 为偶函数且在 $[0,1]$ 上也为减函数，而

$\displaystyle \int_{0}^{1}f\,dx\int_{0}^{1}g\,dx=-\dfrac16<-\dfrac1{12}=\int_{0}^{1}fg\,dx$，

题设不等式不成立。D 同样缺少足够条件。

若 C 项原意为“$f$ 为减函数，$g$ 为增函数”，则两函数反向单调，双重积分右端非正，题设不等式成立。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

9. 设 $f'(x)$ 在 $[a,b]$ 上连续，$f(a)=f(b)=0$，证明：当 $x\in(a,b)$ 时，

$\displaystyle |f(x)|\leq\dfrac12\int_{a}^{b}|f'(t)|\,dt$。

<ExamSolution>

<ExamAnswer>不等式成立。</ExamAnswer>

<ExamExplanation>
由 $f(a)=0$，

$\displaystyle f(x)=\int_{a}^{x}f'(t)\,dt$，

故

$\displaystyle |f(x)|\leq\int_{a}^{x}|f'(t)|\,dt$。

又由 $f(b)=0$，

$\displaystyle f(x)=-\int_{x}^{b}f'(t)\,dt$，

故

$\displaystyle |f(x)|\leq\int_{x}^{b}|f'(t)|\,dt$。

记

$\displaystyle A=\int_{a}^{x}|f'(t)|\,dt$，$\displaystyle B=\int_{x}^{b}|f'(t)|\,dt$，

则 $|f(x)|\leq\min\{A,B\}$。而

$\displaystyle \min\{A,B\}\leq\dfrac{A+B}{2}=\dfrac12\int_{a}^{b}|f'(t)|\,dt$，

结论得证。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

10. 设 $f'(x)$ 在 $[0,1]$ 上连续，且 $f(0)=0$，证明：

$\displaystyle \left|\int_{0}^{1}f(x)\,dx\right|\leq\dfrac{M}{2}$，

其中 $\displaystyle M=\max_{0\leq x\leq1}|f'(x)|$。

<ExamSolution>

<ExamAnswer>不等式成立。</ExamAnswer>

<ExamExplanation>
由 $f(0)=0$，对 $x\in[0,1]$ 有

$\displaystyle f(x)=\int_{0}^{x}f'(t)\,dt$，

故

$\displaystyle |f(x)|\leq\int_{0}^{x}|f'(t)|\,dt\leq Mx$。

因此

$\displaystyle \left|\int_{0}^{1}f(x)\,dx\right|\leq\int_{0}^{1}|f(x)|\,dx\leq M\int_{0}^{1}x\,dx=\dfrac M2$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

11. 设

$\displaystyle f(x)=\int_{x}^{x+1}\sin u^2\,du$，

证明：当 $x>0$ 时，有

$\displaystyle |f(x)|\leq\dfrac1x$。

<ExamSolution>

<ExamAnswer>不等式成立。</ExamAnswer>

<ExamExplanation>
利用

$\displaystyle d(\cos u^2)=-2u\sin u^2\,du$，

可写成

$\displaystyle \sin u^2\,du=-\dfrac1{2u}\,d(\cos u^2)$。

分部积分得

$\displaystyle f(x)=\left.-\dfrac{\cos u^2}{2u}\right|_{x}^{x+1}-\int_{x}^{x+1}\dfrac{\cos u^2}{2u^2}\,du$。

于是

$\displaystyle |f(x)|\leq\dfrac1{2(x+1)}+\dfrac1{2x}+\dfrac12\int_{x}^{x+1}\dfrac{du}{u^2}$。

而

$\displaystyle \dfrac12\int_{x}^{x+1}\dfrac{du}{u^2}=\dfrac12\left(\dfrac1x-\dfrac1{x+1}\right)$，

故

$\displaystyle |f(x)|\leq\dfrac1x$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 强化部分

<ExamQuestion>

1. 设 $f(x)$ 在 $[0,1]$ 上可导，当 $0\leq x\leq1$ 时，$f'(x)+f^2(x)\geq0$，$f(0)>0$，则（ ）

<ExamChoices>
<ExamOption>A. $\displaystyle \int_{0}^{1}f(x)\,dx\leq\ln\dfrac{f(1)}{f(0)}$</ExamOption>
<ExamOption>B. $\displaystyle \int_{0}^{1}f(x)\,dx\geq\ln\dfrac{f(0)}{f(1)}$</ExamOption>
<ExamOption>C. $\displaystyle \int_{0}^{1}f(x)\,dx\leq\ln f(1)$</ExamOption>
<ExamOption>D. $\displaystyle \int_{0}^{1}f(x)\,dx\geq\ln f(0)$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
先证明 $f(x)>0$。令

$\displaystyle \Phi(x)=f(x)e^{\int_{0}^{x}f(t)\,dt}$。

则

$\displaystyle \Phi'(x)=e^{\int_{0}^{x}f(t)\,dt}[f'(x)+f^2(x)]\geq0$。

所以 $\Phi(x)\geq\Phi(0)=f(0)>0$，从而 $f(x)>0$。

因此可以将题设不等式除以 $f(x)$：

$\displaystyle \dfrac{f'(x)}{f(x)}+f(x)\geq0$。

在 $[0,1]$ 上积分，得

$\displaystyle \ln f(1)-\ln f(0)+\int_{0}^{1}f(x)\,dx\geq0$，

即

$\displaystyle \int_{0}^{1}f(x)\,dx\geq\ln\dfrac{f(0)}{f(1)}$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

2. 设函数 $f(x)$ 是 $[0,1]$ 上的连续函数，利用分部积分法证明：

$\displaystyle \int_{0}^{1}\left[\int_{x^2}^{\sqrt{x}}f(t)\,dt\right]dx=\int_{0}^{1}(\sqrt{x}-x^2)f(x)\,dx$。

<ExamSolution>

<ExamAnswer>该恒等式成立。</ExamAnswer>

<ExamExplanation>
令

$\displaystyle F(x)=\int_{0}^{x}f(t)\,dt$。

则左端为

$\displaystyle I=\int_{0}^{1}F(\sqrt{x})\,dx-\int_{0}^{1}F(x^2)\,dx$。

第一项令 $x=u^2$，得

$\displaystyle \int_{0}^{1}F(\sqrt{x})\,dx=2\int_{0}^{1}uF(u)\,du$。

分部积分：

$\displaystyle 2\int_{0}^{1}uF(u)\,du=\left.u^2F(u)\right|_{0}^{1}-\int_{0}^{1}u^2f(u)\,du$。

第二项令 $u=x^2$，得

$\displaystyle \int_{0}^{1}F(x^2)\,dx=\dfrac12\int_{0}^{1}u^{-\frac12}F(u)\,du$。

再次分部积分：

$\displaystyle \dfrac12\int_{0}^{1}u^{-\frac12}F(u)\,du=\left.\sqrt{u}F(u)\right|_{0}^{1}-\int_{0}^{1}\sqrt{u}f(u)\,du$。

两式相减，边界项相消，故

$\displaystyle I=\int_{0}^{1}(\sqrt{u}-u^2)f(u)\,du$。

将积分变量 $u$ 改写为 $x$ 即得结论。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

3. 设 $f(x)$ 是 $[0,1]$ 上的可导函数，$f(0)=f(1)=1$，$\displaystyle \max_{0\leq x\leq1}|f'(x)|=1$，则（ ）

<ExamChoices>
<ExamOption>A. $\displaystyle \dfrac14<\int_{0}^{1}f(x)\,dx<\dfrac12$</ExamOption>
<ExamOption>B. $\displaystyle \dfrac12<\int_{0}^{1}f(x)\,dx<\dfrac34$</ExamOption>
<ExamOption>C. $\displaystyle \dfrac34<\int_{0}^{1}f(x)\,dx<\dfrac54$</ExamOption>
<ExamOption>D. $\displaystyle \dfrac54<\int_{0}^{1}f(x)\,dx<\dfrac74$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>C。</ExamAnswer>

<ExamExplanation>
由 $|f'(x)|\leq1$ 及 $f(0)=1$，

$\displaystyle 1-x\leq f(x)\leq1+x$。

由 $f(1)=1$，

$\displaystyle x\leq f(x)\leq2-x$。

所以

$\displaystyle \max\{x,1-x\}\leq f(x)\leq\min\{1+x,2-x\}$。

积分得

$\displaystyle \int_{0}^{1}\max\{x,1-x\}\,dx=\dfrac34$，

$\displaystyle \int_{0}^{1}\min\{1+x,2-x\}\,dx=\dfrac54$。

若左端取等号，则必须有 $f(x)=\max\{x,1-x\}$，该函数在 $x=\dfrac12$ 处不可导，与题设矛盾；右端取等号时同理。因此

$\displaystyle \dfrac34<\int_{0}^{1}f(x)\,dx<\dfrac54$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

4. 证明：

$\displaystyle \int_{0}^{1}\left(\int_{x}^{\sqrt{x}}\dfrac{\sin t}{t}\,dt\right)dx=1-\sin1$。

<ExamSolution>

<ExamAnswer>积分值为 $\displaystyle 1-\sin1$。</ExamAnswer>

<ExamExplanation>
积分区域为

$\displaystyle D=\{(x,t):0\leq x\leq1,\ x\leq t\leq\sqrt{x}\}$。

由 $x\leq t$ 与 $t\leq\sqrt{x}$ 可得

$\displaystyle t^2\leq x\leq t$，$\displaystyle 0\leq t\leq1$。

交换积分次序：

$\displaystyle I=\int_{0}^{1}\left[\int_{t^2}^{t}dx\right]\dfrac{\sin t}{t}\,dt$，

故

$\displaystyle I=\int_{0}^{1}(t-t^2)\dfrac{\sin t}{t}\,dt=\int_{0}^{1}(1-t)\sin t\,dt$。

再计算

$\displaystyle \int_{0}^{1}(1-t)\sin t\,dt=1-\sin1$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

5. 设函数 $f(x),g(x)$ 在区间 $[a,b]$ 上连续，对任意 $x\in[a,b]$，满足

$\displaystyle \int_{a}^{x}f(t)\,dt\leq\int_{a}^{x}g(t)\,dt$，

证明：

$\displaystyle \int_{a}^{b}xf(x)\,dx\leq\int_{a}^{b}xg(x)\,dx$。

<ExamSolution>

<ExamAnswer>原命题按现有条件不成立，无法证明。</ExamAnswer>

<ExamExplanation>
取区间 $[a,b]=[0,1]$，令

$\displaystyle f(x)=0$，$\displaystyle g(x)=1-2x$。

则对任意 $x\in[0,1]$，

$\displaystyle \int_{0}^{x}f(t)\,dt=0$，

而

$\displaystyle \int_{0}^{x}g(t)\,dt=x-x^2\geq0$，

所以题设条件成立。

但是

$\displaystyle \int_{0}^{1}xf(x)\,dx=0$，

$\displaystyle \int_{0}^{1}xg(x)\,dx=\int_{0}^{1}(x-2x^2)\,dx=-\dfrac16$。

题目所要求的 $\displaystyle 0\leq-\dfrac16$ 显然不成立。因此原命题缺少必要条件或不等号方向有误。

若另加

$\displaystyle \int_{a}^{b}f(t)\,dt=\int_{a}^{b}g(t)\,dt$，

则分部积分反而得到

$\displaystyle \int_{a}^{b}xf(x)\,dx\geq\int_{a}^{b}xg(x)\,dx$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

6. 设 $f(x)$ 在 $[a,b]$ 上可导，且 $f'(x)$ 连续，$f(a)=0$，证明：

$\displaystyle \int_{a}^{b}f^2(x)\,dx\leq\dfrac{(b-a)^2}{2}\int_{a}^{b}[f'(x)]^2\,dx$。

<ExamSolution>

<ExamAnswer>不等式成立。</ExamAnswer>

<ExamExplanation>
由 $f(a)=0$，

$\displaystyle f(x)=\int_{a}^{x}f'(t)\,dt$。

由柯西—施瓦茨不等式，

$\displaystyle f^2(x)\leq(x-a)\int_{a}^{x}[f'(t)]^2\,dt$。

进一步有

$\displaystyle f^2(x)\leq(x-a)\int_{a}^{b}[f'(t)]^2\,dt$。

两端在 $[a,b]$ 上积分：

$\displaystyle \int_{a}^{b}f^2(x)\,dx\leq\left[\int_{a}^{b}(x-a)\,dx\right]\int_{a}^{b}[f'(t)]^2\,dt$。

而

$\displaystyle \int_{a}^{b}(x-a)\,dx=\dfrac{(b-a)^2}{2}$，

故结论成立。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

7. 设 $f(x)$ 是 $[0,1]$ 上单调增加的连续函数，则（ ）

<ExamChoices>
<ExamOption>A. $\displaystyle \int_{0}^{\int_{0}^{1}e^{-x^2}\,dx}f(x)\,dx\geq\int_{0}^{1}f(x)e^{-x^2}\,dx$</ExamOption>
<ExamOption>B. $\displaystyle \int_{0}^{\int_{0}^{1}e^{-x^2}\,dx}f(x)\,dx\leq\int_{0}^{1}f(x)e^{-x^2}\,dx$</ExamOption>
<ExamOption>C. $\displaystyle \int_{0}^{\int_{0}^{1}e^{-x^2}\,dx}f(x)\,dx\geq\int_{0}^{1}f(x)\,dx$</ExamOption>
<ExamOption>D. $\displaystyle \int_{0}^{\int_{0}^{1}e^{-x^2}\,dx}f(x)\,dx\leq\int_{0}^{1}f(x)\,dx$</ExamOption>
</ExamChoices>

<ExamSolution>

<ExamAnswer>B。</ExamAnswer>

<ExamExplanation>
记

$\displaystyle c=\int_{0}^{1}e^{-x^2}\,dx$，则 $0<c<1$。

令

$\displaystyle w(x)=e^{-x^2}-\mathbf{1}_{[0,c]}(x)$。

由于

$\displaystyle \int_{0}^{1}w(x)\,dx=c-c=0$，

故

$\displaystyle \int_{0}^{1}f(x)e^{-x^2}\,dx-\int_{0}^{c}f(x)\,dx=\int_{0}^{1}[f(x)-f(c)]w(x)\,dx$。

当 $0\leq x\leq c$ 时，$f(x)-f(c)\leq0$，且 $w(x)=e^{-x^2}-1\leq0$；当 $c\leq x\leq1$ 时，$f(x)-f(c)\geq0$，且 $w(x)=e^{-x^2}>0$。

因此右端非负，即

$\displaystyle \int_{0}^{c}f(x)\,dx\leq\int_{0}^{1}f(x)e^{-x^2}\,dx$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

8. 设 $f(x)$ 在 $[0,\dfrac{3\pi}{2}]$ 上连续，在 $(0,\dfrac{3\pi}{2})$ 内是函数 $\sin x$ 的一个原函数，$f(0)=0$。

（1）证明 $\displaystyle f\left(\dfrac{3\pi}{2}\right)>0$；

（2）求方程

$\displaystyle \int_{1}^{x}\dfrac{\sin t}{t}\,dt=\ln x^2$

的实根个数。

<ExamSolution>

<ExamAnswer>
（1）$\displaystyle f\left(\dfrac{3\pi}{2}\right)=1>0$；

（2）实根个数为 $2$。
</ExamAnswer>

<ExamExplanation>
（1）在 $(0,\dfrac{3\pi}{2})$ 内有 $f'(x)=\sin x$。由连续性及 $f(0)=0$，

$\displaystyle f(x)=\int_{0}^{x}\sin t\,dt=1-\cos x$。

因此

$\displaystyle f\left(\dfrac{3\pi}{2}\right)=1-\cos\dfrac{3\pi}{2}=1>0$。

（2）定义

$\displaystyle H(x)=\int_{1}^{x}\dfrac{\sin t}{t}\,dt-2\ln|x|$，$\displaystyle x\neq0$。

则

$\displaystyle H'(x)=\dfrac{\sin x-2}{x}$。

当 $x>0$ 时，$\sin x-2<0$，故 $H'(x)<0$，即 $H$ 在 $(0,+\infty)$ 上严格减少。又 $H(1)=0$，所以正根只有 $x=1$。

当 $x<0$ 时，$\sin x-2<0$ 且 $x<0$，故 $H'(x)>0$，即 $H$ 在 $(-\infty,0)$ 上严格增加。由于 $\displaystyle \int_{1}^{x}\dfrac{\sin t}{t}\,dt$ 在 $x\to-\infty$ 时有有限极限，而 $-2\ln|x|\to-\infty$，故

$\displaystyle \lim_{x\to-\infty}H(x)=-\infty$。

又当 $x\to0^-$ 时，积分项有有限极限，而 $-2\ln|x|\to+\infty$，故

$\displaystyle \lim_{x\to0^-}H(x)=+\infty$。

由连续性和严格单调性，负半轴上恰有一个根。故实根总数为 $2$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

<ExamQuestion>

9. 设 $f(x)$ 在 $[0,1]$ 上具有二阶导数，$f(0)=f(1)=0$，$f''(x)<0$，$0\leq f(x)\leq1$，记曲线 $y=f(x)$ 在 $[0,1]$ 上的长度为 $a$，证明：

（1）存在 $\xi\in(0,1)$，使得对任意 $x\in(0,\xi)$，有 $f'(x)>0$；

（2）$a<3$。

<ExamSolution>

<ExamAnswer>两结论均成立。</ExamAnswer>

<ExamExplanation>
（1）由 $f(0)=f(1)=0$，罗尔定理保证存在 $\xi\in(0,1)$，使 $f'(\xi)=0$。

又因 $f''(x)<0$，所以 $f'(x)$ 严格减少。于是当 $0<x<\xi$ 时，

$\displaystyle f'(x)>f'(\xi)=0$。

（2）由于 $f'$ 严格减少且在 $\xi$ 处为零，故 $f$ 在 $[0,\xi]$ 上增加，在 $[\xi,1]$ 上减少。记

$\displaystyle M=f(\xi)=\max_{[0,1]}f(x)$，

则 $0<M\leq1$。曲线弧长为

$\displaystyle a=\int_{0}^{1}\sqrt{1+[f'(x)]^2}\,dx$。

利用

$\displaystyle \sqrt{1+u^2}<1+|u|$，$\displaystyle u\neq0$，

并注意 $f'$ 不恒为零，得

$\displaystyle a<1+\int_{0}^{1}|f'(x)|\,dx$。

由于 $f$ 先增后减，

$\displaystyle \int_{0}^{1}|f'(x)|\,dx=[f(\xi)-f(0)]+[f(\xi)-f(1)]=2M\leq2$。

因此

$\displaystyle a<1+2=3$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>