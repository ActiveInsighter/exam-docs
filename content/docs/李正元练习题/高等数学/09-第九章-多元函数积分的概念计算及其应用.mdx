---
sidebar_position: 9
title: 第九章 多元函数积分的概念、计算及其应用
---

{/* 此文件由 scripts/convert-lizhengyuan-exercises.mjs 自动生成，请勿直接修改。 */}

# 第九章 多元函数积分的概念、计算及其应用

## 填空题

<ExamQuestion>

1. 设 $ D$ 为两个圆

$$
 x^2+y^2\le1
$$


及

$$
 (x-2)^2+y^2\le4
$$


的公共部分，则

$$
 I=\iint_D y\,d\sigma=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{0}
$$
</ExamAnswer>

<ExamExplanation>
两个圆域都关于 $ x$ 轴对称，因此它们的公共部分 $ D$ 也关于 $ x$ 轴对称。

被积函数 $ y$ 关于变量 $ y$ 为奇函数，即

$$
 y(x,-y)=-y(x,y).
$$

根据二重积分的对称性，在关于 $ x$ 轴对称的区域上，对 $ y$ 为奇函数的积分为零，故

$$
 I=\iint_D y\,d\sigma=0.
$$

相关知识点：利用积分区域与被积函数的对称性，可避免直接求出两个圆的交点及公共区域的边界。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 设 $ D$ 为 $ y=x^3$ 及 $ x=-1,y=1$ 所围成的区域，则

$$
 I=\iint_D xy\,d\sigma=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{0}
$$
</ExamAnswer>

<ExamExplanation>
曲线 $ y=x^3$ 与直线 $ y=1$ 相交于 $ x=1$。因此区域可表示为

$$
 D=\{(x,y)\mid -1\le x\le1,\ x^3\le y\le1\}.
$$

于是

$$
 \begin{aligned}
I
&=\int_{-1}^{1}\int_{x^3}^{1}xy\,dy\,dx\\
&=\int_{-1}^{1}x\left[\frac{y^2}{2}\right]_{x^3}^{1}dx\\
&=\frac12\int_{-1}^{1}x(1-x^6)\,dx.
\end{aligned}
$$

其中 $ x(1-x^6)$ 是关于 $ x$ 的奇函数，积分区间 $ [-1,1]$ 关于原点对称，所以

$$
 I=0.
$$

相关知识点：先根据边界曲线确定积分区域，再利用奇偶性简化一元积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 题目如下：

$$
 I=\iint_{|x|+|y|\le1}|xy|\,dxdy=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{\frac16}
$$
</ExamAnswer>

<ExamExplanation>
区域 $ |x|+|y|\le1$ 关于两坐标轴均对称，而 $ |xy|$ 在四个象限取值相同，因此只需计算第一象限部分再乘以 $ 4$。

第一象限内区域为

$$
 0\le x\le1,\qquad 0\le y\le1-x.
$$

故

$$
 \begin{aligned}
I
&=4\int_0^1\int_0^{1-x}xy\,dy\,dx\\
&=4\int_0^1x\left[\frac{y^2}{2}\right]_0^{1-x}dx\\
&=2\int_0^1x(1-x)^2\,dx\\
&=2\int_0^1(x-2x^2+x^3)\,dx\\
&=2\left(\frac12-\frac23+\frac14\right)\\
&=\frac16.
\end{aligned}
$$

相关知识点：含绝对值的被积函数通常可结合区域对称性分象限处理。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 设

$$
 D:0\le x\le1,
\qquad
0\le y\le1,
$$


则

$$
 I=\iint_D\frac{e^{x^2}}{e^{x^2}+e^{y^2}}\,d\sigma
=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{\frac12}
$$
</ExamAnswer>

<ExamExplanation>
记

$$
 I=\iint_D\frac{e^{x^2}}{e^{x^2}+e^{y^2}}\,d\sigma.
$$

区域 $ D=[0,1]\times[0,1]$ 关于直线 $ y=x$ 对称。交换积分变量 $ x,y$，有

$$
 I=\iint_D\frac{e^{y^2}}{e^{x^2}+e^{y^2}}\,d\sigma.
$$

将两式相加，得到

$$
 \begin{aligned}
2I
&=\iint_D
\frac{e^{x^2}+e^{y^2}}{e^{x^2}+e^{y^2}}\,d\sigma\\
&=\iint_D1\,d\sigma\\
&=S_D=1.
\end{aligned}
$$

所以

$$
 I=\frac12.
$$

相关知识点：若区域关于 $ y=x$ 对称，可通过交换变量构造互补被积函数。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. 设

$$
 I_1=\iint_{x^2+y^2\le1}(x^4+y^4)\,d\sigma,
$$


$$
 I_2=\iint_{\substack{|x|\le1\\|y|\le1}}(x^4+y^4)\,d\sigma,
$$


$$
 I_3=\iint_{x^2+y^2\le1}2x^2y^2\,d\sigma,
$$


则这三个积分的大小顺序是

$$
 \underline{\qquad}<\underline{\qquad}<\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I_3<I_1<I_2}
$$
</ExamAnswer>

<ExamExplanation>
先比较 $ I_1$ 与 $ I_3$。在单位圆域内恒有

$$
 x^4+y^4-2x^2y^2=(x^2-y^2)^2\ge0.
$$

除去集合 $ x^2=y^2$ 外，上式在区域内严格大于零，而该集合的面积为零。因此

$$
 I_1>I_3.
$$

再比较 $ I_1$ 与 $ I_2$。单位圆域严格包含于正方形区域

$$
 [-1,1]\times[-1,1],
$$

且被积函数 $ x^4+y^4\ge0$，并在正方形减去单位圆的区域内不恒为零，所以

$$
 I_2>I_1.
$$

综上，

$$
 I_3<I_1<I_2.
$$

相关知识点：比较重积分时，可分别利用被积函数的点态大小和积分区域的包含关系。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. 设 $ D$ 为圆域

$$
 x^2+y^2\le x,
$$


则

$$
 I=\iint_D\sqrt{x^2+y^2}\,d\sigma=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{\frac49}
$$
</ExamAnswer>

<ExamExplanation>
采用极坐标变换

$$
 x=r\cos\theta,
\qquad
y=r\sin\theta,
\qquad
d\sigma=r\,dr\,d\theta.
$$

区域条件

$$
 x^2+y^2\le x
$$

化为

$$
 r^2\le r\cos\theta.
$$

由于 $ r\ge0$，故

$$
 0\le r\le\cos\theta,
\qquad
-\frac\pi2\le\theta\le\frac\pi2.
$$

又

$$
 \sqrt{x^2+y^2}=r,
$$

因此

$$
 \begin{aligned}
I
&=\int_{-\frac\pi2}^{\frac\pi2}\int_0^{\cos\theta}r\cdot r\,dr\,d\theta\\
&=\frac13\int_{-\frac\pi2}^{\frac\pi2}\cos^3\theta\,d\theta\\
&=\frac23\int_0^{\frac\pi2}\cos^3\theta\,d\theta\\
&=\frac23\cdot\frac23\\
&=\frac49.
\end{aligned}
$$

相关知识点：圆域 $ x^2+y^2\le x$ 在极坐标下可直接写成 $ 0\le r\le\cos\theta$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. 设 $ L$ 是正方形边界

$$
 |x|+|y|=a\qquad(a>0),
$$


则

$$
 I=\int_Lxy\,ds=\underline{\qquad},
\qquad
J=\int_L|x|\,ds=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=0,
\qquad
J=2\sqrt2\,a^2}
$$
</ExamAnswer>

<ExamExplanation>
正方形边界 $ L$ 关于 $ x$ 轴对称，而函数 $ xy$ 关于 $ y$ 为奇函数；弧长微元 $ ds$ 在对称点处不变。因此

$$
 I=\int_Lxy\,ds=0.
$$

对于 $ J$，由于 $ |x|$ 及边界均关于两坐标轴对称，只需计算第一象限边界再乘以 $ 4$。

第一象限的边为

$$
 y=a-x,
\qquad 0\le x\le a.
$$

于是

$$
 ds=\sqrt{1+\left(\frac{dy}{dx}\right)^2}\,dx
=\sqrt2\,dx.
$$

故

$$
 \begin{aligned}
J
&=4\int_0^a x\sqrt2\,dx\\
&=4\sqrt2\left[\frac{x^2}{2}\right]_0^a\\
&=2\sqrt2\,a^2.
\end{aligned}
$$

相关知识点：第一类曲线积分与曲线方向无关，但可利用曲线及被积函数的对称性。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. 设 $ \Sigma$ 为平面 $ y+z=5$ 被柱面

$$
 x^2+y^2=25
$$


所截得的部分，则曲面积分

$$
 I=\iint_\Sigma(x+y+z)\,dS=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{125\sqrt2\,\pi}
$$
</ExamAnswer>

<ExamExplanation>
平面可写为

$$
 z=5-y.
$$

其在 $ Oxy$ 平面上的投影区域为

$$
 D:x^2+y^2\le25.
$$

由于

$$
 z_x=0,
\qquad
z_y=-1,
$$

所以

$$
 dS=\sqrt{1+z_x^2+z_y^2}\,dxdy
=\sqrt2\,dxdy.
$$

又在曲面上

$$
 x+y+z=x+y+(5-y)=x+5.
$$

因此

$$
 \begin{aligned}
I
&=\sqrt2\iint_D(x+5)\,dxdy\\
&=\sqrt2\left(\iint_Dx\,dxdy+5\iint_D1\,dxdy\right).
\end{aligned}
$$

圆域 $ D$ 关于 $ y$ 轴对称，故

$$
 \iint_Dx\,dxdy=0.
$$

而

$$
 S_D=25\pi.
$$

于是

$$
 I=\sqrt2\cdot5\cdot25\pi
=125\sqrt2\,\pi.
$$

相关知识点：第一类曲面积分化为投影区域上的二重积分时，应乘曲面面积因子。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. 设 $ f(x,y)$ 为二元连续函数，区域

$$
 D(t)=\{(x,y)\mid0\le x\le t^2,\ \sqrt{x}\le y\le t\},
$$


其中 $ t>0$ 为参数，又 $ f(0,0)=6$，则

$$
 I=\lim_{t\to0^+}
\frac{\iint_{D(t)}f(x,y)\,d\sigma}
{\sin t\ln(1+t^2)}
=\underline{\qquad}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{2}
$$
</ExamAnswer>

<ExamExplanation>
当 $ t\to0^+$ 时，区域 $ D(t)$ 收缩到点 $ (0,0)$。由 $ f(x,y)$ 在 $ (0,0)$ 处连续，得

$$
 f(x,y)=6+o(1)
$$

在收缩区域 $ D(t)$ 上一致成立。因此

$$
 \iint_{D(t)}f(x,y)\,d\sigma
=[6+o(1)]S_{D(t)}.
$$

先求区域面积：

$$
 \begin{aligned}
S_{D(t)}
&=\int_0^{t^2}(t-\sqrt{x})\,dx\\
&=t\cdot t^2-\left[\frac23x^{3/2}\right]_0^{t^2}\\
&=t^3-\frac23t^3\\
&=\frac13t^3.
\end{aligned}
$$

所以

$$
 \iint_{D(t)}f(x,y)\,d\sigma
=2t^3+o(t^3).
$$

同时

$$
 \sin t\sim t,
\qquad
\ln(1+t^2)\sim t^2,
$$

故

$$
 \sin t\ln(1+t^2)\sim t^3.
$$

于是

$$
 I=\lim_{t\to0^+}
\frac{2t^3+o(t^3)}{t^3+o(t^3)}=2.
$$

相关知识点：连续函数在收缩区域上的二重积分可用函数值乘区域面积作主项估计。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 求下列二重积分

<ExamQuestion>

1. 计算

$$
 I=\iint_D\sqrt{x}\,dxdy,
$$


其中 $ D$ 为曲线 $ y=\ln x$ 与两直线

$$
 y=0,
\qquad
y=(e+1)-x
$$


所围成的平面区域。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
I=\frac{4}{15}\left[(e+1)^{\frac52}-e^{\frac52}\right]
-\frac49\left(e^{\frac32}-1\right)
}
$$
</ExamAnswer>

<ExamExplanation>
先确定三条边界的交点。

曲线 $ y=\ln x$ 与直线 $ y=0$ 相交于

$$
 (1,0).
$$

直线 $ y=e+1-x$ 与 $ y=0$ 相交于

$$
 (e+1,0).
$$

曲线 $ y=\ln x$ 与直线 $ y=e+1-x$ 相交于

$$
 (e,1),
$$

因为

$$
 \ln e=1=e+1-e.
$$

若先对 $ x$ 积分，则区域可统一表示为

$$
 D=\{(x,y)\mid 0\le y\le1,\ e^y\le x\le e+1-y\}.
$$

因此

$$
 \begin{aligned}
I
&=\int_0^1\int_{e^y}^{e+1-y}\sqrt{x}\,dx\,dy\\
&=\frac23\int_0^1
\left[(e+1-y)^{\frac32}-e^{\frac32y}\right]dy.
\end{aligned}
$$

分别计算：

$$
 \int_0^1(e+1-y)^{\frac32}dy
=\frac25\left[(e+1)^{\frac52}-e^{\frac52}\right],
$$

以及

$$
 \int_0^1e^{\frac32y}dy
=\frac23\left(e^{\frac32}-1\right).
$$

故

$$
 I=\frac{4}{15}\left[(e+1)^{\frac52}-e^{\frac52}\right]
-\frac49\left(e^{\frac32}-1\right).
$$

相关知识点：当区域按 $ x$ 型分割需要分段、而按 $ y$ 型表示可以统一时，应优先交换积分次序以简化计算。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 计算

$$
 I=\iint_Dx^2e^{-y}\,dxdy,
$$


其中 $ D$ 是以 $ O(0,0),A(1,1),B(-1,1)$ 为顶点的三角形区域。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=4-\frac{32}{3e}}
$$
</ExamAnswer>

<ExamExplanation>
三角形的两条斜边分别为

$$
 y=x,
\qquad
y=-x.
$$

按先 $ x$ 后 $ y$ 的次序，区域可表示为

$$
 D=\{(x,y)\mid0\le y\le1,\ -y\le x\le y\}.
$$

于是

$$
 \begin{aligned}
I
&=\int_0^1\int_{-y}^{y}x^2e^{-y}\,dx\,dy\\
&=\int_0^1e^{-y}
\left[\frac{x^3}{3}\right]_{-y}^{y}dy\\
&=\frac23\int_0^1y^3e^{-y}\,dy.
\end{aligned}
$$

利用分部积分，或直接使用

$$
 \int y^3e^{-y}dy
=-e^{-y}(y^3+3y^2+6y+6)+C,
$$

得到

$$
 \begin{aligned}
\int_0^1y^3e^{-y}dy
&=\left[-e^{-y}(y^3+3y^2+6y+6)\right]_0^1\\
&=6-\frac{16}{e}.
\end{aligned}
$$

因此

$$
 I=\frac23\left(6-\frac{16}{e}\right)
=4-\frac{32}{3e}.
$$

相关知识点：三角形关于 $ y$ 轴对称，且被积函数关于 $ x$ 为偶函数，也可先将 $ x$ 的积分化为两倍右半区域上的积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 计算

$$
 I=\iint_D\arctan\frac yx\,d\sigma,
$$


其中

$$
 D:1\le x^2+y^2\le9,
\qquad
\frac x{\sqrt3}\le y\le\sqrt3x.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{\pi^2}{6}}
$$
</ExamAnswer>

<ExamExplanation>
区域由两个同心圆

$$
 r=1,
\qquad
r=3
$$

以及两条射线

$$
 y=\frac{x}{\sqrt3},
\qquad
y=\sqrt3x
$$

围成。

注意当 $ x<0$ 时，

$$
 \frac{x}{\sqrt3}>\sqrt3x,
$$

不可能同时满足

$$
 \frac{x}{\sqrt3}\le y\le\sqrt3x.
$$

故区域位于第一象限，两条射线对应的极角分别为

$$
 \theta=\frac\pi6,
\qquad
\theta=\frac\pi3.
$$

在该角域内

$$
 \arctan\frac yx=\theta,
\qquad
d\sigma=r\,dr\,d\theta.
$$

因此

$$
 \begin{aligned}
I
&=\int_{\frac\pi6}^{\frac\pi3}
\int_1^3\theta r\,dr\,d\theta\\
&=\left(\int_1^3r\,dr\right)
\left(\int_{\frac\pi6}^{\frac\pi3}\theta\,d\theta\right)\\
&=4\cdot\frac12
\left[\left(\frac\pi3\right)^2-
\left(\frac\pi6\right)^2\right]\\
&=\frac{\pi^2}{6}.
\end{aligned}
$$

相关知识点：含有 $ x^2+y^2$、直线过原点以及 $ \arctan(y/x)$ 的二重积分，通常适合使用极坐标。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 计算

$$
 I=\iint_D|\sin(x-y)|\,dxdy,
$$


其中

$$
 D:0\le x\le y\le2\pi.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=4\pi}
$$
</ExamAnswer>

<ExamExplanation>
按题设直接写成累次积分：

$$
 I=\int_0^{2\pi}\int_0^y|\sin(x-y)|\,dx\,dy.
$$

在内层积分中令

$$
 u=y-x,
\qquad
du=-dx.
$$

当 $ x=0$ 时 $ u=y$，当 $ x=y$ 时 $ u=0$，故

$$
 \int_0^y|\sin(x-y)|\,dx
=\int_0^y|\sin u|\,du.
$$

于是

$$
 I=\int_0^{2\pi}\left(\int_0^y|\sin u|\,du\right)dy.
$$

交换积分次序：

$$
 0\le u\le y\le2\pi,
$$

因此

$$
 I=\int_0^{2\pi}(2\pi-u)|\sin u|\,du.
$$

将区间在 $ u=\pi$ 处分开：

$$
 \begin{aligned}
I
&=\int_0^\pi(2\pi-u)\sin u\,du
+\int_\pi^{2\pi}(2\pi-u)(-\sin u)\,du.
\end{aligned}
$$

第一项为

$$
 \int_0^\pi(2\pi-u)\sin u\,du=3\pi,
$$

第二项为

$$
 \int_\pi^{2\pi}(2\pi-u)(-\sin u)\,du=\pi.
$$

故

$$
 I=3\pi+\pi=4\pi.
$$

相关知识点：绝对值三角函数积分应根据符号变化点分段；交换积分次序后，被积函数只含一个变量，计算会更简洁。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. 计算

$$
 I=\iint_D(x+y)^2\,dxdy,
$$


其中

$$
 D:|x|+|y|\le1.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac23}
$$
</ExamAnswer>

<ExamExplanation>
令

$$
 u=x+y,
\qquad
v=x-y.
$$

则反变换为

$$
 x=\frac{u+v}{2},
\qquad
y=\frac{u-v}{2}.
$$

雅可比为

$$
 \left|\frac{\partial(x,y)}{\partial(u,v)}\right|
=\left|
\begin{vmatrix}
\frac12&\frac12\\[2mm]
\frac12&-\frac12
\end{vmatrix}
\right|
=\frac12.
$$

又因为恒有

$$
 |x|+|y|
=\frac12\bigl(|u+v|+|u-v|\bigr)
=\max\{|u|,|v|\},
$$

所以区域 $ D$ 在 $ uv$ 平面中变为正方形

$$
 D':-1\le u\le1,
\qquad
-1\le v\le1.
$$

同时

$$
 (x+y)^2=u^2.
$$

因此

$$
 \begin{aligned}
I
&=\frac12\int_{-1}^1\int_{-1}^1u^2\,dv\,du\\
&=\frac12\int_{-1}^1 2u^2\,du\\
&=\int_{-1}^1u^2\,du\\
&=\frac23.
\end{aligned}
$$

相关知识点：对于菱形区域 $ |x|+|y|\le a$，使用线性代换 $ u=x+y,\ v=x-y$ 可将区域化为矩形，同时简化含 $ x+y$ 或 $ x-y$ 的被积函数。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. 计算

$$
 I=\iint_D\cos\left(\frac{x-y}{x+y}\right)dxdy,
$$


其中

$$
 D:x\ge0,
\qquad
y\ge0,
\qquad
x+y\le1.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{\sin1}{2}}
$$
</ExamAnswer>

<ExamExplanation>
积分区域为第一象限内由坐标轴和直线

$$
 x+y=1
$$

围成的三角形。由于被积函数中同时出现

$$
 x+y,
\qquad
\frac{x-y}{x+y},
$$

作变量代换

$$
 u=x+y,
\qquad
v=\frac{x-y}{x+y}.
$$

由

$$
 x+y=u,
\qquad
x-y=uv,
$$

可得反变换

$$
 x=\frac{u(1+v)}{2},
\qquad
y=\frac{u(1-v)}{2}.
$$

雅可比为

$$
 \begin{aligned}
\left|\frac{\partial(x,y)}{\partial(u,v)}\right|
&=\left|
\begin{vmatrix}
\frac{1+v}{2}&\frac u2\\[2mm]
\frac{1-v}{2}&-\frac u2
\end{vmatrix}
\right|\\
&=\frac u2.
\end{aligned}
$$

由

$$
 x\ge0,
\qquad
y\ge0,
\qquad
x+y\le1,
$$

得到

$$
 0\le u\le1,
\qquad
-1\le v\le1.
$$

又

$$
 \cos\left(\frac{x-y}{x+y}\right)=\cos v.
$$

因此

$$
 \begin{aligned}
I
&=\int_0^1\int_{-1}^1\cos v\cdot\frac u2\,dv\,du\\
&=\frac12\left(\int_0^1u\,du\right)
\left(\int_{-1}^1\cos v\,dv\right)\\
&=\frac12\cdot\frac12\cdot2\sin1\\
&=\frac{\sin1}{2}.
\end{aligned}
$$

相关知识点：当被积函数含有 $ x+y$ 与 $ (x-y)/(x+y)$ 时，取这两个式子作为新变量，常可同时将被积函数与积分区域化简。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. 设 $ a>0$ 为常数，求积分

$$
 I=\iint_Dxy^2\,d\sigma,
$$


其中

$$
 D:x^2+y^2\le ax.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{\pi a^5}{128}}
$$
</ExamAnswer>

<ExamExplanation>
区域边界

$$
 x^2+y^2=ax
$$

可写成

$$
 \left(x-\frac a2\right)^2+y^2=\frac{a^2}{4},
$$

即圆心为 $ (a/2,0)$、半径为 $ a/2$ 的圆域。

采用极坐标

$$
 x=r\cos\theta,
\qquad
y=r\sin\theta,
\qquad
d\sigma=r\,dr\,d\theta.
$$

区域条件化为

$$
 r^2\le ar\cos\theta.
$$

由于 $ r\ge0$，故

$$
 0\le r\le a\cos\theta,
\qquad
-\frac\pi2\le\theta\le\frac\pi2.
$$

被积函数为

$$
 xy^2=r^3\cos\theta\sin^2\theta.
$$

因此

$$
 \begin{aligned}
I
&=\int_{-\frac\pi2}^{\frac\pi2}
\int_0^{a\cos\theta}
r^4\cos\theta\sin^2\theta\,dr\,d\theta\\
&=\frac{a^5}{5}
\int_{-\frac\pi2}^{\frac\pi2}
\cos^6\theta\sin^2\theta\,d\theta.
\end{aligned}
$$

利用被积函数关于 $ \theta$ 为偶函数，得

$$
 \int_{-\frac\pi2}^{\frac\pi2}
\cos^6\theta\sin^2\theta\,d\theta
=2\int_0^{\frac\pi2}\sin^2\theta\cos^6\theta\,d\theta.
$$

由公式

$$
 \int_0^{\frac\pi2}
\sin^m\theta\cos^n\theta\,d\theta
=\frac12
B\left(\frac{m+1}{2},\frac{n+1}{2}\right),
$$

可得

$$
 \begin{aligned}
2\int_0^{\frac\pi2}\sin^2\theta\cos^6\theta\,d\theta
&=B\left(\frac32,\frac72\right)\\
&=\frac{\Gamma\left(\frac32\right)
\Gamma\left(\frac72\right)}{\Gamma(5)}\\
&=\frac{5\pi}{128}.
\end{aligned}
$$

故

$$
 I=\frac{a^5}{5}\cdot\frac{5\pi}{128}
=\frac{\pi a^5}{128}.
$$

也可利用三角函数幂次积分的递推公式计算最后一个定积分。

相关知识点：圆域 $ x^2+y^2\le ax$ 在极坐标中可直接写成 $ 0\le r\le a\cos\theta$，无需先配方。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 改变二重积分的累次积分的顺序

<ExamQuestion>

1. 改变积分次序

$$
 \int_0^1dx\int_0^{x^2}f(x,y)\,dy+
\int_1^3dx\int_0^{\sqrt{3-x}}f(x,y)\,dy.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^1dy\int_{\sqrt y}^{3-y^2}f(x,y)\,dx
+
\int_1^{\sqrt2}dy\int_1^{3-y^2}f(x,y)\,dx
}
$$
</ExamAnswer>

<ExamExplanation>
第一部分积分区域为

$$
 D_1:0\le x\le1,
\qquad
0\le y\le x^2.
$$

改写为按 $ y$ 分层，有

$$
 0\le y\le1,
\qquad
\sqrt y\le x\le1.
$$

第二部分积分区域为

$$
 D_2:1\le x\le3,
\qquad
0\le y\le\sqrt{3-x}.
$$

由

$$
 y\le\sqrt{3-x}
\Longleftrightarrow
x\le3-y^2,
$$

可得

$$
 0\le y\le\sqrt2,
\qquad
1\le x\le3-y^2.
$$

当

$$
 0\le y\le1
$$

时，两个区域在 $ x=1$ 处相接，合并后有

$$
 \sqrt y\le x\le3-y^2.
$$

当

$$
 1\le y\le\sqrt2
$$

时，只剩区域 $ D_2$，故

$$
 1\le x\le3-y^2.
$$

因此改变积分次序后为

$$
 \int_0^1dy\int_{\sqrt y}^{3-y^2}f(x,y)\,dx
+
\int_1^{\sqrt2}dy\int_1^{3-y^2}f(x,y)\,dx.
$$

相关知识点：改变积分次序时，应先画出两个子区域并判断它们在新积分方向上能否合并；本题的分界高度为 $ y=1$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 改变积分次序

$$
 \int_{-1}^0dx\int_{-x}^{2-x^2}f(x,y)\,dy+
\int_0^1dx\int_x^{2-x^2}f(x,y)\,dy.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^1dy\int_{-y}^{y}f(x,y)\,dx
+
\int_1^2dy\int_{-\sqrt{2-y}}^{\sqrt{2-y}}f(x,y)\,dx
}
$$
</ExamAnswer>

<ExamExplanation>
两部分区域可统一写成

$$
 D:-1\le x\le1,
\qquad
|x|\le y\le2-x^2.
$$

区域下边界为

$$
 y=|x|,
$$

上边界为

$$
 y=2-x^2.
$$

两条边界的交点由

$$
 |x|=2-x^2
$$

确定。在 $ x\ge0$ 时，解方程

$$
 x=2-x^2
$$

得 $ x=1$；由对称性另一个交点为 $ x=-1$。

改为按 $ y$ 分层。由

$$
 y\ge|x|
$$

可得

$$
 -y\le x\le y.
$$

由

$$
 y\le2-x^2
$$

可得

$$
 -\sqrt{2-y}\le x\le\sqrt{2-y}.
$$

比较两个横向界限。令

$$
 y=\sqrt{2-y},
$$

得到

$$
 y^2+y-2=0,
$$

在积分范围内取

$$
 y=1.
$$

因此：

当

$$
 0\le y\le1
$$

时，直线边界限制更强，故

$$
 -y\le x\le y;
$$

当

$$
 1\le y\le2
$$

时，抛物线边界限制更强，故

$$
 -\sqrt{2-y}\le x\le\sqrt{2-y}.
$$

所以改变次序后为

$$
 \int_0^1dy\int_{-y}^{y}f(x,y)\,dx
+
\int_1^2dy\int_{-\sqrt{2-y}}^{\sqrt{2-y}}f(x,y)\,dx.
$$

相关知识点：区域关于 $ y$ 轴对称；改变次序后的分界值由两种横向边界 $ y$ 与 $ \sqrt{2-y}$ 的大小关系确定。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 改变积分次序

$$
 \int_0^{t^2}dx\int_t^{\sqrt{x}}f(x,y)\,dy
\qquad(t>0).
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
-\int_0^t dy\int_0^{y^2}f(x,y)\,dx
}
$$




或等价地写成

$$
 \boxed{
\int_0^t dy\int_{y^2}^{0}f(x,y)\,dx
}.
$$
</ExamAnswer>

<ExamExplanation>
由于

$$
 0\le x\le t^2,
$$

所以

$$
 \sqrt x\le t.
$$

原积分的内层下限大于上限，因此先交换上下限：

$$
 \int_t^{\sqrt x}f(x,y)\,dy
=-\int_{\sqrt x}^{t}f(x,y)\,dy.
$$

于是原积分为

$$
 -\int_0^{t^2}dx\int_{\sqrt x}^{t}f(x,y)\,dy.
$$

对应的实际区域为

$$
 0\le x\le t^2,
\qquad
\sqrt x\le y\le t.
$$

由

$$
 y\ge\sqrt x
\Longleftrightarrow
0\le x\le y^2
$$

可知按 $ y$ 分层时

$$
 0\le y\le t,
\qquad
0\le x\le y^2.
$$

因此

$$
 \int_0^{t^2}dx\int_t^{\sqrt{x}}f(x,y)\,dy
=-\int_0^t dy\int_0^{y^2}f(x,y)\,dx.
$$

相关知识点：当累次积分的下限大于上限时，改变积分次序前必须先提出负号，否则会错误地改变积分的符号。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 改变极坐标累次积分的次序

$$
 \int_0^{\frac\pi2}d\theta
\int_0^{\sqrt{\sin2\theta}}
f(r\cos\theta,r\sin\theta)r\,dr.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^1 r\,dr
\int_{\frac12\arcsin r^2}^{\frac\pi2-\frac12\arcsin r^2}
f(r\cos\theta,r\sin\theta)\,d\theta
}.
$$
</ExamAnswer>

<ExamExplanation>
原积分区域在极坐标下为

$$
 0\le\theta\le\frac\pi2,
\qquad
0\le r\le\sqrt{\sin2\theta}.
$$

由

$$
 r\le\sqrt{\sin2\theta}
$$

可得

$$
 r^2\le\sin2\theta.
$$

因为

$$
 0\le\sin2\theta\le1,
$$

所以半径的总取值范围为

$$
 0\le r\le1.
$$

固定 $ r\in[0,1]$，令

$$
 \alpha=\arcsin r^2,
\qquad
0\le\alpha\le\frac\pi2.
$$

在

$$
 0\le2\theta\le\pi
$$

内，不等式

$$
 \sin2\theta\ge r^2=\sin\alpha
$$

等价于

$$
 \alpha\le2\theta\le\pi-\alpha.
$$

因此

$$
 \frac12\arcsin r^2
\le\theta\le
\frac\pi2-\frac12\arcsin r^2.
$$

故改变积分次序后为

$$
 \int_0^1 r\,dr
\int_{\frac12\arcsin r^2}^{\frac\pi2-\frac12\arcsin r^2}
f(r\cos\theta,r\sin\theta)\,d\theta.
$$

相关知识点：极坐标累次积分换序时，应先确定 $ r$ 的整体范围，再对固定的 $ r$ 解关于 $ \theta$ 的三角不等式。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 求下列二重积分的累次积分

<ExamQuestion>

1. 计算

$$
 I=\int_0^1dx\int_x^{\sqrt{x}}\frac{\sin y}{y}\,dy.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=1-\sin1}.
$$
</ExamAnswer>

<ExamExplanation>
原积分区域为

$$
 D=\{(x,y)\mid 0\le x\le1,\ x\le y\le\sqrt x\}.
$$

由

$$
 y\le\sqrt x\Longleftrightarrow y^2\le x,
\qquad
x\le y,
$$

可知按 $ y$ 分层时

$$
 0\le y\le1,
\qquad
y^2\le x\le y.
$$

因此改变积分次序得

$$
 \begin{aligned}
I
&=\int_0^1dy\int_{y^2}^{y}\frac{\sin y}{y}\,dx\\
&=\int_0^1\frac{\sin y}{y}(y-y^2)\,dy\\
&=\int_0^1(1-y)\sin y\,dy.
\end{aligned}
$$

分别计算

$$
 \int_0^1\sin y\,dy=1-\cos1,
$$

以及分部积分

$$
 \int_0^1y\sin y\,dy
=\left[-y\cos y+\sin y\right]_0^1
=-\cos1+\sin1.
$$

所以

$$
 \begin{aligned}
I
&=(1-\cos1)-(-\cos1+\sin1)\\
&=1-\sin1.
\end{aligned}
$$

相关知识点：当被积函数只含内层变量 $ y$ 时，改变积分次序往往可以先对 $ x$ 积分，从而显著简化计算。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 计算

$$
 I=\int_0^Rdx\int_0^{\sqrt{R^2-x^2}}
\ln(1+x^2+y^2)\,dy,
\qquad R>0.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
I=\frac\pi4\left[(1+R^2)\ln(1+R^2)-R^2\right]
}.
$$
</ExamAnswer>

<ExamExplanation>
积分区域为第一象限内的四分之一圆域

$$
 D:x^2+y^2\le R^2,
\qquad x\ge0,
\qquad y\ge0.
$$

被积函数只与

$$
 x^2+y^2
$$

有关，故采用极坐标变换

$$
 x=r\cos\theta,
\qquad
y=r\sin\theta,
\qquad
dxdy=r\,drd\theta.
$$

区域变为

$$
 0\le\theta\le\frac\pi2,
\qquad
0\le r\le R.
$$

于是

$$
 \begin{aligned}
I
&=\int_0^{\frac\pi2}d\theta
\int_0^R\ln(1+r^2)r\,dr\\
&=\frac\pi2\int_0^R\ln(1+r^2)r\,dr.
\end{aligned}
$$

令

$$
 u=1+r^2,
\qquad
du=2r\,dr,
$$

则

$$
 \begin{aligned}
\int_0^R\ln(1+r^2)r\,dr
&=\frac12\int_1^{1+R^2}\ln u\,du\\
&=\frac12\left[u\ln u-u\right]_1^{1+R^2}\\
&=\frac12\left[(1+R^2)\ln(1+R^2)-R^2\right].
\end{aligned}
$$

因此

$$
 I=\frac\pi4\left[(1+R^2)\ln(1+R^2)-R^2\right].
$$

相关知识点：圆域上只依赖于 $ x^2+y^2$ 的二重积分，通常优先使用极坐标。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 计算下列三重积分或将三重积分化成累次积分

<ExamQuestion>

1. 计算

$$
 I=\iiint_\Omega x^3y^2z\,dV,
$$


其中 $ \Omega$ 是由

$$
 x=1,
\quad
x=2,
\quad
y=0,
\quad
y=x^2,
\quad
z=0,
\quad
z=\frac1x
$$


所围成的区域。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{85}{16}}.
$$
</ExamAnswer>

<ExamExplanation>
由边界条件可将区域写为

$$
 \Omega:
1\le x\le2,
\qquad
0\le y\le x^2,
\qquad
0\le z\le\frac1x.
$$

因此

$$
 I=\int_1^2dx\int_0^{x^2}dy\int_0^{1/x}x^3y^2z\,dz.
$$

先对 $ z$ 积分：

$$
 \int_0^{1/x}z\,dz=\frac1{2x^2}.
$$

再对 $ y$ 积分：

$$
 \int_0^{x^2}y^2\,dy=\frac{x^6}{3}.
$$

于是

$$
 \begin{aligned}
I
&=\int_1^2x^3\cdot\frac{x^6}{3}\cdot\frac1{2x^2}\,dx\\
&=\frac16\int_1^2x^7\,dx\\
&=\frac1{48}\left[x^8\right]_1^2\\
&=\frac{2^8-1}{48}\\
&=\frac{255}{48}\\
&=\frac{85}{16}.
\end{aligned}
$$

相关知识点：当积分区域的上下界按某一变量逐层给出时，直接按该顺序写成累次积分即可；若被积函数可分离，还可分别计算各层积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 计算

$$
 I=\iiint_\Omega(lx^2+my^2+nz^2)\,dV,
$$


其中

$$
 \Omega:x^2+y^2+z^2\le a^2,
$$


$ l,m,n$ 为常数。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
I=\frac{4\pi a^5}{15}(l+m+n)
}.
$$
</ExamAnswer>

<ExamExplanation>
球域 $ \Omega$ 关于三个坐标面完全对称，并且交换 $ x,y,z$ 后区域保持不变，因此

$$
 \iiint_\Omega x^2\,dV
=
\iiint_\Omega y^2\,dV
=
\iiint_\Omega z^2\,dV.
$$

又因为

$$
 x^2+y^2+z^2=r^2,
$$

故

$$
 3\iiint_\Omega x^2\,dV
=
\iiint_\Omega r^2\,dV.
$$

采用球坐标，体积元为

$$
 dV=r^2\sin\varphi\,drd\varphi d\theta,
$$

其中

$$
 0\le r\le a,
\qquad
0\le\varphi\le\pi,
\qquad
0\le\theta\le2\pi.
$$

于是

$$
 \begin{aligned}
\iiint_\Omega r^2\,dV
&=\int_0^{2\pi}d\theta
\int_0^\pi\sin\varphi\,d\varphi
\int_0^a r^4\,dr\\
&=2\pi\cdot2\cdot\frac{a^5}{5}\\
&=\frac{4\pi a^5}{5}.
\end{aligned}
$$

所以

$$
 \iiint_\Omega x^2\,dV
=
\iiint_\Omega y^2\,dV
=
\iiint_\Omega z^2\,dV
=\frac{4\pi a^5}{15}.
$$

从而

$$
 \begin{aligned}
I
&=l\iiint_\Omega x^2\,dV
+m\iiint_\Omega y^2\,dV
+n\iiint_\Omega z^2\,dV\\
&=\frac{4\pi a^5}{15}(l+m+n).
\end{aligned}
$$

相关知识点：球域具有坐标轮换对称性，因此 $ x^2,y^2,z^2$ 在球域上的积分相等。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 计算

$$
 I=\iiint_\Omega z\,dV,
$$


其中

$$
 \Omega:x^2+y^2+z^2\le2,
\qquad
x^2+y^2\le z.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{7\pi}{12}}.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
 x=r\cos\theta,
\qquad
y=r\sin\theta,
\qquad
dV=r\,dz\,dr\,d\theta.
$$

区域条件化为

$$
 r^2+z^2\le2,
\qquad
z\ge r^2.
$$

抛物面

$$
 z=r^2
$$

与球面上半部

$$
 z=\sqrt{2-r^2}
$$

的交线满足

$$
 r^2=\sqrt{2-r^2}.
$$

两边平方得

$$
 r^4+r^2-2=0,
$$

即

$$
 (r^2-1)(r^2+2)=0.
$$

由于 $ r\ge0$，故交线对应

$$
 r=1,
\qquad z=1.
$$

因此柱坐标下区域为

$$
 0\le\theta\le2\pi,
\qquad
0\le r\le1,
\qquad
r^2\le z\le\sqrt{2-r^2}.
$$

于是

$$
 \begin{aligned}
I
&=\int_0^{2\pi}d\theta
\int_0^1r\,dr
\int_{r^2}^{\sqrt{2-r^2}}z\,dz\\
&=2\pi\int_0^1
\frac12\left(2-r^2-r^4\right)r\,dr\\
&=\pi\int_0^1\left(2r-r^3-r^5\right)dr\\
&=\pi\left(1-\frac14-\frac16\right)\\
&=\frac{7\pi}{12}.
\end{aligned}
$$

相关知识点：由旋转曲面围成的立体通常优先使用柱坐标；确定径向范围时，应先求两边界曲面的交线。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 计算

$$
 I=\iiint_\Omega(x+y+z)\,dV,
$$


其中

$$
 \Omega:x^2+y^2+z^2\le2az,
\qquad
\sqrt{x^2+y^2}\le z,
\qquad
a>0.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{7\pi a^4}{6}}.
$$
</ExamAnswer>

<ExamExplanation>
区域关于 $ z$ 轴旋转对称，因此

$$
 \iiint_\Omega x\,dV=0,
\qquad
\iiint_\Omega y\,dV=0.
$$

故

$$
 I=\iiint_\Omega z\,dV.
$$

采用球坐标

$$
 x=\rho\sin\varphi\cos\theta,
\qquad
y=\rho\sin\varphi\sin\theta,
\qquad
z=\rho\cos\varphi,
$$

且

$$
 dV=\rho^2\sin\varphi\,d\rho\,d\varphi\,d\theta.
$$

球面条件

$$
 x^2+y^2+z^2\le2az
$$

化为

$$
 \rho^2\le2a\rho\cos\varphi,
$$

故

$$
 0\le\rho\le2a\cos\varphi.
$$

圆锥条件

$$
 \sqrt{x^2+y^2}\le z
$$

化为

$$
 \rho\sin\varphi\le\rho\cos\varphi,
$$

故

$$
 0\le\varphi\le\frac\pi4.
$$

又由于区域绕 $ z$ 轴一周，

$$
 0\le\theta\le2\pi.
$$

于是

$$
 \begin{aligned}
I
&=\int_0^{2\pi}d\theta
\int_0^{\pi/4}d\varphi
\int_0^{2a\cos\varphi}
\rho\cos\varphi\cdot\rho^2\sin\varphi\,d\rho\\
&=2\pi\int_0^{\pi/4}\cos\varphi\sin\varphi
\left[\frac{\rho^4}{4}\right]_0^{2a\cos\varphi}d\varphi\\
&=8\pi a^4\int_0^{\pi/4}\cos^5\varphi\sin\varphi\,d\varphi\\
&=8\pi a^4\left[-\frac{\cos^6\varphi}{6}\right]_0^{\pi/4}\\
&=\frac{8\pi a^4}{6}\left(1-\frac18\right)\\
&=\frac{7\pi a^4}{6}.
\end{aligned}
$$

相关知识点：球面与顶点在原点的圆锥面共同围成的区域，使用球坐标最简便；利用旋转对称性可先消去奇函数项。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. 计算

$$
 I=\iiint_\Omega xyz\,dV,
$$


其中

$$
 \Omega:x^2+y^2+z^2\le1
$$


位于第一卦限的部分。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac1{48}}.
$$
</ExamAnswer>

<ExamExplanation>
第一卦限内有

$$
 x\ge0,
\qquad y\ge0,
\qquad z\ge0.
$$

采用球坐标

$$
 x=\rho\sin\varphi\cos\theta,
\qquad
y=\rho\sin\varphi\sin\theta,
\qquad
z=\rho\cos\varphi,
$$

其中

$$
 0\le\rho\le1,
\qquad
0\le\varphi\le\frac\pi2,
\qquad
0\le\theta\le\frac\pi2.
$$

又

$$
 dV=\rho^2\sin\varphi\,d\rho\,d\varphi\,d\theta.
$$

因此

$$
 \begin{aligned}
I
&=\int_0^{\pi/2}d\theta
\int_0^{\pi/2}d\varphi
\int_0^1
\rho^3\sin^2\varphi\cos\varphi
\cos\theta\sin\theta
\cdot\rho^2\sin\varphi\,d\rho\\
&=\left(\int_0^1\rho^5\,d\rho\right)
\left(\int_0^{\pi/2}\sin^3\varphi\cos\varphi\,d\varphi\right)
\left(\int_0^{\pi/2}\sin\theta\cos\theta\,d\theta\right)\\
&=\frac16\cdot\frac14\cdot\frac12\\
&=\frac1{48}.
\end{aligned}
$$

相关知识点：球域第一卦限在球坐标中对应两个角变量均取 $ [0,\pi/2]$，且乘积型被积函数可分离成三个一元积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. 将三重积分

$$
 \iiint_\Omega f(x,y,z)\,dV
$$


在三种坐标系下化成累次积分，其中 $ \Omega$ 是由

$$
 x^2+y^2+z^2\le R^2,
\qquad
x^2+y^2\le z^2,
\qquad
z\ge0
$$


所围成的区域。

<ExamSolution>

<ExamAnswer>
直角坐标：


$$
 \boxed{
\int_{-R/\sqrt2}^{R/\sqrt2}dx
\int_{-\sqrt{R^2/2-x^2}}^{\sqrt{R^2/2-x^2}}dy
\int_{\sqrt{x^2+y^2}}^{\sqrt{R^2-x^2-y^2}}
f(x,y,z)\,dz
}.
$$

柱坐标：

$$
 \boxed{
\int_0^{2\pi}d\theta
\int_0^{R/\sqrt2}r\,dr
\int_r^{\sqrt{R^2-r^2}}
f(r\cos\theta,r\sin\theta,z)\,dz
}.
$$

球坐标：

$$
 \boxed{
\int_0^{2\pi}d\theta
\int_0^{\pi/4}\sin\varphi\,d\varphi
\int_0^R
f(\rho\sin\varphi\cos\theta,
\rho\sin\varphi\sin\theta,
\rho\cos\varphi)\rho^2\,d\rho
}.
$$
</ExamAnswer>

<ExamExplanation>
由

$$
 x^2+y^2\le z^2,
\qquad z\ge0
$$

可知区域位于圆锥面

$$
 z=\sqrt{x^2+y^2}
$$

的上方，同时位于球面

$$
 x^2+y^2+z^2=R^2
$$

的内部。

在直角坐标下，对固定的 $ (x,y)$，有

$$
 \sqrt{x^2+y^2}\le z\le\sqrt{R^2-x^2-y^2}.
$$

为使上下限满足大小关系，应有

$$
 \sqrt{x^2+y^2}\le\sqrt{R^2-x^2-y^2},
$$

即

$$
 x^2+y^2\le\frac{R^2}{2}.
$$

因此在 $ Oxy$ 平面上的投影是半径为 $ R/\sqrt2$ 的圆域，从而得到直角坐标形式。

在柱坐标下，圆锥面与球面的交线满足

$$
 z=r,
\qquad
r^2+z^2=R^2,
$$

故

$$
 2r^2=R^2,
\qquad
r=\frac{R}{\sqrt2}.
$$

于是

$$
 0\le r\le\frac{R}{\sqrt2},
\qquad
r\le z\le\sqrt{R^2-r^2}.
$$

在球坐标下，圆锥条件化为

$$
 \rho^2\sin^2\varphi\le\rho^2\cos^2\varphi.
$$

结合 $ z\ge0$，得

$$
 0\le\varphi\le\frac\pi4.
$$

球域条件给出

$$
 0\le\rho\le R,
$$

而绕 $ z$ 轴一周有

$$
 0\le\theta\le2\pi.
$$

相关知识点：同一区域在不同坐标系下的关键是分别确定投影范围、径向范围和角度范围，并正确带入相应的雅可比因子。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 改变三重积分的累次积分的顺序或转换三重积分的累次积分

<ExamQuestion>

1. 将

$$
 \int_0^h dz\int_0^a dx
\int_{\frac{a-x}{2}}^{\sqrt{a^2-x^2}}f(x,y,z)\,dy
$$


变成由 $ z$ 到 $ y$ 再到 $ x$ 的顺序。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^a dx
\int_{\frac{a-x}{2}}^{\sqrt{a^2-x^2}}dy
\int_0^h f(x,y,z)\,dz
}.
$$
</ExamAnswer>

<ExamExplanation>
原积分对应的空间区域为

$$
 \Omega:
0\le z\le h,
\qquad
0\le x\le a,
\qquad
\frac{a-x}{2}\le y\le\sqrt{a^2-x^2}.
$$

其中 $ z$ 的范围与 $ x,y$ 无关，故要求先对 $ z$ 积分、再对 $ y$ 积分、最后对 $ x$ 积分时，只需把 $ z$ 移到最内层，而 $ x,y$ 的投影区域及其上下限保持不变。

因此积分次序改写为

$$
 \int_0^a dx
\int_{\frac{a-x}{2}}^{\sqrt{a^2-x^2}}dy
\int_0^h f(x,y,z)\,dz.
$$

相关知识点：改变三重积分次序时，应先写出立体区域；若某个变量的范围是常数区间且与其他变量无关，则可直接将该变量调整到所需积分层次。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 将

$$
 \int_0^1dx\int_0^1dy\int_0^{x^2+y^2}f(x,y,z)\,dz
$$


改换成先 $ y$ 最后 $ x$ 的顺序。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^1dx\left[
\int_0^{x^2}dz\int_0^1 f(x,y,z)\,dy
+
\int_{x^2}^{x^2+1}dz\int_{\sqrt{z-x^2}}^1 f(x,y,z)\,dy
\right]
}.
$$
</ExamAnswer>

<ExamExplanation>
原积分所对应的空间区域为

$$
 \Omega:
0\le x\le1,
\qquad
0\le y\le1,
\qquad
0\le z\le x^2+y^2.
$$

要求“先 $ y$ 最后 $ x$”积分，即积分次序为

$$
 dy\,dz\,dx.
$$

固定 $ x\in[0,1]$ 后，由

$$
 0\le y\le1,
\qquad
z\le x^2+y^2
$$

可得

$$
 y^2\ge z-x^2.
$$

需要根据 $ z$ 与 $ x^2$ 的大小分段。

当

$$
 0\le z\le x^2
$$

时，恒有 $ z-x^2\le0$，因此任意 $ y\in[0,1]$ 均满足条件，即

$$
 0\le y\le1.
$$

当

$$
 x^2<z\le x^2+1
$$

时，应有

$$
 y\ge\sqrt{z-x^2},
$$

故

$$
 \sqrt{z-x^2}\le y\le1.
$$

于是积分改写为

$$
 \int_0^1dx\left[
\int_0^{x^2}dz\int_0^1 f(x,y,z)\,dy
+
\int_{x^2}^{x^2+1}dz\int_{\sqrt{z-x^2}}^1 f(x,y,z)\,dy
\right].
$$

相关知识点：改变三重积分次序时，应先固定最外层变量，再根据截面区域确定中间变量的分段范围和最内层变量的上下限。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 考虑柱坐标系下的三重累次积分

$$
 I=\int_0^{2\pi}d\theta\int_0^{\sqrt2}dr
\int_r^{\sqrt{4-r^2}}3r\,dz.
$$


（Ⅰ）将 $ I$ 用直角坐标 $ (Oxyz)$ 化为累次积分；

（Ⅱ）将 $ I$ 用球坐标化为累次积分；

（Ⅲ）求 $ I$ 的值。

<ExamSolution>

<ExamAnswer>
（Ⅰ）


$$
 \boxed{
I=
\int_{-\sqrt2}^{\sqrt2}dx
\int_{-\sqrt{2-x^2}}^{\sqrt{2-x^2}}dy
\int_{\sqrt{x^2+y^2}}^{\sqrt{4-x^2-y^2}}3\,dz
}.
$$

（Ⅱ）

$$
 \boxed{
I=
\int_0^{2\pi}d\theta
\int_0^{\pi/4}d\varphi
\int_0^2 3\rho^2\sin\varphi\,d\rho
}.
$$

（Ⅲ）

$$
 \boxed{I=8\pi(2-\sqrt2)}.
$$
</ExamAnswer>

<ExamExplanation>
原积分在柱坐标中的区域为

$$
 0\le\theta\le2\pi,
\qquad
0\le r\le\sqrt2,
\qquad
r\le z\le\sqrt{4-r^2}.
$$

其中下边界

$$
 z=r
$$

是上半圆锥面，上边界

$$
 r^2+z^2=4
$$

是半径为 $ 2$ 的球面。两曲面交线满足

$$
 r=\sqrt{4-r^2},
$$

从而

$$
 r=\sqrt2.
$$

原积分中的 $ 3r\,dz\,dr\,d\theta$ 正是直角坐标下的

$$
 3\,dV.
$$

因此在直角坐标中，区域在 $ Oxy$ 平面上的投影为

$$
 x^2+y^2\le2,
$$

且

$$
 \sqrt{x^2+y^2}\le z\le\sqrt{4-x^2-y^2}.
$$

故得到（Ⅰ）中的累次积分。

在球坐标中，圆锥面

$$
 z=\sqrt{x^2+y^2}
$$

对应

$$
 \rho\cos\varphi=\rho\sin\varphi,
$$

即

$$
 \varphi=\frac\pi4.
$$

区域位于圆锥内部且位于球内，所以

$$
 0\le\varphi\le\frac\pi4,
\qquad
0\le\rho\le2,
\qquad
0\le\theta\le2\pi.
$$

球坐标的体积元为

$$
 dV=\rho^2\sin\varphi\,d\rho\,d\varphi\,d\theta,
$$

故得到（Ⅱ）中的表达式。

最后

$$
 \begin{aligned}
I
&=\int_0^{2\pi}d\theta
\int_0^{\pi/4}d\varphi
\int_0^2 3\rho^2\sin\varphi\,d\rho\\
&=2\pi\cdot\left(1-\cos\frac\pi4\right)\cdot8\\
&=16\pi\left(1-\frac{\sqrt2}{2}\right)\\
&=8\pi(2-\sqrt2).
\end{aligned}
$$

相关知识点：由柱坐标改写为直角坐标或球坐标时，应先识别边界曲面，再根据几何区域确定投影范围、极角范围和径向范围。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 求

$$
 \int_{-1}^1dx\int_0^{\sqrt{1-x^2}}dy
\int_1^{1+\sqrt{1-x^2-y^2}}
\frac{dz}{\sqrt{x^2+y^2+z^2}}.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{\frac{\pi}{6}(7-4\sqrt2)}.
$$
</ExamAnswer>

<ExamExplanation>
原区域满足

$$
 x^2+y^2\le1,
\qquad
y\ge0,
\qquad
1\le z\le1+\sqrt{1-x^2-y^2}.
$$

上界等价于

$$
 x^2+y^2+(z-1)^2\le1,
\qquad z\ge1.
$$

因此该区域是球面

$$
 x^2+y^2+(z-1)^2=1
$$

的上半球内部，且只取 $ y\ge0$ 的一半。

采用以原点为极点的球坐标

$$
 x=\rho\sin\varphi\cos\theta,
\qquad
y=\rho\sin\varphi\sin\theta,
\qquad z=\rho\cos\varphi.
$$

由 $ y\ge0$ 得

$$
 0\le\theta\le\pi.
$$

球面条件

$$
 x^2+y^2+(z-1)^2\le1
$$

化为

$$
 \rho^2-2\rho\cos\varphi\le0,
$$

即

$$
 0\le\rho\le2\cos\varphi.
$$

又由 $ z\ge1$ 得

$$
 \rho\cos\varphi\ge1,
$$

即

$$
 \rho\ge\sec\varphi.
$$

要使上下限有意义，应满足

$$
 \sec\varphi\le2\cos\varphi,
$$

即

$$
 \cos^2\varphi\ge\frac12.
$$

故

$$
 0\le\varphi\le\frac\pi4.
$$

由于

$$
 \sqrt{x^2+y^2+z^2}=\rho,
$$

故积分为

$$
 \begin{aligned}
I
&=\int_0^\pi d\theta
\int_0^{\pi/4}d\varphi
\int_{\sec\varphi}^{2\cos\varphi}
\frac1\rho\rho^2\sin\varphi\,d\rho\\
&=\pi\int_0^{\pi/4}\sin\varphi
\left[\frac{\rho^2}{2}\right]_{\sec\varphi}^{2\cos\varphi}d\varphi\\
&=\frac\pi2\int_0^{\pi/4}
\left(4\sin\varphi\cos^2\varphi-\frac{\sin\varphi}{\cos^2\varphi}\right)d\varphi\\
&=\frac\pi2\left[
\frac{4-\sqrt2}{3}-(\sqrt2-1)
\right]\\
&=\frac\pi6(7-4\sqrt2).
\end{aligned}
$$

相关知识点：当区域是偏心球域时，若被积函数含 $ \sqrt{x^2+y^2+z^2}$，可优先采用以原点为极点的球坐标，再把偏心球面转化为径向上界。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

## 计算曲线积分与曲面积分及多元函数积分学的应用

<ExamQuestion>

1. （Ⅰ）设 $ L$ 为抛物线 $ y=x^2$ 上，从点 $ A(-1,1)$ 到 $ B(1,1)$ 的一段，求

$$
 I=\int_L(x^2-2xy)\,dx+(y^2-2xy)\,dy.
$$


（Ⅱ）求积分

$$
 I=\int_C\frac1y\,dx+\frac1x\,dy,
$$


其中 $ C$ 由

$$
 y=1,
\qquad
x=4,
\qquad
y=\sqrt{x}
$$


组成，取逆时针一周。

<ExamSolution>

<ExamAnswer>
（Ⅰ）


$$
 \boxed{I=-\frac{14}{15}}.
$$

（Ⅱ）

$$
 \boxed{I=\frac34}.
$$
</ExamAnswer>

<ExamExplanation>
（Ⅰ）取参数

$$
 x=t,
\qquad
y=t^2,
\qquad -1\le t\le1.
$$

则

$$
 dx=dt,
\qquad dy=2t\,dt.
$$

因此

$$
 \begin{aligned}
I
&=\int_{-1}^1
\left[(t^2-2t^3)+(t^4-2t^3)2t\right]dt\\
&=\int_{-1}^1
\left(t^2-2t^3-4t^4+2t^5\right)dt.
\end{aligned}
$$

其中奇函数项在对称区间上的积分为零，所以

$$
 \begin{aligned}
I
&=\int_{-1}^1(t^2-4t^4)\,dt\\
&=\frac23-\frac85\\
&=-\frac{14}{15}.
\end{aligned}
$$

（Ⅱ）令

$$
 P(x,y)=\frac1y,
\qquad
Q(x,y)=\frac1x.
$$

曲线 $ C$ 为正向闭曲线，所围区域可写为

$$
 D:
1\le y\le2,
\qquad
y^2\le x\le4.
$$

在该区域内 $ x>0,y>0$，故可用格林公式：

$$
 \oint_C P\,dx+Q\,dy
=
\iint_D\left(\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}\right)d\sigma.
$$

又

$$
 \frac{\partial Q}{\partial x}=-\frac1{x^2},
\qquad
\frac{\partial P}{\partial y}=-\frac1{y^2},
$$

故

$$
 \begin{aligned}
I
&=\int_1^2dy\int_{y^2}^4
\left(\frac1{y^2}-\frac1{x^2}\right)dx\\
&=\int_1^2\left(\frac3{y^2}-\frac34\right)dy\\
&=\left[-\frac3y-\frac34y\right]_1^2\\
&=\frac34.
\end{aligned}
$$

相关知识点：第一类参数曲线上的第二类曲线积分可直接参数化；闭曲线积分若满足格林公式条件，则通常可转化为二重积分简化计算。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

2. 设 $ L$ 为曲线

$$
 \begin{cases}
x^2+y^2+z^2=a^2,\\
y+z=0,
\end{cases}
$$


求积分

$$
 I=\int_L(x^2+3y+3z)\,ds.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\pi a^3}.
$$
</ExamAnswer>

<ExamExplanation>
由于曲线 $ L$ 位于平面

$$
 y+z=0
$$

上，因此沿 $ L$ 恒有

$$
 3y+3z=0.
$$

故积分化为

$$
 I=\int_Lx^2\,ds.
$$

平面 $ y+z=0$ 经过球心，因此它与球面

$$
 x^2+y^2+z^2=a^2
$$

的交线是半径为 $ a$ 的大圆。

可取参数方程

$$
 \begin{cases}
x=a\cos t,\\
y=\frac{a}{\sqrt2}\sin t,\\
z=-\frac{a}{\sqrt2}\sin t,
\end{cases}
\qquad0\le t\le2\pi.
$$

于是

$$
 \begin{aligned}
ds
&=\sqrt{
\left(-a\sin t\right)^2
+\left(\frac{a}{\sqrt2}\cos t\right)^2
+\left(-\frac{a}{\sqrt2}\cos t\right)^2
}\,dt\\
&=a\,dt.
\end{aligned}
$$

因此

$$
 \begin{aligned}
I
&=\int_0^{2\pi}a^2\cos^2t\cdot a\,dt\\
&=a^3\int_0^{2\pi}\cos^2t\,dt\\
&=\pi a^3.
\end{aligned}
$$

相关知识点：球面与过球心平面的交线是大圆；对第一类曲线积分，参数方向不影响积分值。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

3. 计算曲面积分

$$
 I=\iint_\Sigma(x+y+z)\,dS,
$$


其中 $ \Sigma$ 为左半球

$$
 x^2+y^2+z^2=R^2,
\qquad
y\le0.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=-\pi R^3}.
$$
</ExamAnswer>

<ExamExplanation>
曲面 $ \Sigma$ 关于平面 $ x=0$ 及平面 $ z=0$ 对称，而函数 $ x$ 关于 $ x$ 为奇函数，函数 $ z$ 关于 $ z$ 为奇函数，因此

$$
 \iint_\Sigma x\,dS=0,
\qquad
\iint_\Sigma z\,dS=0.
$$

故

$$
 I=\iint_\Sigma y\,dS.
$$

采用球面参数

$$
 \begin{cases}
x=R\sin\varphi\cos\theta,\\
y=R\sin\varphi\sin\theta,\\
z=R\cos\varphi,
\end{cases}
$$

其中

$$
 0\le\varphi\le\pi,
\qquad
\pi\le\theta\le2\pi,
$$

后一个角度范围恰好对应 $ y\le0$。球面的面积元为

$$
 dS=R^2\sin\varphi\,d\varphi\,d\theta.
$$

于是

$$
 \begin{aligned}
I
&=R^3\int_\pi^{2\pi}\sin\theta\,d\theta
\int_0^\pi\sin^2\varphi\,d\varphi\\
&=R^3(-2)\cdot\frac\pi2\\
&=-\pi R^3.
\end{aligned}
$$

相关知识点：第一类曲面积分与曲面方向无关；利用积分区域和被积函数的奇偶对称性，可先消去积分为零的部分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

4. 计算曲面积分

$$
 \iint_\Sigma\frac{dS}{r^2},
$$


其中 $ \Sigma$ 为圆柱面

$$
 x^2+y^2=R^2
$$


界于 $ z=0$ 及 $ z=H$ 之间的部分，$ r$ 为曲面上的点到原点的距离，$ H>0$。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=2\pi\arctan\frac HR}.
$$
</ExamAnswer>

<ExamExplanation>
取圆柱面的参数方程

$$
 \begin{cases}
x=R\cos\theta,\\
y=R\sin\theta,\\
z=z,
\end{cases}
\qquad
0\le\theta\le2\pi,
\quad
0\le z\le H.
$$

曲面面积元为

$$
 dS=R\,d\theta\,dz.
$$

曲面上任一点到原点的距离满足

$$
 r^2=x^2+y^2+z^2=R^2+z^2.
$$

因此

$$
 \begin{aligned}
I
&=\int_0^{2\pi}d\theta\int_0^H\frac{R}{R^2+z^2}\,dz\\
&=2\pi R\cdot\frac1R
\left[\arctan\frac zR\right]_0^H\\
&=2\pi\arctan\frac HR.
\end{aligned}
$$

相关知识点：圆柱侧面的参数化可取圆周角与高度为参数，其面积元为 $ R\,d\theta\,dz$。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

5. 设 $ S$ 为柱面

$$
 x^2+y^2=a^2
\qquad(0\le z\le h)
$$


的外侧，满足 $ x\ge0$ 的部分，求

$$
 I=\iint_S z\,dydz+xyz\,dzdx+yz\,dxdy.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=ah^2+\frac13a^3h^2
=ah^2\left(1+\frac{a^2}{3}\right)}.
$$
</ExamAnswer>

<ExamExplanation>
令

$$
 \mathbf F=(P,Q,R)=(z,xyz,yz).
$$

所求积分是向量场 $ \mathbf F$ 通过曲面 $ S$ 外侧的通量。

取参数方程

$$
 \mathbf r(\theta,z)
=(a\cos\theta,a\sin\theta,z),
$$

其中

$$
 -\frac\pi2\le\theta\le\frac\pi2,
\qquad
0\le z\le h,
$$

角度范围对应 $ x=a\cos\theta\ge0$。有

$$
 \mathbf r_\theta=(-a\sin\theta,a\cos\theta,0),
\qquad
\mathbf r_z=(0,0,1),
$$

故指向圆柱外侧的有向面积元为

$$
 \mathbf r_\theta\times\mathbf r_z
=(a\cos\theta,a\sin\theta,0).
$$

在曲面上，

$$
 \mathbf F
=\left(z,a^2z\cos\theta\sin\theta,az\sin\theta\right).
$$

因此

$$
 \begin{aligned}
\mathbf F\cdot
(\mathbf r_\theta\times\mathbf r_z)
&=az\cos\theta
+a^3z\cos\theta\sin^2\theta.
\end{aligned}
$$

于是

$$
 \begin{aligned}
I
&=\int_0^h z\,dz
\int_{-\frac\pi2}^{\frac\pi2}
\left(a\cos\theta+a^3\cos\theta\sin^2\theta\right)d\theta\\
&=\frac{h^2}{2}
\left(2a+\frac{2a^3}{3}\right)\\
&=ah^2+\frac13a^3h^2.
\end{aligned}
$$

其中使用了

$$
 \int_{-\frac\pi2}^{\frac\pi2}\cos\theta\,d\theta=2,
\qquad
\int_{-\frac\pi2}^{\frac\pi2}\cos\theta\sin^2\theta\,d\theta=\frac23.
$$

相关知识点：第二类曲面积分可写成向量场通量；参数化时必须根据题目要求选择正确的法向方向。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

6. 求曲面积分

$$
 I=\iint_S(x+\cos y)\,dydz+(y+\cos z)\,dzdx+(z+\cos x)\,dxdy,
$$


其中 $ S$ 为

$$
 x+y+z=\pi
$$


在第一卦限部分，取上侧。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=\frac{\pi^3}{2}+6}.
$$
</ExamAnswer>

<ExamExplanation>
平面可写成

$$
 z=\pi-x-y,
$$

其在 $ Oxy$ 平面上的投影区域为

$$
 D:
0\le x\le\pi,
\qquad
0\le y\le\pi-x.
$$

令

$$
 P=x+\cos y,
\qquad
Q=y+\cos z,
\qquad
R=z+\cos x.
$$

对于上侧，第二类曲面积分可化为

$$
 I=\iint_D
\left(-Pz_x-Qz_y+R\right)dxdy.
$$

由于

$$
 z_x=z_y=-1,
$$

故

$$
 -Pz_x-Qz_y+R=P+Q+R.
$$

又因 $ x+y+z=\pi$，所以

$$
 P+Q+R
=\pi+\cos x+\cos y+\cos z.
$$

因此

$$
 I=\pi\iint_Ddxdy
+\iint_D\cos x\,dxdy
+\iint_D\cos y\,dxdy
+\iint_D\cos z\,dxdy.
$$

区域 $ D$ 的面积为

$$
 \frac{\pi^2}{2},
$$

故常数项积分为

$$
 \pi\cdot\frac{\pi^2}{2}=\frac{\pi^3}{2}.
$$

再计算

$$
 \begin{aligned}
\iint_D\cos x\,dxdy
&=\int_0^\pi(\pi-x)\cos x\,dx\\
&=2.
\end{aligned}
$$

由区域关于 $ x,y$ 对称，

$$
 \iint_D\cos y\,dxdy=2.
$$

对于 $ z=\pi-x-y$，令 $ u=x+y$，则

$$
 \begin{aligned}
\iint_D\cos z\,dxdy
&=\int_0^\pi u\cos(\pi-u)\,du\\
&=2.
\end{aligned}
$$

综上，

$$
 I=\frac{\pi^3}{2}+2+2+2
=\frac{\pi^3}{2}+6.
$$

相关知识点：对于曲面 $ z=z(x,y)$ 的上侧，第二类曲面积分可利用有向面积向量 $ (-z_x,-z_y,1)\,dxdy$ 化为二重积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

7. 求曲线积分

$$
 I=\int_Cxy\,dx+yz\,dy+xz\,dz,
$$


$ C$ 为椭圆周

$$
 x^2+y^2=1,
\qquad
x+y+z=1,
$$


取逆时针方向。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{I=-\pi}.
$$
</ExamAnswer>

<ExamExplanation>
曲线在 $ Oxy$ 平面上的投影为单位圆。按逆时针方向取参数

$$
 \begin{cases}
x=\cos t,\\
y=\sin t,\\
z=1-\cos t-\sin t,
\end{cases}
\qquad
0\le t\le2\pi.
$$

于是

$$
 dx=-\sin t\,dt,
\qquad
dy=\cos t\,dt,
\qquad
dz=(\sin t-\cos t)\,dt.
$$

代入得

$$
 \begin{aligned}
I
=\int_0^{2\pi}
&\bigl[-\cos t\sin^2t
+\sin t(1-\cos t-\sin t)\cos t\\
&\quad+\cos t(1-\cos t-\sin t)(\sin t-\cos t)\bigr]dt.
\end{aligned}
$$

整理后，可将积分中在一个完整周期上积分为零的三角函数项消去，剩余

$$
 I=-\int_0^{2\pi}\cos^2t\,dt.
$$

因此

$$
 I=-\pi.
$$

也可将原积分中的 $ z=1-x-y$、$ dz=-dx-dy$ 直接代入，先化成平面闭曲线积分，再用格林公式计算。

相关知识点：空间曲线若是曲面与柱面的交线，可利用投影参数化；参数方向应与题目规定的逆时针方向一致。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

8. 求下列区域 $ \Omega$ 的体积

（Ⅰ）

$$
 \Omega:x^2+y^2\le a^2,
\qquad
z\ge0,
\qquad
z\le mx
\qquad(m>0);
$$


（Ⅱ）$ \Omega$ 由

$$
 y^2=a^2-az,
\qquad
x^2+y^2=ax,
\qquad
z=0
\qquad(a>0)
$$


围成；

（Ⅲ）$ \Omega$ 由

$$
 z=x^2+y^2,
\qquad
x+y+z=1
$$


所围成；

（Ⅳ）$ \Omega$ 由曲面

$$
 z=y^2\ (y\ge0),
\qquad
z=4y^2\ (y\ge0),
\qquad
z=x,
\qquad
z=2x,
\qquad
z=4
$$


所围成。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{V_{\mathrm{Ⅰ}}=\frac{2ma^3}{3}},
\qquad
\boxed{V_{\mathrm{Ⅱ}}=\frac{15\pi a^3}{64}},
$$




$$
 \boxed{V_{\mathrm{Ⅲ}}=\frac{9\pi}{8}},
\qquad
\boxed{V_{\mathrm{Ⅳ}}=\frac{16}{5}}.
$$
</ExamAnswer>

<ExamExplanation>
（Ⅰ）由

$$
 0\le z\le mx
$$

可知必须有 $ x\ge0$。在 $ Oxy$ 平面上的投影区域为右半圆域

$$
 D:x^2+y^2\le a^2,
\qquad x\ge0.
$$

因此

$$
 V=\iint_Dmx\,dxdy.
$$

采用极坐标

$$
 x=r\cos\theta,
\qquad
-\frac\pi2\le\theta\le\frac\pi2,
\qquad
0\le r\le a,
$$

得

$$
 \begin{aligned}
V
&=m\int_{-\frac\pi2}^{\frac\pi2}d\theta
\int_0^a r^2\cos\theta\,dr\\
&=m\cdot2\cdot\frac{a^3}{3}
=\frac{2ma^3}{3}.
\end{aligned}
$$

（Ⅱ）由

$$
 y^2=a^2-az
$$

得上边界

$$
 z=a-\frac{y^2}{a}.
$$

柱面在 $ Oxy$ 平面内围成圆域

$$
 D:x^2+y^2\le ax,
$$

即

$$
 \left(x-\frac a2\right)^2+y^2\le\frac{a^2}{4}.
$$

故

$$
 V=\iint_D\left(a-\frac{y^2}{a}\right)dxdy.
$$

圆域 $ D$ 的面积为

$$
 |D|=\frac{\pi a^2}{4}.
$$

又由于该圆域关于 $ x$ 轴对称，且半径为 $ a/2$，有

$$
 \iint_Dy^2\,dxdy
=\frac{\pi}{4}\left(\frac a2\right)^4
=\frac{\pi a^4}{64}.
$$

因此

$$
 \begin{aligned}
V
&=a|D|-\frac1a\iint_Dy^2\,dxdy\\
&=a\cdot\frac{\pi a^2}{4}
-\frac1a\cdot\frac{\pi a^4}{64}\\
&=\frac{15\pi a^3}{64}.
\end{aligned}
$$

（Ⅲ）下曲面为

$$
 z_1=x^2+y^2,
$$

上平面为

$$
 z_2=1-x-y.
$$

投影区域由

$$
 x^2+y^2\le1-x-y
$$

确定，即

$$
 \left(x+\frac12\right)^2
+
\left(y+\frac12\right)^2
\le\frac32.
$$

作平移

$$
 u=x+\frac12,
\qquad
v=y+\frac12,
$$

则区域化为

$$
 u^2+v^2\le\frac32,
$$

且上下曲面之差为

$$
 1-x-y-x^2-y^2
=\frac32-u^2-v^2.
$$

再用极坐标，令

$$
 R^2=\frac32,
$$

则

$$
 \begin{aligned}
V
&=\int_0^{2\pi}d\theta
\int_0^R(R^2-r^2)r\,dr\\
&=2\pi\cdot\frac{R^4}{4}
=\frac{\pi R^4}{2}\\
&=\frac{9\pi}{8}.
\end{aligned}
$$

（Ⅳ）以 $ z$ 为外层变量。由

$$
 z=y^2,
\qquad
z=4y^2,
\qquad y\ge0,
$$

得

$$
 \frac{\sqrt z}{2}\le y\le\sqrt z.
$$

由

$$
 z=x,
\qquad
z=2x
$$

得

$$
 \frac z2\le x\le z.
$$

又由顶面 $ z=4$，有

$$
 0\le z\le4.
$$

故

$$
 \begin{aligned}
V
&=\int_0^4dz
\int_{\frac{\sqrt z}{2}}^{\sqrt z}dy
\int_{\frac z2}^{z}dx\\
&=\int_0^4
\frac z2\cdot\frac{\sqrt z}{2}\,dz\\
&=\frac14\int_0^4z^{\frac32}\,dz
=\frac{16}{5}.
\end{aligned}
$$

相关知识点：求立体体积时，应先根据曲面方程选择最简投影方向；平移坐标常可把偏心圆域化为标准圆域。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

9. 设曲面 $ S$ 是上半球面

$$
 x^2+y^2+z^2=a^2
\qquad(z\ge0,a>0)
$$


被柱面

$$
 x^2+y^2=ax
$$


所割下部分，求 $ S$ 的面积。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{|S|=a^2(\pi-2)}.
$$
</ExamAnswer>

<ExamExplanation>
上半球面可写成

$$
 z=\sqrt{a^2-x^2-y^2}.
$$

令

$$
 r=\sqrt{x^2+y^2},
$$

则球面的面积元为

$$
 dS
=\sqrt{1+z_x^2+z_y^2}\,dxdy
=\frac{a}{\sqrt{a^2-r^2}}\,r\,drd\theta.
$$

柱面在极坐标下为

$$
 r^2=ar\cos\theta,
$$

即

$$
 r=a\cos\theta.
$$

所割下部分在柱面内部，因此

$$
 -\frac\pi2\le\theta\le\frac\pi2,
\qquad
0\le r\le a\cos\theta.
$$

于是

$$
 \begin{aligned}
|S|
&=\int_{-\frac\pi2}^{\frac\pi2}d\theta
\int_0^{a\cos\theta}
\frac{ar}{\sqrt{a^2-r^2}}\,dr\\
&=a^2\int_{-\frac\pi2}^{\frac\pi2}
\left(1-|\sin\theta|\right)d\theta\\
&=a^2(\pi-2).
\end{aligned}
$$

这里出现绝对值，是因为

$$
 \sqrt{a^2-a^2\cos^2\theta}=a|\sin\theta|.
$$

相关知识点：求球面上一块曲面面积时，可将曲面投影到坐标面，并用柱面边界确定极坐标积分区域。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

10. 设曲面

$$
 z=\frac12(x^2+y^2),
$$


其面密度 $ \mu$ 为常数，求该曲面在

$$
 0\le z\le\frac32
$$


部分 $ S$ 的质量与质心。

<ExamSolution>

<ExamAnswer>
质量为


$$
 \boxed{M=\frac{14\pi\mu}{3}}.
$$

质心为

$$
 \boxed{\left(0,0,\frac{29}{35}\right)}.
$$
</ExamAnswer>

<ExamExplanation>
由

$$
 z=\frac{r^2}{2},
\qquad
0\le z\le\frac32,
$$

得

$$
 0\le r\le\sqrt3.
$$

由于

$$
 z_x=x,
\qquad
z_y=y,
$$

故面积元为

$$
 dS=\sqrt{1+x^2+y^2}\,dxdy
=\sqrt{1+r^2}\,r\,drd\theta.
$$

因此质量

$$
 \begin{aligned}
M
&=\mu\iint_SdS\\
&=2\pi\mu\int_0^{\sqrt3}r\sqrt{1+r^2}\,dr\\
&=\frac{2\pi\mu}{3}
\left[(1+r^2)^{\frac32}\right]_0^{\sqrt3}\\
&=\frac{14\pi\mu}{3}.
\end{aligned}
$$

曲面及密度均关于 $ z$ 轴旋转对称，所以

$$
 \bar x=\bar y=0.
$$

关于 $ Oxy$ 平面的静矩为

$$
 \begin{aligned}
M_{xy}
&=\mu\iint_Sz\,dS\\
&=2\pi\mu\int_0^{\sqrt3}
\frac{r^2}{2}\,r\sqrt{1+r^2}\,dr\\
&=\pi\mu\int_0^{\sqrt3}r^3\sqrt{1+r^2}\,dr.
\end{aligned}
$$

令

$$
 u=1+r^2,
$$

则

$$
 \begin{aligned}
\int_0^{\sqrt3}r^3\sqrt{1+r^2}\,dr
&=\frac12\int_1^4(u-1)u^{\frac12}\,du\\
&=\left[\frac15u^{\frac52}-\frac13u^{\frac32}\right]_1^4\\
&=\frac{58}{15}.
\end{aligned}
$$

故

$$
 M_{xy}=\frac{58\pi\mu}{15},
$$

从而

$$
 \bar z
=\frac{M_{xy}}{M}
=\frac{\frac{58\pi\mu}{15}}
{\frac{14\pi\mu}{3}}
=\frac{29}{35}.
$$

相关知识点：均匀旋转曲面的质心必在旋转轴上；曲面质量与静矩均使用第一类曲面积分计算。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

11. 设质点 $ P$ 沿以 $ AB$ 为直径的下半圆周，从点 $ A(1,2)$ 运动到 $ B(3,4)$ 的过程中，受变力 $ F$ 的作用，$ F$ 的大小等于点 $ P$ 到原点 $ O$ 之距离，方向垂直于线段 $ OP$，与 $ y$ 轴正向的夹角小于 $ \frac\pi2$，求变力 $ F$ 对质点 $ P$ 做的功。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{W=2\pi-2}.
$$
</ExamAnswer>

<ExamExplanation>
设质点位置为 $ P(x,y)$，则

$$
 \overrightarrow{OP}=(x,y).
$$

与 $ \overrightarrow{OP}$ 垂直且模长等于

$$
 \sqrt{x^2+y^2}
$$

的两个向量为

$$
 (-y,x),
\qquad
(y,-x).
$$

在所给圆弧上 $ x>0$，而题目要求力与 $ y$ 轴正向夹角小于 $ \pi/2$，即力的第二分量为正，因此

$$
 \mathbf F=(-y,x).
$$

以 $ AB$ 为直径的圆的圆心为

$$
 C(2,3),
$$

半径为

$$
 R=\frac{|AB|}{2}=\sqrt2.
$$

下半圆弧从 $ A$ 到 $ B$ 可参数化为

$$
 \begin{cases}
x=2+\sqrt2\cos t,\\
y=3+\sqrt2\sin t,
\end{cases}
\qquad
\frac{5\pi}{4}\le t\le\frac{9\pi}{4}.
$$

于是

$$
 dx=-\sqrt2\sin t\,dt,
\qquad
dy=\sqrt2\cos t\,dt.
$$

所做功为

$$
 W=\int_C\mathbf F\cdot d\mathbf r
=\int_C(-y\,dx+x\,dy).
$$

代入参数方程，得

$$
 \begin{aligned}
W
&=\int_{\frac{5\pi}{4}}^{\frac{9\pi}{4}}
\left(2\sqrt2\cos t+3\sqrt2\sin t+2\right)dt\\
&=4-6+2\pi\\
&=2\pi-2.
\end{aligned}
$$

相关知识点：根据力的模长和方向条件先确定向量场，再将曲线参数化计算第二类曲线积分；参数区间必须对应题目指定的下半圆弧及运动方向。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

12. 设有平面光滑曲线

$$
 l:x=x(t),
\qquad
y=y(t),
\qquad
z=0,
\qquad
t\in[\alpha,\beta],
$$


以及空间光滑曲线

$$
 L:x=x(t),
\qquad
y=y(t),
\qquad
z=f(x(t),y(t)),
\qquad
t\in[\alpha,\beta],
$$


$ t=\alpha,t=\beta$ 分别是起点与终点的参数。

（Ⅰ）试说明 $ l,L$ 及曲面 $ S:z=f(x,y)$ 的关系；

（Ⅱ）若 $ P,Q,R$ 连续，$ f(x,y)$ 有连续的偏导数，求证：

$$
 \begin{aligned}
&\int_LP(x,y,z)\,dx+Q(x,y,z)\,dy+R(x,y,z)\,dz\\
&\quad=\int_l\left[P(x,y,f(x,y))+\frac{\partial f(x,y)}{\partial x}R(x,y,f(x,y))\right]dx\\
&\qquad+\left[Q(x,y,f(x,y))+\frac{\partial f(x,y)}{\partial y}R(x,y,f(x,y))\right]dy.
\end{aligned}
$$

<ExamSolution>

<ExamAnswer>
（Ⅰ）曲线 $ L$ 位于曲面


$$
 S:z=f(x,y)
$$

上，平面曲线 $ l$ 是空间曲线 $ L$ 在 $ Oxy$ 平面上的投影。

（Ⅱ）题中所给等式成立。
</ExamAnswer>

<ExamExplanation>
（Ⅰ）空间曲线 $ L$ 上任一点均具有形式

$$
 \bigl(x(t),y(t),f(x(t),y(t))\bigr),
$$

所以它满足曲面方程

$$
 z=f(x,y),
$$

从而

$$
 L\subset S.
$$

将 $ L$ 上每一点沿平行于 $ z$ 轴的方向投影到 $ Oxy$ 平面，所得点为

$$
 (x(t),y(t),0),
$$

这正是曲线 $ l$。因此 $ l$ 是 $ L$ 在 $ Oxy$ 平面上的投影。

（Ⅱ）沿曲线 $ L$，有

$$
 z=f(x,y).
$$

由于 $ f$ 有连续偏导数，其全微分为

$$
 dz=f_x(x,y)\,dx+f_y(x,y)\,dy.
$$

因此

$$
 \begin{aligned}
&P(x,y,z)\,dx+Q(x,y,z)\,dy+R(x,y,z)\,dz\\
&=P(x,y,f(x,y))\,dx
+Q(x,y,f(x,y))\,dy\\
&\quad+R(x,y,f(x,y))
\left[f_x(x,y)\,dx+f_y(x,y)\,dy\right]\\
&=\left[P(x,y,f(x,y))
+f_x(x,y)R(x,y,f(x,y))\right]dx\\
&\quad+
\left[Q(x,y,f(x,y))
+f_y(x,y)R(x,y,f(x,y))\right]dy.
\end{aligned}
$$

空间曲线 $ L$ 与其投影曲线 $ l$ 使用相同的参数 $ t$，且起点、终点参数一致，因此对上式沿曲线积分，即得

$$
 \begin{aligned}
&\int_LP(x,y,z)\,dx+Q(x,y,z)\,dy+R(x,y,z)\,dz\\
&=\int_l\left[P(x,y,f(x,y))+f_x(x,y)R(x,y,f(x,y))\right]dx\\
&\quad+
\left[Q(x,y,f(x,y))+f_y(x,y)R(x,y,f(x,y))\right]dy.
\end{aligned}
$$

故命题得证。

相关知识点：空间曲线位于显式曲面 $ z=f(x,y)$ 上时，可利用

$$
 dz=f_x\,dx+f_y\,dy
$$

将空间第二类曲线积分化为其在 $ Oxy$ 平面投影曲线上的积分。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

13. 设 $ P(x,y,z),Q(x,y,z),R(x,y,z)$ 在区域 $ \Omega$ 连续，

$$
 \Gamma:x=x(t),
\qquad
y=y(t),
\qquad
z=z(t)
$$


是 $ \Omega$ 中一条光滑曲线，起点 $ A$、终点 $ B$ 分别对应参数 $ t_A,t_B$，又设在 $ \Omega$ 上存在函数 $ u(x,y,z)$，使得

$$
 du=P\,dx+Q\,dy+R\,dz.
$$


求证：

$$
 I=\int_\Gamma P\,dx+Q\,dy+R\,dz=u\Big|_A^B.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_\Gamma P\,dx+Q\,dy+R\,dz=u(B)-u(A)}.
$$
</ExamAnswer>

<ExamExplanation>
由

$$
 du=P\,dx+Q\,dy+R\,dz
$$

可知

$$
 u_x=P,
\qquad
u_y=Q,
\qquad
u_z=R.
$$

沿曲线 $ \Gamma$ 作复合函数

$$
 U(t)=u(x(t),y(t),z(t)).
$$

由链式法则，

$$
 \begin{aligned}
U'(t)
&=u_x\frac{dx}{dt}+u_y\frac{dy}{dt}+u_z\frac{dz}{dt}\\
&=P(x(t),y(t),z(t))x'(t)\\
&\quad+Q(x(t),y(t),z(t))y'(t)\\
&\quad+R(x(t),y(t),z(t))z'(t).
\end{aligned}
$$

因此

$$
 \begin{aligned}
I
&=\int_{t_A}^{t_B}
\left[P x'(t)+Q y'(t)+R z'(t)\right]dt\\
&=\int_{t_A}^{t_B}U'(t)\,dt\\
&=U(t_B)-U(t_A)\\
&=u(B)-u(A).
\end{aligned}
$$

即

$$
 I=u\Big|_A^B.
$$

故命题得证。

相关知识点：当第二类曲线积分的被积微分式是某函数的全微分时，积分只与起点、终点有关，与具体路径无关。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

14. 设 $ f(x)$ 在区间 $ [0,1]$ 上连续，请用重积分方法证明：

$$
 \int_0^1f(x)\,dx\int_x^1f(y)\,dy
=\frac12\left[\int_0^1f(x)\,dx\right]^2.
$$

<ExamSolution>

<ExamAnswer>
$$
 \boxed{
\int_0^1 f(x)\left(\int_x^1f(y)\,dy\right)dx
=\frac12\left(\int_0^1f(x)\,dx\right)^2}.
$$
</ExamAnswer>

<ExamExplanation>
记

$$
 D_1=\{(x,y)\mid 0\le x\le y\le1\}.
$$

则左端可写为

$$
 \int_0^1 f(x)\left(\int_x^1f(y)\,dy\right)dx
=\iint_{D_1}f(x)f(y)\,dxdy.
$$

再记

$$
 D_2=\{(x,y)\mid 0\le y\le x\le1\}.
$$

区域 $ D_1$ 与 $ D_2$ 关于直线 $ y=x$ 对称，而被积函数

$$
 f(x)f(y)
$$

交换 $ x,y$ 后不变，因此

$$
 \iint_{D_1}f(x)f(y)\,dxdy
=
\iint_{D_2}f(x)f(y)\,dxdy.
$$

正方形

$$
 D=[0,1]\times[0,1]
$$

除去面积为零的对角线后，恰由 $ D_1,D_2$ 组成，故

$$
 \begin{aligned}
\iint_Df(x)f(y)\,dxdy
&=2\iint_{D_1}f(x)f(y)\,dxdy.
\end{aligned}
$$

另一方面，由积分的可分离性，

$$
 \begin{aligned}
\iint_Df(x)f(y)\,dxdy
&=\int_0^1\int_0^1f(x)f(y)\,dydx\\
&=\left(\int_0^1f(x)\,dx\right)
\left(\int_0^1f(y)\,dy\right)\\
&=\left(\int_0^1f(x)\,dx\right)^2.
\end{aligned}
$$

因此

$$
 \int_0^1 f(x)\left(\int_x^1f(y)\,dy\right)dx
=\frac12\left(\int_0^1f(x)\,dx\right)^2.
$$

故命题得证。

相关知识点：利用积分区域关于 $ y=x$ 的对称性，可将三角形区域上的二重积分转化为正方形区域积分的一半。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

15. 设半径为 $ R$ 的球面 $ \Sigma$ 的球心在定球面

$$
 x^2+y^2+z^2=a^2
\qquad(a>0)
$$


上，问 $ R$ 为何值时球面 $ \Sigma$ 在定球面内部的那部分面积最大？

<ExamSolution>

<ExamAnswer>
$$
 \boxed{R=\frac{4a}{3}}.
$$




此时最大面积为

$$
 \boxed{S_{\max}=\frac{32\pi a^2}{27}}.
$$
</ExamAnswer>

<ExamExplanation>
由于问题具有旋转对称性，不妨设球面 $ \Sigma$ 的球心为

$$
 C(a,0,0).
$$

于是 $ \Sigma$ 的方程为

$$
 (x-a)^2+y^2+z^2=R^2.
$$

它与定球面

$$
 x^2+y^2+z^2=a^2
$$

的交圆所在平面可由两式相减得到：

$$
 -2ax+a^2=R^2-a^2,
$$

即

$$
 x=a-\frac{R^2}{2a}.
$$

相对于球心 $ C$，交平面到球心的距离为

$$
 d=\frac{R^2}{2a}.
$$

球面 $ \Sigma$ 位于定球内部的部分是朝向原点的一球冠，其高为

$$
 H=R-d=R-\frac{R^2}{2a}.
$$

要有非空球冠，须

$$
 0<R\le2a.
$$

球冠面积为

$$
 \begin{aligned}
S(R)
&=2\pi RH\\
&=2\pi R\left(R-\frac{R^2}{2a}\right)\\
&=2\pi R^2-\frac{\pi}{a}R^3.
\end{aligned}
$$

求导得

$$
 S'(R)=4\pi R-\frac{3\pi}{a}R^2
=\pi R\left(4-\frac{3R}{a}\right).
$$

在 $ 0<R<2a$ 内，唯一驻点为

$$
 R=\frac{4a}{3}.
$$

并且

$$
 S''(R)=4\pi-\frac{6\pi}{a}R,
$$

故

$$
 S''\left(\frac{4a}{3}\right)=-4\pi<0.
$$

所以面积在

$$
 R=\frac{4a}{3}
$$

时取得最大值。代入得

$$
 \begin{aligned}
S_{\max}
&=2\pi\left(\frac{4a}{3}\right)^2
-\frac{\pi}{a}\left(\frac{4a}{3}\right)^3\\
&=\frac{32\pi a^2}{27}.
\end{aligned}
$$

相关知识点：两球相交时，交圆位于两球方程相减所得的平面内；球冠面积公式为

$$
 S=2\pi RH.
$$
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

16. 求一段均匀圆柱面

$$
 S:x^2+y^2=R^2
\qquad(0\le z\le h)
$$


对原点处单位质点的引力。假设该圆柱面的面密度为 $ 1$。

<ExamSolution>

<ExamAnswer>
引力方向沿 $ z$ 轴正向，其大小为


$$
 \boxed{
F=2\pi k\left(1-\frac{R}{\sqrt{R^2+h^2}}\right)}.
$$

相应的引力向量为

$$
 \boxed{
\mathbf F=2\pi k\left(1-\frac{R}{\sqrt{R^2+h^2}}\right)\mathbf k}.
$$

其中 $ k$ 为万有引力常数。
</ExamAnswer>

<ExamExplanation>
将圆柱面参数化为

$$
 \mathbf r(\theta,z)
=(R\cos\theta,R\sin\theta,z),
$$

其中

$$
 0\le\theta\le2\pi,
\qquad
0\le z\le h.
$$

圆柱面的面积元为

$$
 dS=R\,d\theta\,dz.
$$

由于面密度为 $ 1$，质量元为

$$
 dm=dS=R\,d\theta\,dz.
$$

该质量元到原点的距离为

$$
 \rho=\sqrt{R^2+z^2}.
$$

它对原点处单位质点产生的引力向量为

$$
 d\mathbf F
=k\frac{dm}{\rho^3}
(R\cos\theta,R\sin\theta,z).
$$

由于圆柱面关于 $ z$ 轴旋转对称，积分后水平方向分量相互抵消，只剩 $ z$ 方向分量。因此

$$
 \begin{aligned}
F_z
&=k\int_0^h\int_0^{2\pi}
\frac{z}{(R^2+z^2)^{3/2}}
R\,d\theta\,dz\\
&=2\pi kR\int_0^h
\frac{z}{(R^2+z^2)^{3/2}}\,dz.
\end{aligned}
$$

而

$$
 \int\frac{z}{(R^2+z^2)^{3/2}}\,dz
=-\frac{1}{\sqrt{R^2+z^2}},
$$

故

$$
 \begin{aligned}
F_z
&=2\pi kR
\left(\frac1R-\frac{1}{\sqrt{R^2+h^2}}\right)\\
&=2\pi k\left(1-\frac{R}{\sqrt{R^2+h^2}}\right).
\end{aligned}
$$

所以

$$
 \mathbf F
=F_z\mathbf k.
$$

相关知识点：对轴对称质量分布求引力时，可先利用对称性消去横向分量，只积分轴向分量。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>

---

<ExamQuestion>

17. 求

$$
 \lim_{R\to+\infty}
\oint_L\frac{x\,dy-y\,dx}{(x^2+xy+y^2)^2},
$$


其中 $ L:x^2+y^2=R^2$ 的正方向。

<ExamSolution>

<ExamAnswer>
$$
 \boxed{0}.
$$
</ExamAnswer>

<ExamExplanation>
取正向参数方程

$$
 x=R\cos t,
\qquad
y=R\sin t,
\qquad
0\le t\le2\pi.
$$

则

$$
 dx=-R\sin t\,dt,
\qquad
dy=R\cos t\,dt,
$$

从而

$$
 x\,dy-y\,dx=R^2\,dt.
$$

又

$$
 \begin{aligned}
x^2+xy+y^2
&=R^2\left(\cos^2t+\sin t\cos t+\sin^2t\right)\\
&=R^2\left(1+\frac12\sin2t\right).
\end{aligned}
$$

因此

$$
 \begin{aligned}
\oint_L\frac{x\,dy-y\,dx}{(x^2+xy+y^2)^2}
&=\frac1{R^2}
\int_0^{2\pi}
\frac{dt}{\left(1+\frac12\sin2t\right)^2}.
\end{aligned}
$$

由于

$$
 1+\frac12\sin2t\ge\frac12,
$$

故

$$
 0\le
\int_0^{2\pi}
\frac{dt}{\left(1+\frac12\sin2t\right)^2}
\le8\pi.
$$

于是

$$
 0\le
\oint_L\frac{x\,dy-y\,dx}{(x^2+xy+y^2)^2}
\le\frac{8\pi}{R^2}.
$$

令 $ R\to+\infty$，由夹逼定理得

$$
 \lim_{R\to+\infty}
\oint_L\frac{x\,dy-y\,dx}{(x^2+xy+y^2)^2}=0.
$$

事实上，参数积分还可算得

$$
 \int_0^{2\pi}
\frac{dt}{\left(1+\frac12\sin2t\right)^2}
=\frac{16\pi}{3\sqrt3},
$$

故原曲线积分等于

$$
 \frac{16\pi}{3\sqrt3R^2},
$$

其极限仍为 $ 0$。

相关知识点：当曲线按尺度 $ R$ 放大时，可通过参数化判断分子、分母的齐次阶数；本题积分整体为 $ R^{-2}$ 量级。
</ExamExplanation>

</ExamSolution>

</ExamQuestion>
